# CLARIN-DSpace v9 — Remediation Plan (to genuinely finish the upgrade)

> Generated 2026-07-08 by a multi-agent planning workflow (9 domain planners -> adversarial critics -> revision -> synthesis -> final evaluator; 29 agents), grounded in the 2026-07-08 adversarial audit (8 auditors + verifiers, 57 agents) of BE PR #1339 / FE PR #1316.
> **Prime directive: REUSE, don't rewrite.** Every work item names the exact `origin/dtq-dev` source to port and the v9 adaptation. Machine-checkable acceptance criteria live in `CLARIN_V9_ACCEPTANCE_CRITERIA.md`.
> Push targets: **only** BE PR #1339 (`ufal/clarin-dspace-upgrade-v9`) and FE PR #1316. No new branches/PRs.

## Executive summary

The CLARIN->DSpace 9 upgrade is NOT done: PRs #1339/#1316 are green but core CLARIN features are broken or entirely absent on the live stack, and the progress file falsely claims end-to-end success. The live re-baseline changes the shape of the work in one decisive way: the C1 item-file-listing REST tier already exists and returns HTTP 200 on :18080, but as SIX UNTRACKED working-tree files — so the very first action is to COMMIT them under a concurrency guard, not port from scratch. Overwhelmingly this remediation is REUSE, not rewrite: for nearly every gap the fix exists byte-identical (or as a small delta) in origin/dtq-dev and needs only a mechanical v9 adaptation — jakarta.* imports, the NAME->PLURAL_NAME REST bean-resolution fix (the same class of bug behind M2/M3/BE-CHK-5), registering models in provide-core models[], or hand-reconciling ~3 heavily-drifted modified-vanilla files (ItemUtils, XOAI, MetadataValidation) that will not patch cleanly. The plan is sequenced so foundations that unblock compilation and fresh-deploy correctness land first (config include for clarin-dspace.cfg, registries, and the shared DCInput.ComplexDefinitions primitive that OAI depends on), then the three security/UX criticals C1/C2/C3, then the OAI/CMDI/BibTeX export layer (C5) plus submission workflow, then the FE GUIs/facets/static pages (C4, M1-M8), then the deferred test/CI/Docker port (M11) and native-v9 adoption plus an honest progress-file rewrite. Acceptance is machine-checkable throughout: because the production assetstore is not imported, every LIVE check is a negative gate (401/403) or metadata-only 200 that short-circuits before file read, and positive byte delivery is proven only by the ported ITs that stand up their own test assetstore. All work pushes only to BE #1339 and FE #1316; no new branches or PRs.

## Final evaluator verdict on this plan

**Verdict: plan-has-gaps** — the gaps found are folded into this document as addendum work items **GAP-1..GAP-5** (see Addendum) and as acceptance-criteria hardening rules; with them applied the evaluator's objections are addressed.

The plan is unusually thorough and its claims are grounded, not vibes. I cross-checked every audit finding against a work item and against the live repos. Coverage of the criticals and majors is real: every C1-C5 and M1-M13 maps to concrete work items with dtq-dev source paths, and the mediums (submission steps S2-S7, autocomplete/metadatavalues 404, ISO facet, license-selector, ORCID bean, type-bind, Czech i18n, curation, CLI, ~120 tests) and the documented-deferred/env-gated set (assetstore, S3, Shibboleth/WAYF, Matomo PDF e2e, ItemVersionLinker CLI) are all named and flagged. I verified three things a skeptic would suspect were missing and found them handled: (a) the 6 C1 metadata-bitstream files are genuinely untracked (git status '??'), exactly as BE-BASE-0 assumes; (b) CLARIN Flyway migrations (V7.2 Lindat_Clarin_schema, V7.6 Clarin_token, Matomo_report_registry, Preview_Tables) ARE present at HEAD, so DB schema is NOT an unowned gap; (c) the dspace-oai delta is ~25 classes (HEAD 96 vs dtq-dev 121), consistent with BE-OAI-1's '28 classes'. The DoD gates (a)-(f) are objective and the sequencing (BE-BASE-0 first, DCInput.ComplexDefinitions before OAI, oai import before ListMetadataFormats verify, M13 config before downstream props) is sound. It is NOT plan-complete because a few genuine holes remain that could let the exact green-but-broken failure mode recur: an under-specified C2 (the ClarinBitstreamServiceImpl port is not named though it is absent at HEAD and the token subsystem has no positive acceptance check), a systemic verification ceiling where most live ACs are negative gates that also pass under total breakage, and thin/unowned spots (FE preview viewer component, fresh-deploy Solr configset). These are under-specification and verification gaps rather than whole-missing features, so plan-has-gaps rather than plan-inadequate.

## Phased execution plan

Phases are ordered so each unblocks the next; criticals land first. Phase entries reference audit codes (C1-C5, M1-M13, S2-S7...) and/or catalog work-item IDs; the full catalog with reuse sources is in the Work-item catalog section.

### Phase 0 — Foundations & config (fresh-deploy correctness + shared primitives)

Make a fresh clone actually load CLARIN config and registries, and land the shared code primitives that later phases compile against, so nothing downstream builds green-but-broken.

Work items: `M13-config-definition-include-clarin-cfg`, `M12-metadata-bitstream-registries`, `M9-default-license`, `BE-SUB-DCINPUT-COMPLEXDEFS`, `M10-modified-vanilla-deltas`, `M11-docker-config-portion`

Exit criteria:
- [ ] git-tracked dspace/config/config-definition.xml (or spring config) includes clarin-dspace.cfg so a fresh clone loads all CLARIN props without the gitignored local.cfg; GET /server/api/config/properties returns a known CLARIN key
- [ ] local-types.xml contains the ~30 local.* fields, bitstream-formats.xml the archive formats, dublin-core-types.xml dc.rights.label (git grep proofs)
- [ ] org.dspace.app.util.DCInput.ComplexDefinitions is present and tracked; mvn -q -pl dspace-api -am -DskipTests compile exits 0
- [ ] dspace/config/default.license is the CLARIN legal text (not the vanilla placeholder), consistent with default_cs.license
- [ ] Modified-vanilla deltas applied (InstallItemServiceImpl.addLanguageNameToMetadata, ItemConverter, Application encoded-slash handle firewall, WorkspaceItemRestRepository license patch ops)

### Phase 1 — Criticals: file listing, download gate, restricted-access (C1/C2/C3)

Restore the item Files box, enforce the license/download-token gate, and turn the restricted-item 500 NPE into a clean 401 — with the C1 tier actually committed so PR #1339 contains it.

Work items: `BE-BASE-0`, `BE-GATE-1`, `BE-REST-2`, `BE-FILES-3`, `BE-FILES-4`, `BE-CHK-5`, `BE-ZIP-7`, `BE-PREVIEW-8`, `BE-TEST-6`

Exit criteria:
- [ ] git ls-files shows all 6 C1 metadata-bitstream files tracked and reachable from ufal/clarin-dspace-upgrade-v9 HEAD; git status --porcelain shows none as '??'
- [ ] byHandle returns 200 with 2 rows and hrefs matching the /server/api/core/bitstreams/handle/... regex; FE item page renders exactly 2 file rows
- [ ] Anonymous GET /content of the confirmation=1 bitstream faf919b4 returns 401 (was 500); open bitstream /authrn f15230ca stays 200
- [ ] Anonymous /api/pid/find for a private item returns 401 not 500 (covered by extended IdentifierRestRepositoryIT)
- [ ] /checksum returns 200 with databaseChecksum.value matching the DB md5; @Component uses PLURAL_NAME
- [ ] allzip for a gated item returns 401/403 with NO application/zip Content-Type and NO attachment header
- [ ] mvn -pl dspace-server-webapp test -Dtest=MetadataBitstreamRestRepositoryIT,BitstreamByHandleRestControllerIT,AuthorizationRestControllerIT,IdentifierRestRepositoryIT,MetadataBitstreamControllerIT BUILD SUCCESS, 0 failures/errors

### Phase 2 — OAI/CMDI/BibTeX export layer + submission workflow (C5, S2-S7)

Bring the inert CLARIN crosswalks to life (28 Java classes + xoai.xml/oai.cfg wiring + OAI solr import) so ListMetadataFormats/GetRecord/ref-box export/CMDI upload work, and re-activate the CLARIN submission steps.

Work items: `BE-OAI-1`, `BE-OAI-2`, `BE-OAI-3`, `BE-OAI-4`, `BE-OAI-5`, `BE-OAI-8`, `BE-OAI-9`, `BE-OAI-10`, `S5-CMDI-upload`, `S2-complex-fields`, `S3-admin-only-fields`, `S4-autocomplete-metadatavalues`, `S6-clarin-license-step`, `S7-notice-step`, `L4-field-acl`, `SUB-forms-en-item-submission-xml`

Exit criteria:
- [ ] GET /server/oai/request?verb=ListMetadataFormats returns cmdi, olac, oai_metasharev2, bibtex, elg, oai_datacite prefixes
- [ ] GET /api/core/refbox/citations?type=bibtex returns a valid BibTeX record (no 'Unknown metadata format')
- [ ] GetRecord with metadataPrefix=xoai emits restrictedAccess/itemId/owningCollection and lindat-form bitstream URLs; TEXT/THUMBNAIL/SWORD bundles excluded
- [ ] git ls-tree HEAD counts 21 function classes + SharedSaxonProcessor + ColComFilter + 5 utils present; mvn -q -pl dspace-oai -am -DskipTests compile exits 0
- [ ] CMDIRestController /cmdi endpoint reachable and jakarta-migrated; CMDIFileBundleMaintainer validation invoked from MetadataValidation
- [ ] item-submission.xml + submission-forms.xml (EN) match the ported CS forms; complex/autocomplete/admin-only/license/notice steps render and function; /api/core/metadatavalues returns 200

### Phase 3 — FE GUIs, facets, static pages, auth features (C4, M1-M8)

Fix the FE-side regressions where BE data already exists: admin GUIs that render blank, the empty ISO-language facet, the 82 static pages 404, and the auth/eperson gaps.

Work items: `C4-static-files-assets`, `M1-license-manage-table-ngbactivemodal`, `M2-handle-table-model-registration`, `M3-epic-plural-name-bean`, `M4-iso-language-facet-fe`, `M5-share-submission-button`, `M6-item-license-tab-route-guard`, `M7-personal-access-tokens`, `M8-eperson-user-registration-hook`

Exit criteria:
- [ ] FE /licenses/manage-table renders rows (NgbActiveModal provider restored in standalone component); /handle-table shows rows (Handle model in provide-core models[]); ePIC GUI populated (PLURAL_NAME bean)
- [ ] Search 'Language (ISO)' facet is non-empty (FilterType.isoLanguage enum + renderer ported)
- [ ] ~82 static HTML pages return 200 (src/static-files/ + angular.json asset entry present); e.g. /static/cookies.html 200
- [ ] MyDSpace share button reaches /share-submission; item-edit license tab route+guard+CanManageLicense FeatureID present and tab loads ItemLicenseMapperComponent
- [ ] clarin_token authenticates a REST call and can be created via CLI; admin EPerson create writes a user_registration DB row

### Phase 4 — Tests, CI/CD, Docker, CLI/curation port (M11 + coverage)

Port the dtq-dev CI/Docker/scripts mandate and the ~120 CLARIN test classes, and register the CLI/curation commands, so the green PR is backed by real coverage and reproducible infra.

Work items: `M11-ci-workflows-be-fe`, `M11-dockerfiles-scripts`, `TEST-clarin-it-builders-matchers`, `CLI-launcher-clarin-token-item-version-linker-file-preview`, `CURATION-tasks-registration`, `I18N-cs-json5-keys`

Exit criteria:
- [ ] dtq-dev workflow files present (BE 13, FE 18) and Dockerfile CLARIN customizations + scripts/ tree (incl. matomo compose) restored
- [ ] Ported CLARIN ITs/builders/matchers compile and the targeted Surefire suites run > 0 tests with 0 failures
- [ ] launcher.xml registers clarin-token, item-version-linker, file-preview; curation tasks (checkhandles/metadataqa/RequiredMetadata/reporters/ItemHandleChecker) registered
- [ ] Czech i18n cs.json5 gap closed (missing 598/679 fork keys added)

### Phase 5 — Native-v9 adoption, polish, honest progress file

Fold in the low-risk native-v9 wins, apply the small regression fixes, resolve conflicts with CLARIN features, and rewrite the progress file to match reality.

Work items: `NATIVE-matomo-tracking`, `CLARIN-matomo-report-fe-half`, `NATIVE-signposting`, `NATIVE-accessibility-settings`, `ORCID-caching-connector-bean-fix`, `NATIVE-request-a-copy-reconcile`, `OPEN-license-selector-widget`, `TYPE-BIND-late-load-new-version-button`, `PROGRESS-FILE-honest-rewrite`

Exit criteria:
- [ ] GET /api/config/properties/matomo.enabled returns true once keys set in git-tracked modules/matomo.cfg; MatomoEventListener unit tests green (e2e documented as M11-docker-gated)
- [ ] CLARIN Matomo report subscription BE 200 + matomo_report_registry row + FE button spec green (lr.statistics.api.* keys set)
- [ ] Signposting enabled (do-not-regress); native Accessibility settings page reachable (noted as NOT the EN 301 549 legal statement); ORCID bean re-added so enabling ORCID does not NPE
- [ ] Request-a-Copy reconciled against the C2 license/token gate; OPEN license-selector widget bundled; Orejime verified byte-identical (no port needed)
- [ ] CLARIN_DSPACE_V9_PROGRESS.md rewritten: no false 'END-TO-END green'; each C/M marked with its objective acceptance status

## Critical path & dependencies

Critical path: BE-BASE-0 must run FIRST and in isolation — the 6 C1 files are untracked and the clarinv9-dspace container shows an active parallel process, so it must git-diff and stage ONLY those 6 paths (never git add -A) before any C1 verify (BE-FILES-3/4) can be trusted. Phase 0's BE-SUB-DCINPUT-COMPLEXDEFS is a hard compile blocker for BE-OAI-1 (SpecialItemService imports DCInput.ComplexDefinitions), which in turn gates the entire OAI layer BE-OAI-2..10; and BE-OAI-8 (populate the empty OAI solr core via `dspace oai import`) must precede BE-OAI-2/4 verification because ListMetadataFormats/GetRecord assert against real records. Phase 0's M13 config include underpins both the C2 gate props and the Matomo/preview flags — if clarin-dspace.cfg loads only via gitignored local.cfg, every downstream prop silently vanishes on fresh clone. BE-GATE-1 (C2) is the linchpin of Phase 1's negative gates: it must land before BE-FILES-4 (by-handle download must inherit the gate) and BE-ZIP-7 (allzip pre-auth pass). Cross-domain: C4 static-files (Phase 3) blocks the Orejime learn-more/cookies link in Phase 5; C2 (Phase 1) blocks the native Request-a-Copy reconciliation decision in Phase 5; M11 docker (Phase 4) blocks Matomo end-to-end proof in Phase 5. The FE checksum model field names (databaseChecksum/activeStore/synchronizedStore) must be cross-checked against BE-CHK-5's ported BitstreamChecksumRest shape so the two halves bind.

## Definition-of-Done gates (what flips each criterion to MET)

- [ ] (a) runs-in-docker: the clarinv9 stack serves all restored endpoints — byHandle 200, /content 401 for gated, ListMetadataFormats with CLARIN prefixes, refbox bibtex 200, and the FE admin GUIs/facets/static pages render — without a rebuild that loses untracked work (BE-BASE-0 committed).
- [ ] (b) all features: every audit item C1-C5 and M1-M13 (plus the medium submission/OAI/i18n items) has its specific objective acceptance check passing — negative-gate curl probes on :18080 for env-gated bytes, and ITs for positive-byte paths.
- [ ] (c) tests: mvn -pl dspace-server-webapp/dspace-oai targeted Surefire suites (MetadataBitstream*, BitstreamByHandle*, Authorization*, Identifier*, MetadataBitstreamController*, plus the ~120 ported CLARIN ITs) run > 0 tests with 0 failures/errors, and the FE matomo-subscription-button and facet specs are green.
- [ ] (d) PRs green: BE #1339 and FE #1316 CI pass AND git ls-files proves the ported files are actually committed (not merely present in the working tree) — specifically the 6 C1 files, the 28 OAI classes, checksum tier, and FE static-files/models.
- [ ] (e) no unresolved critical: C1 (byHandle 200 + FE 2 rows), C2 (gated /content 401, open 200), C3 (private /pid/find 401), C4 (static pages 200), C5 (ListMetadataFormats CLARIN prefixes + refbox 200) all pass their machine checks.
- [ ] (f) progress-file honest: CLARIN_DSPACE_V9_PROGRESS.md rewritten to remove the false 'I2 END-TO-END green' and every other unbacked claim; each C/M line carries its objective acceptance status and explicitly flags the documented-deferred/env-gated items (assetstore, S3, Shibboleth/WAYF, Matomo PDF e2e, ItemVersionLinker CLI).

## Cross-cutting risks

- Concurrency/clobber: the C1 tier and other fixes live as untracked working-tree files while a parallel process (container 'Up 12 minutes') edits the same area; every commit must git-diff and stage explicit paths only, re-checking git status immediately before commit, never git add -A.
- Env-gated verification ceiling: the production assetstore is not imported, so positive byte delivery (200 + content) for /content, /handle/, allzip, and populated preview fileInfo cannot be proven live — only via ITs that stand up their own test assetstore. Every live AC is deliberately a negative gate (401/403) or a metadata-only 200 that short-circuits before file read.
- Bean-resolution gotcha (NAME->PLURAL_NAME): the same bug class that left M2/M3 broken will silently 404 BE-CHK-5 and any REST repo/link ported verbatim from dtq-dev; grep-level ACs must assert PLURAL_NAME and provide-core models[] registration.
- Modified-vanilla hand-reconcile: ItemUtils (~99 lines), XOAI (~72 lines), MetadataValidation, and AuthorizeServiceImpl carry heavy v9 drift — fork patches will NOT apply cleanly and must be reconciled onto the HEAD 5-arg overloads/methods, not blindly copied (fork used English.plural / 4-arg overloads that no longer match).
- Config placement (M13): CLARIN props loaded only via gitignored local.cfg vanish on fresh clone; Matomo tracking keys must live in git-tracked modules/matomo.cfg (not clarin-dspace.cfg, whose matomo.* keys are 'no longer read'), while the CLARIN report engine reads lr.statistics.api.* from clarin-dspace.cfg — mixing these breaks both subsystems independently.
- Push restriction: only BE #1339 / FE #1316 — no new branches/PRs; all commits must target the existing ufal/clarin-dspace-upgrade-v9 branches.
- Fixture fragility: only one gated (faf919b4, confirmation=1) and one open (f15230ca, confirmation=0) UUID are pinned on the dev-5 dataset; a data refresh changes them, so self-seeded ITs are the durable proof and curl probes only a fast smoke test.
- Matomo e2e and Docker/CI depend on the deferred M11 port; without dtq-dev docker/matomo-w-db.yml only unit-level wiring can be proven, and adopting native tracking accepts the loss of OAI-PMH harvest statistics (no native OAI tracker), which interacts with C5.

## Addendum — evaluator gap closures (mandatory, same priority as their parent items)

### GAP-1 — C2: port ClarinBitstreamServiceImpl.java (critical)

BE-GATE-1 wires the AuthorizeServiceImpl hook but the download-token/license service ClarinBitstreamServiceImpl exists in origin/dtq-dev and is ABSENT at HEAD. Port it (strip S3-sync deps the same way the BitstreamByHandle port did) so the token path can actually issue/honor download tokens.

### GAP-2 — C2: positive download-token acceptance check (critical)

All C2 criteria are negative gates. Add a positive AC: complete the license-agreement/user-metadata flow (or use a valid download token) for a gated bitstream as a logged-in user, then GET /content -> 200 with bytes. Guards against an over-blocking gate that 401s everything.

### GAP-3 — C1: FE preview viewer action (major)

Verified live: the file card renders but the Preview button navigates to /home instead of opening a preview (fileInfo tree empty + FE preview action routing). Own the FE viewer: port the dtq-dev preview action/dialog wiring in clarin-files-section/preview-section, and generate PreviewContent (file-preview CLI) for an archive fixture. AC: clicking Preview on a zip shows the file tree, on a text file shows content or a download fallback — never a silent redirect to /home.

### GAP-4 — Fresh-deploy Solr configset/core provisioning (major)

No work item owns CLARIN Solr schema/configset deltas (search *_ac autocomplete + search_text copyfields, oai core fields, statistics/authority cores) for a FRESH deploy. Diff dtq-dev solr/ configsets vs HEAD, port the needed deltas. AC: fresh docker compose up + reindex yields the autocomplete fields and a searchable OAI core.

### GAP-5 — OAI-PMH harvest statistics decision (medium)

Native org.dspace.matomo has no OAI tracker; dtq-dev ClarinMatomoOAITracker was dropped. DECIDE with maintainer: accept the loss (document in progress file) or re-port the OAI tracker onto the native pipeline. Until decided, list under documented-deferred with reason.

### Acceptance-criteria hardening rules (apply to the whole catalog)

- Dataset-pinned checks (specific UUIDs/handles/counts from the dev-5 dump) MUST state the dataset assumption; where possible replace with invariant checks (e.g. totalElements > 0, 'the item's own uploaded file appears').
- Every negative gate (401/403) MUST be paired with a positive check on an open resource, so over-blocking cannot pass.
- 'Renders and functions' style criteria are forbidden — each step needs a named test, HTTP probe, or exact UI assertion.
- Static-files AC must enumerate: count of files served == count of files in src/static-files/ (not a single-page sample).
- ACs gated on deferred infra (assetstore, Matomo stack, external IdP) must say so explicitly and name the IT that covers them instead.

## Native DSpace 9 features — adoption recommendations

Revised after adversarial critique; every claim below is git-verified against BE HEAD d27f85b133, FE HEAD dddddf35ce, origin/dtq-dev, and vanilla tags dspace-9.0/7.6.3, plus live probes on :18080. Three buckets. (A) ALREADY-CHOSEN, MUST-FINISH — two DISTINCT Matomo subsystems that the draft wrongly conflated: (1) native org.dspace.matomo TRACKING (MatomoEventListener/MatomoUsageEventHandler, gated by dspace/config/modules/matomo.cfg matomo.enabled, live=false) which supersedes and correctly REPLACES the removed dtq-dev ClarinMatomoTracker/ClarinMatomoBitstreamTracker trio; and (2) CLARIN's OWN report engine MatomoHelper.java+MatomoPDFExporter.java, which are CLARIN-AUTHORED classes that merely co-inhabit the org.dspace.matomo package namespace (verified: both present on HEAD and dtq-dev, ABSENT from vanilla dspace-9.0 git ls-tree — there is NO native rewrite and NO class collision, refuting the draft). Subsystem 2 reads lr.statistics.api.url/.cached.url/.auth.token/.site_id + lr.tracker.bitstream.site_id (MatomoHelper lines 81-86) and does NOT read matomo.enabled — so flipping matomo.enabled=true fixes tracking only, never the PDF report. Two functional caveats the draft missed: native tracking has NO OAI-PMH harvest tracker (dtq-dev ClarinMatomoOAITracker's job, absent on HEAD) so adopting native = accepted loss of VLO/WebLicht harvest statistics (interacts with C5); and end-to-end tracking cannot be proven without a running Matomo server whose reuse sources (dtq-dev docker/matomo-w-db.yml, docker/matomo-settings.ts, FE src/config/matomo-config.ts) are all part of audit M11 (absent on HEAD). (B) LOW-RISK NATIVE WINS: Signposting (modules/signposting.cfg signposting.enabled=true, describedby crosswalk=DataCite) — do-not-regress FAIR machine navigation; native Accessibility SETTINGS page (/info/accessibility) — additive user prefs, but it is NOT the EN 301 549 legal accessibility STATEMENT the draft claimed 'for free' (no such static page exists in the fork's 82 static-files to reuse); native DOI/DataCite base CLARIN builds on; Bulk Access UI and Content Reports as admin tooling. (C) CONFLICT/LOW-FITNESS: native Request-a-Copy contends with CLARIN's unported license/download-token gate (C2) — reconcile after C2; the 'ORCID' item is NOT native-org.dspace.orcid adoption but a one-block regression fix (re-add the deleted CachingOrcidRestConnector bean); native External-metadata live-import (~30 bibliographic providers) and IIIF/COAR-Notify/QA-broker are OFF-by-default and low-fitness for a language-resource repo — skip. Orejime is essentially DONE: orejime-configuration.ts on HEAD is byte-identical to vanilla dspace-9.0 and already ships accessibility+matomo apps, and CLARIN's only Klaro delta was a Klaro-only CSS hook (stylePrefix) plus an accessibility app native orejime already has — so there is NOTHING CLARIN-specific to port (draft's port task was phantom; effort downgraded M->S). Every item honors REUSE-not-rewrite (config/wiring/theme-surface on existing code) and pushes only to BE #1339 / FE #1316. Cross-domain dependencies that gate acceptance are called out in openQuestions/risks: C4 (static cookies.html) blocks the Orejime learn-more link; C2 blocks the Request-a-Copy decision; M11 (docker) blocks Matomo e2e; M13 (config in gitignored local.cfg) dictates WHERE the keys must live.

| Feature | Recommendation | Fitness | Effort | Value for CLARIN | Interaction with CLARIN features |
|---|---|---|---|---|---|
| Native Matomo TRACKING (org.dspace.matomo usage-event pipeline) | **adopt** | high | M | Officially-chosen, maintained replacement for CLARIN's bespoke trackers; the only tracking path left on HEAD. Machine-checkable acceptance: (1) set keys in dspace/config/modules/matomo.cfg (NOT clarin-dspace.cfg — see interaction): matomo.enabled=tru | REPLACES dtq-dev app/statistics/clarin/ClarinMatomoTracker + ClarinMatomoBitstreamTracker (correctly ABSENT on HEAD — do NOT re-port). ACCEPTED FUNCTIONAL LOSS: dtq-dev's THIRD tracker ClarinMatomoOAITracker.java (absent on HEAD) tracked OAI-PMH harv |
| CLARIN Matomo report/PDF subscription (finish FE half; distinct from native tracking) | **adopt** | high | M | Lets users subscribe to periodic Matomo usage PDF reports per item — a shipped CLARIN feature. BE stack is FULLY ported (verified both refs). Machine-checkable acceptance: (1) set lr.statistics.api.url, lr.statistics.api.cached.url, lr.statistics.api | CRITICAL CORRECTION to draft: MatomoPDFExporter/MatomoHelper are NOT native v9 rewrites and do NOT collide with native classes (verified absent in vanilla dspace-9.0). They are CLARIN's own report engine; they read lr.statistics.api.* and do NOT read |
| Orejime cookie-consent (native, replaced Klaro) | **adopt** | high | S | Maintained native consent integrated with native Matomo; retires CLARIN's Klaro. Machine-checkable acceptance: (1) consent modal at first load lists the 'statistical' (Matomo) purpose; (2) toggling statistical OFF removes the dsMatomo cookie (assert  | CORRECTION to draft: the port task was PHANTOM. Verified orejime-configuration.ts on HEAD is BYTE-IDENTICAL to vanilla dspace-9.0 (git diff dspace-9.0..HEAD empty) and already ships authentication/preferences/acknowledgement/correlation-id/matomo/goo |
| Signposting (FAIR machine navigation / Link headers) | **adopt** | high | S | High FAIR value: gives VLO/WebLicht/machine clients typed links complementing the (still-broken, C5) CLARIN OAI layer, and partially offsets the lost OAI harvest tracking above. Machine-checkable acceptance: (1) modules/signposting.cfg signposting.en | Complements CLARIN OAI + DOI; describedby reuses the DataCite crosswalk CLARIN already depends on. No conflict. Only regression risk: a theme/proxy stripping the Link header — item (3) above guards it. |
| Native Accessibility SETTINGS page (user prefs) — NOT the legal statement | **adopt** | medium | S | Additive UX win, live at http://localhost:14000/info/accessibility. Machine-checkable acceptance: (1) /info/accessibility renders the settings form (200); (2) a changed setting persists across reload (assert dsAccessibilitySettings cookie value); (3) | CORRECTION to draft: this settings page does NOT satisfy the EN 301 549 / EU Web Accessibility Directive legal obligation, which requires a published accessibility STATEMENT (conformance declaration + feedback mechanism). Verified the fork has NO acc |
| CLARIN ORCID authority regression fix — re-add CachingOrcidRestConnector bean | **adopt** | high | S | Fixes the audit-flagged 'ORCID authority NPE if enabled'. Concrete one-block reuse: git diff origin/dtq-dev..HEAD -- dspace/config/spring/api/orcid-authority-services.xml shows the <bean name="CachingOrcidRestConnector" class="org.dspace.external.Cac | CORRECTION to draft (which mis-framed this under 'native ORCID adoption' and buried it in open questions): this is a targeted regression fix of a CLARIN feature, not a native-feature adoption. The FULL native org.dspace.orcid stack (72-class OAuth lo |
| Native DOI / DataCite identifier base (do-not-regress) | **adopt** | high | S | CLARIN builds on this base — do-not-regress, not a new adoption. Machine-checkable acceptance: (1) native DOIIdentifierProvider/VersionedDOIIdentifierProvider beans stay active in identifier-service.xml; (2) DOIOrganiser mint/register round-trips aga | No conflict as long as the active HandleIdentifierProvider bean remains the CLARIN one. Verified both ClarinVersionedDOIIdentifierProvider.java and ClarinVersionedHandleIdentifierProvider.java exist on HEAD, but only the native VersionedDOIIdentifier |
| Bulk Access conditions UI | **consider** | medium | S | Admin efficiency for embargoes/restrictions across CLARIN collections. Machine-checkable acceptance: (1) /access-control/bulk-access renders 200 for an admin; (2) applying an embargo to a selected item writes a resourcepolicy row (start_date set) and | Operates on resource policies — orthogonal to CLARIN's app-layer license-acceptance gate (C2), so additive/compatible. Low risk. Must be surfaced in the custom-ported CLARIN admin sidebar. |
| Content Reports (Filtered Collections / Filtered Items) | **consider** | medium | S | Metadata/bitstream quality curation, complements the (unregistered) CLARIN metadataqa curation task. Machine-checkable acceptance: (1) unauth GET http://localhost:18080/server/api/contentreport/filteredcollections returns 401 (live-verified = present | Distinct from the native health-page (present on both refs = server status, not a content report — no conflict). Additive admin tooling. |
| Native Request-a-Copy (RequestItem + altcha captcha) | **consider** | medium | M | Only relevant for genuinely restricted CLARIN items with no anonymous READ. Machine-checkable acceptance (AFTER C2 lands): on a restricted CLARIN item, exactly ONE gate fires — the CLARIN license-acceptance modal XOR the request-a-copy button, never  | CONFLICT/OVERLAP with CLARIN's license/download-token gate (audit C2: authorizationBitstreamUtils.authorizeBitstream in AuthorizeServiceImpl.authorizeAction, dtq-dev, ABSENT on HEAD). Native request-a-copy surfaces on exactly the no-READ bitstream st |
| Native ROR organization live-import | **consider** | medium | M | Populate CLARIN affiliation/organization fields with authoritative ROR IDs at submission. Machine-checkable acceptance (only if adopted): (1) GET http://localhost:18080/server/api/integration/externalsources/ror/entries?query=test returns 200 (live-v | No CLARIN-specific ROR class in the fork (native only) — no conflict. Only relevant if CLARIN wants ROR-backed org authority; otherwise leave available-but-unwired. Wiring must land in submission-forms.xml (EN) which is still vanilla per a medium aud |
| Native External-metadata live-import providers (submission 'Import from external source') | **skip** | low | M | Low fitness: these are publication/bibliographic authorities; CLARIN deposits are datasets/tools/corpora, not journal articles. If a specific provider is ever wanted, it is already wired natively (additive, no conflict). Machine-checkable acceptance  | Additive; no conflict with any CLARIN feature. Recommend leaving available-but-unwired — do not spend effort integrating publication importers into a language-resource submission flow. |
| Native IIIF/Mirador, COAR-Notify/LDN inbox, QA/OpenAIRE broker | **skip** | low | S | Low fitness for a language-resource/data repository: IIIF targets image collections; COAR-Notify/LDN and QA-broker target scholarly-publication interoperability CLARIN does not use. No fork code exists for any of them (nothing to port). Machine-check | No interaction; keep disabled. Skip. |

Risks: Native Matomo without M11 docker: with no running Matomo server (dtq-dev docker/matomo-w-db.yml, docker/matomo-settings.ts absent on HEAD) and only placeholder siteid/token in modules/matomo.cfg, flipping matomo.enabled=true makes tracking silently no-op; e2e 'download -> matomo.php' is UNVERIFIABLE and only BE unit tests prove wiring. · Silent OAI-harvest statistics loss: native org.dspace.matomo tracks only UsageEvents; dtq-dev ClarinMatomoOAITracker (OAI-PMH harvest tracking) is absent on HEAD and has no native equivalent, so VLO/WebLicht/repository-harvest counts disappear once native tracking is the only path (interacts with C5). · Config-placement fragility (M13): if Matomo keys are put in clarin-dspace.cfg (which explicitly marks them 'no longer read') or the report keys rely on gitignored local.cfg, they are ignored and/or lost on fresh clone; keys must live in the git-tracked dspace/config/modules/matomo.cfg (tracking) and a tracked source for lr.statistics.api.* (report). · Two-subsystem confusion: treating native tracking and the CLARIN PDF report as one Matomo feature (as the draft did) risks 'fixing' one and declaring both done — they use different config keys and are functionally independent; the PDF report can be broken while tracking is green and vice versa. · Request-a-Copy double-gate: if native request-a-copy is left enabled after C2 lands without a reconciliation decision, restricted CLARIN items show both the license modal and the request-a-copy button on the same no-READ bitstream state — a confusing, possibly gate-bypassing UX. · Signposting Link-header strip: a theme or the FE reverse proxy dropping the rel=cite-as/describedby Link header silently regresses FAIR machine navigation with a green REST endpoint.

Open questions (maintainer): OAI harvest tracking (cross-domain, C5): accept the loss of VLO/WebLicht harvest statistics, or adapt dtq-dev app/statistics/clarin/ClarinMatomoOAITracker.java onto the native org.dspace.matomo client and re-hook it in the OAI layer? Decision belongs jointly to this domain and the OAI-port domain. · Docker Matomo (cross-domain, M11): who ports dtq-dev docker/matomo-w-db.yml + docker/matomo-settings.ts (+ FE src/config/matomo-config.ts) so Matomo tracking has a real server to test against? Matomo e2e acceptance cannot pass until M11 provides one; note push-restriction limits Docker changes to what fits BE #1339 / FE #1316. · CLARIN Matomo PDF report ownership (cross-domain): the BE stack is fully ported but the FE spec (matomo-subscription-button.component.spec.ts) is unported and live reachability at /items/{uuid}/statistics is unverified — confirm whether this lands under this domain or the CLARIN-feature-completion domain so it is not dropped between them. · Accessibility STATEMENT content (cross-domain, C4): the EN 301 549 legal statement has no reuse source in the fork's 82 static-files — who authors it, and does it get served via the C4 static-files mechanism (cookies.html-style)? This is content, not code. · Identifier providers (cross-domain, M2/M3): is leaving ClarinVersionedDOIIdentifierProvider.java unwired (only native VersionedDOIIdentifierProvider active) intended, and is ClarinVersionedHandleIdentifierProvider the ACTIVE uncommented HandleIdentifierProvider bean in identifier-service.xml? Confirm with the handle/identifier domain to avoid a versioning regression. · Request-a-Copy vs C2 sequencing (cross-domain): the disable-or-scope decision for native request-a-copy depends on C2 (license/download-token gate) landing first; the C2 owner and this domain must agree the final single-gate behavior and the request.item.type value.

## Work-item catalog (by domain; full reuse sources + v9 adaptations)

### BE — item file listing, file preview, license download gate, restricted-access (C1, C2, C3 + preview-content, checksum, allzip/ZIP chain)

This domain owns three audit criticals plus the preview-content, checksum, and allzip/ZIP chains. Live re-baseline (git status, live probes on :18080, production DB clarinv9-dspacedb) overturns the draft's core premise: C1 is NOT unstarted — the 6 metadata-bitstream REST-tier files ALREADY exist on disk with the exact v9 adaptation the draft proposed (MetadataBitstreamWrapperRest has PLURAL_NAME=NAME+"s"+getTypePlural(); the repository has jakarta imports + @Component(CATEGORY+"."+PLURAL_NAME); BitstreamByHandleRestController has jakarta + @RequestMapping(...BitstreamRest.PLURAL_NAME.../handle)). byHandle returns HTTP 200 with 2 populated rows RIGHT NOW. The fatal problem is that all 6 files are UNTRACKED (git status '??', HEAD d27f85b13); a checkout/clean loses them and PR #1339 would ship green-but-broken — exactly the audit's core complaint. So C1's remaining action is COMMIT the port + add the 3 missing ITs, not port from scratch. C2 and C3 remain genuine one-file deltas absent on disk (verified). New reuse strategy, sequenced: (1) BE-BASE-0 first — commit the untracked C1 tier to the branch feeding PR #1339 under a concurrency guard (the clarinv9-dspace container shows 'Up 12 minutes' = an active parallel process editing this exact area); (2) BE-GATE-1 (C2) and BE-REST-2 (C3) independent one-file changes; (3) BE-CHK-5 checksum tier port (entirely unported, needs the M3-style NAME→PLURAL_NAME bean fix); (4) BE-TEST-6 port the 3 absent ITs; (5) BE-ZIP-7 harden the allzip path so a gated item throws before headers commit; (6) BE-PREVIEW-8 verify the preview-content chain (already fully ported; live emptiness is env-gated by the missing assetstore, same deferral as O-assetstore). Every NEGATIVE gate check and the file-list-200 are locally verifiable because authorization short-circuits before file read; positive byte delivery is env-gated (no assetstore) and proven only by ITs with a test assetstore.

#### BE-BASE-0 — Commit the untracked C1 metadata-bitstream REST tier to the PR #1339 branch (concurrency-guarded re-baseline)  `[critical/S]`

- **Gap/impact:** The entire C1 fix exists only in the working tree as 6 UNTRACKED files (git status '??' at HEAD d27f85b13): dspace-server-webapp/.../rest/repository/MetadataBitstreamRestRepository.java, .../rest/BitstreamByHandleRestController.java, .../rest/model/MetadataBitstreamWrapperRest.java, .../rest/model/wrapper/MetadataBitstreamWrapper.java, .../rest/model/hateoas/MetadataBitstreamWrapperResource.java, .../rest/converter/MetadataBitstreamWrapperConverter.java. They are live on :18080 (byHandle=200) but in NO commit, so PR #1339 does not contain them and any git clean/checkout destroys the C1 work — a green PR with a broken feature. The clarinv9-dspace container is 'Up 12 minutes' (just rebuilt), indicating a parallel agent actively touching this area; committing without a reconcile risks clobber.
- **Reuse source:** Working-tree files listed above (already present, correct v9 form). Originals: origin/dtq-dev:dspace-server-webapp/src/main/java/org/dspace/app/rest/{repository/MetadataBitstreamRestRepository.java,BitstreamByHandleRestController.java,model/MetadataBitstreamWrapperRest.java,model/wrapper/MetadataBitstreamWrapper.java,model/hateoas/MetadataBitstreamWrapperResource.java,converter/MetadataBitstreamWrapperConverter.java}. No fresh port needed — diff the on-disk files vs origin/dtq-dev to confirm only jakarta + PLURAL_NAME deltas.
- **v9 adaptation:** None to the files (already jakarta-migrated + PLURAL_NAME-adapted on disk). Process adaptation only: before staging, `git status` + `git diff` the 6 paths to reconcile against any parallel edit; stage ONLY these 6 paths (do not `git add -A`); commit onto the existing ufal/clarin-dspace-upgrade-v9 branch (no new branch/PR — push target PR #1339).
- **Verification:** git ls-files | grep -c MetadataBitstreamWrapperRest.java (expect 1); git status --porcelain | grep -c MetadataBitstream (expect 0).

#### BE-GATE-1 — C2: wire the license/token download gate into AuthorizeServiceImpl (5-arg authorizeAction overload)  `[critical/S]`

- **Gap/impact:** The CLARIN license-agreement/token gate is not enforced on bitstream downloads. Verified live: bitstream faf919b4-c68f-48f4-8bbb-0ee61f4c0ddb (syn2005.gz) is mapped to a confirmation=1 license (production DB: license_resource_mapping join license_definition -> confirmation=1) yet anonymous GET /api/core/bitstreams/faf919b4.../content returns HTTP 500 (reached file read = authorization PASSED, failed only on missing assetstore), and byHandle reports isAllowed=y for it. The advisory /api/authrn/faf919b4... already correctly returns 401, proving every dependency (AuthorizationBitstreamUtils, MissingLicenseAgreementException, DownloadTokenExpiredException, verifyToken, isUserAllowedToAccessTheResource) is ported+jakarta-migrated at HEAD; only the enforcement call-site in AuthorizeServiceImpl is missing (grep confirms 'HOOK ABSENT ON DISK').
- **Reuse source:** git diff dspace-7.6.5 origin/dtq-dev -- dspace-api/src/main/java/org/dspace/authorize/AuthorizeServiceImpl.java (verified 2 hunks: (1) new field `@Autowired(required=true) AuthorizationBitstreamUtils authorizationBitstreamUtils;` after the searchService field ~line 85; (2) after the not-authorized throw block: `if (o.getType()==Constants.BITSTREAM && action!=Constants.WRITE && !isAdmin(c,o)) { authorizationBitstreamUtils.authorizeBitstream(c,(Bitstream)o); }`). Dependency org.dspace.authorize.AuthorizationBitstreamUtils EXISTS+tracked at HEAD.
- **v9 adaptation:** CORRECTION vs draft: the throw block + hook live in the 5-arg overload authorizeAction(Context c, EPerson e, DSpaceObject o, int action, boolean useInheritance) (HEAD line 129; next method authorizeActionBoolean at line 184), NOT the 4-arg overload (line 123, which merely delegates to the 5-arg). Insert the hook just before that overload's closing brace, after the throw block (~HEAD line 180). AuthorizationBitstreamUtils is in the same package org.dspace.authorize -> no import; confirm org.dspace.content.Bitstream + Constants imports exist (add if missing). No jakarta change. Verified path to 401: /content @PreAuthorize hasPermission(#uuid,'BITSTREAM','READ') -> authorizeActionBoolean(...) which catches AuthorizeException and returns false (MissingLicenseAgreementException extends AuthorizeException, confirmed) -> Spring Security denies -> 401 for anonymous.
- **Verification:** curl the two /content + two /authrn probes above; jq the byHandle href for isAllowed=n; then mvn -pl dspace-server-webapp test -Dtest=AuthorizationRestControllerIT.

#### BE-REST-2 — C3: add null guard in IdentifierRestRepository.getDSObyIdentifier (restricted item -> 401, not 500 NPE)  `[critical/S]`

- **Gap/impact:** getDSObyIdentifier (endpoint /api/pid/find?id=) calls `DSpaceObjectRest dsor = converter.toRest(dso, utils.obtainProjection())` (HEAD line 278) then immediately dereferences `dsor.getController()/getCategory()/getTypePlural()` (line 279) with no null check. converter.toRest returns null when the anonymous user cannot READ the resolved DSO -> NullPointerException -> HTTP 500 instead of 401/login. Guard is ABSENT at HEAD (grep confirms lines 278->279 adjacent, no null check).
- **Reuse source:** git diff dspace-7.6.5 origin/dtq-dev -- dspace-server-webapp/src/main/java/org/dspace/app/rest/repository/IdentifierRestRepository.java (verified single 4-line hunk: `if (dsor == null) { response.sendError(HttpServletResponse.SC_UNAUTHORIZED, "Authentication is required"); return; }` inserted immediately after the converter.toRest call, before the linkTo(...) line).
- **v9 adaptation:** Insert the guard between HEAD line 278 (toRest) and line 279 (linkTo). Do NOT copy the fork's surrounding link line — fork used English.plural(dsor.getType()); HEAD already correctly uses dsor.getTypePlural(). Only the 4-line guard applies.
- **Verification:** grep 'dsor == null' dspace-server-webapp/src/main/java/org/dspace/app/rest/repository/IdentifierRestRepository.java; mvn -pl dspace-server-webapp test -Dtest=IdentifierRestRepositoryIT (new private-item method must assert 401).

#### BE-FILES-3 — C1 verify: metadata-bitstream byHandle file-listing is committed, correct, and covered by an IT  `[critical/S]`

- **Gap/impact:** After BE-BASE-0 the byHandle endpoint exists, but its correctness must be locked by machine checks + an IT so a future refactor cannot silently re-break the Files box. FE (src/app/core/data/metadata-bitstream-data.service.ts, linkPath='metadatabitstreams', searchByHandleLinkPath='byHandle') and consumers clarin-files-section.component.ts / preview-section.component.ts depend on this shape.
- **Reuse source:** Committed in BE-BASE-0 (files already on disk). IT source: origin/dtq-dev:dspace-server-webapp/src/test/java/org/dspace/app/rest/MetadataBitstreamRestRepositoryIT.java (verified present in dtq-dev, absent at HEAD).
- **v9 adaptation:** None to the repository (verified on disk: @Component(CATEGORY+"."+PLURAL_NAME) -> bean 'core.metadatabitstreams' resolved by v9 Utils.getResourceRepositoryByCategoryAndModel; MetadataBitstreamWrapperRest.getTypePlural() returns 'metadatabitstreams'). IT ported under BE-TEST-6.
- **Depends on:** BE-BASE-0, BE-TEST-6
- **Verification:** Run the curl+jq assertions above against :18080; mvn -Dtest=MetadataBitstreamRestRepositoryIT; open the FE item page and count file rows.

#### BE-FILES-4 — C1 verify + gate-cover: BitstreamByHandleRestController (download-link target for every file row)  `[critical/S]`

- **Gap/impact:** composePreviewURL builds hrefs like /server/api/core/bitstreams/handle/11858/00-097C-0000-0023-119E-8/syn2005.gz served by BitstreamByHandleRestController (committed in BE-BASE-0). It calls authorizeService.authorizeAction(context,bitstream,Constants.READ) directly, so it MUST inherit the BE-GATE-1 hook: a gated file requested by handle must fail at authorization (before the retrieve/file-read stage) with 401, not leak or 500-after-headers. Live baseline: GET /handle/.../syn2005.gz currently returns 500 (route resolves, missing assetstore) — a '!=404' check is too weak to gate real behavior.
- **Reuse source:** Committed in BE-BASE-0 (on disk: @RequestMapping("/api/"+BitstreamRest.CATEGORY+"/"+BitstreamRest.PLURAL_NAME+"/handle"), value "/{prefix}/{suffix}/{filename:.+}", jakarta imports). IT source: origin/dtq-dev:dspace-server-webapp/src/test/java/org/dspace/app/rest/BitstreamByHandleRestControllerIT.java (verified present in dtq-dev, absent at HEAD).
- **v9 adaptation:** None (plain @RestController, no PLURAL bean-resolution issue; jakarta.servlet.* + jakarta.ws.rs.InternalServerErrorException already on disk).
- **Depends on:** BE-BASE-0, BE-GATE-1, BE-TEST-6
- **Verification:** curl the /handle/ path (assert non-404, then assert 401 after BE-GATE-1); mvn -Dtest=BitstreamByHandleRestControllerIT (positive path needs the IT's test assetstore since local prod assetstore is absent).

#### BE-CHK-5 — Port the CLARIN bitstream-checksum REST tier (/api/core/bitstreams/{uuid}/checksum) with the NAME->PLURAL_NAME bean fix  `[medium/M]`

- **Gap/impact:** FE (src/app/core/bitstream-checksum-data.service.ts linkPath='checksum', + shared/bitstream-checksum.model.ts, bitstream-checksum.resource.ts — all present) calls /api/core/bitstreams/{uuid}/checksum, which returns 404 live. The whole CLARIN checksum tier is UNPORTED: BitstreamRest at HEAD has no CHECKSUM @LinkRest (only BUNDLE/ACCESS_STATUS/FORMAT/THUMBNAIL), and none of the checksum classes are tracked or on disk.
- **Reuse source:** origin/dtq-dev: dspace-server-webapp/src/main/java/org/dspace/app/rest/repository/BitstreamCheckSumLinkRepository.java, dspace-server-webapp/src/main/java/org/dspace/app/rest/model/BitstreamChecksumRest.java, dspace-server-webapp/src/main/java/org/dspace/app/rest/model/BitstreamChecksum.java, dspace-server-webapp/src/main/java/org/dspace/app/rest/converter/BitstreamChecksumConverter.java, dspace-server-webapp/src/main/java/org/dspace/app/rest/model/hateoas/BitstreamChecksumResource.java; plus the BitstreamRest.java delta adding `public static final String CHECKSUM = "checksum";` and `@LinkRest(name = BitstreamRest.CHECKSUM, method = "getChecksum")`. (CheckSumRest.java is vanilla and already present.)
- **v9 adaptation:** CRITICAL M3-style bean gotcha (verified): fork's BitstreamCheckSumLinkRepository uses @Component(BitstreamRest.CATEGORY + "." + BitstreamRest.NAME + "." + BitstreamRest.CHECKSUM) -> 'core.bitstream.checksum', but v9 resolves link repos by PLURAL (existing HEAD BitstreamAccessStatusLinkRepository/BitstreamFormatLinkRepository use `BitstreamRest.CATEGORY + "." + BitstreamRest.PLURAL_NAME + "." + <link>`). Change NAME -> PLURAL_NAME so the bean is 'core.bitstreams.checksum', else the LinkRest stays 404. Also jakarta: javax.servlet.http.HttpServletRequest -> jakarta.servlet.http.HttpServletRequest and javax.annotation.Nullable -> jakarta.annotation.Nullable in BitstreamCheckSumLinkRepository. BitstreamChecksumRest extends RestAddressableModel -> add `public static final String PLURAL_NAME` + `@Override getTypePlural()` (abstract in v9) to compile. When editing BitstreamRest, MERGE the CHECKSUM @LinkRest into the existing HEAD @LinkRest set (keep ACCESS_STATUS) — do not overwrite with the fork's older 3-entry set.
- **Verification:** curl+jq the /checksum endpoint (200 + md5 match); grep the @Component/@LinkRest lines; git ls-files count of checksum classes.

#### BE-TEST-6 — Port the 3 missing CLARIN download/gate ITs and extend IdentifierRestRepositoryIT  `[major/M]`

- **Gap/impact:** BE-FILES-3, BE-FILES-4 and BE-GATE-1 all rely on ITs that DO NOT exist at HEAD. Verified: only IdentifierRestRepositoryIT.java is tracked; MetadataBitstreamRestRepositoryIT, BitstreamByHandleRestControllerIT and AuthorizationRestControllerIT are absent from HEAD and disk though all four exist in dtq-dev. Without them the criticals are 'verified' only by manual curl, and the local missing-assetstore blocks positive-byte verification.
- **Reuse source:** origin/dtq-dev:dspace-server-webapp/src/test/java/org/dspace/app/rest/{MetadataBitstreamRestRepositoryIT.java,BitstreamByHandleRestControllerIT.java,AuthorizationRestControllerIT.java} (all verified present). IdentifierRestRepositoryIT.java is already tracked at HEAD — extend it, do not re-port.
- **v9 adaptation:** jakarta migration of test imports (javax.servlet.* -> jakarta.servlet.*, javax.ws.rs.* -> jakarta.ws.rs.*); confirm AbstractControllerIntegrationTest base + builders (BitstreamBuilder, ClarinLicenseBuilder / license_resource_mapping seeding) exist at HEAD, port any missing builder/matcher used only by these ITs. The ITs create their own test assetstore, so they cover the positive byte + gated-401 paths that are env-blocked locally. Add to IdentifierRestRepositoryIT one method building a private item and asserting anonymous /api/pid/find?id=... -> 401 (C3 null branch).
- **Depends on:** BE-BASE-0
- **Verification:** mvn -pl dspace-server-webapp test -Dtest=MetadataBitstreamRestRepositoryIT,BitstreamByHandleRestControllerIT,AuthorizationRestControllerIT,IdentifierRestRepositoryIT (Surefire report: 0 failures/errors).

#### BE-ZIP-7 — Harden allzip bulk download so a gated item throws BEFORE response headers/bytes commit  `[major/M]`

- **Gap/impact:** MetadataBitstreamController.downloadFileZip (/api/core/items/{uuid}/allzip?handleId=..., route present, returns 400 without handleId) sets Content-Disposition + Content-Type='application/zip' and constructs ZipArchiveOutputStream BEFORE the bitstream loop, then calls bitstreamService.retrieve(context,bitstream) inside the loop — which now (with BE-GATE-1) throws MissingLicenseAgreementException mid-stream for a gated bitstream. Result for a gated/mixed item: HTTP 200 already committed with 'application/zip' + attachment header and possibly the first ungated entry flushed, then a mid-stream error -> a CORRUPT/partial zip instead of a clean 401/403. The gated bytes themselves are NOT leaked (retrieve throws), but the download is broken and the status is misleading. No work item/test covered this.
- **Reuse source:** N/A — new glue. Verified the fork's origin/dtq-dev MetadataBitstreamController.downloadFileZip is byte-identical to HEAD (same header-before-loop ordering, no pre-auth), so there is no fork source to port; this is a small correctness hardening on top of fork parity.
- **v9 adaptation:** Add a pre-pass in downloadFileZip that iterates all ORIGINAL-bundle bitstreams and calls authorizeService.authorizeAction(context, bitstream, Constants.READ) (inherits the BE-GATE-1 gate) BEFORE any response.setHeader/setContentType/new ZipArchiveOutputStream. Let the resulting AuthorizeException/MissingLicenseAgreementException propagate (method already declares throws AuthorizeException) so the standard handler emits a clean 401/403 with no zip headers/body written.
- **Depends on:** BE-GATE-1
- **Verification:** curl -D - the allzip URL for the gated item and assert status in {401,403} AND no application/zip Content-Type; mvn -pl dspace-server-webapp test -Dtest=MetadataBitstreamControllerIT.

#### BE-PREVIEW-8 — Verify the file-preview CONTENT chain (canPreview/getFilePreviewContent -> fileInfo) via IT; document live population as env-gated  `[medium/S]`

- **Gap/impact:** 'File preview' is in the domain title but only the file LIST is exercised; the preview CONTENT box renders empty. Live byHandle returns fileInfo:[] for both files and the production DB previewcontent table has only 1 row. Root cause verified as ENV/DATA, not code: the entire chain is ported+wired at HEAD (PreviewContentServiceImpl.composePreviewURL/canPreview/hasPreview/getFilePreviewContent, org.dspace.util.FileInfo, and the FilePreview + FilePreviewConfiguration CLI registered in dspace/config/spring/api/scripts.xml as bean 'file-preview'); previewcontent is populated by the FilePreview batch over the assetstore, which is absent locally, and create.file-preview.on-item-page-load=false matches the fork exactly (fork clarin-dspace.cfg also =false) so on-load emptiness is expected, not a regression.
- **Reuse source:** Chain already at HEAD: dspace-api/src/main/java/org/dspace/content/PreviewContentServiceImpl.java, dspace-api/src/main/java/org/dspace/scripts/filepreview/{FilePreview.java,FilePreviewConfiguration.java}, dspace/config/spring/api/scripts.xml (bean file-preview). IT/test-data source: reuse origin/dtq-dev preview coverage inside MetadataBitstreamRestRepositoryIT (fileInfo assertions) if present; else add a focused IT.
- **v9 adaptation:** None to the service chain. Two concrete gaps to close: (1) dspace/config/launcher.xml does NOT register file-preview (verified: only scripts.xml does) — port the fork's launcher.xml <command name="file-preview"> entry so operators can run the batch via the CLI (cross-check origin/dtq-dev:dspace/config/launcher.xml). (2) Add/confirm an IT that builds an item with a small text (and zip) bitstream in a test assetstore, generates preview content (invoke FilePreview or the service), and asserts the byHandle element.fileInfo array is non-empty with the extracted content.
- **Depends on:** BE-BASE-0
- **Verification:** grep 'file-preview' dspace/config/launcher.xml; mvn -pl dspace-server-webapp test -Dtest=MetadataBitstreamRestRepositoryIT (fileInfo-populated method). Live fileInfo remains empty (expected, env-gated).

**Domain risks:** Concurrency/clobber: clarinv9-dspace container is 'Up 12 minutes' and the 6 C1 files are untracked — a parallel agent is editing this exact area. BE-BASE-0 must `git diff` the 6 paths and stage ONLY them (never `git add -A`) to avoid committing another process's half-work or clobbering it. Re-check `git status` immediately before commit. · Env-gated verification ceiling: the production assetstore is not imported, so positive byte delivery (200 + content) for /content, /handle/, allzip, and populated fileInfo cannot be proven on :18080 — only via ITs that stand up their own test assetstore. Every LIVE acceptance check in this section is deliberately a NEGATIVE gate (401/403) or a metadata-only 200 that short-circuits before file read. · allzip pre-auth hardening (BE-ZIP-7) has no fork source (fork is byte-identical and equally buggy). It is a minimal correctness change, but if an implementing agent over-engineers it (e.g., buffering the whole zip) it could regress large-file streaming — keep it to a pre-loop authorizeAction pass only. · BE-CHK-5 NAME->PLURAL_NAME is the same class of bean-resolution bug that left M2/M3 broken; if an implementer ports the fork's @Component verbatim the endpoint stays 404 despite all classes compiling — the grep AC guards against this. · Only one live gated fixture (faf919b4, confirmation=1) and one open fixture (f15230ca, confirmation=0) are pinned; if the dev-5 dataset is refreshed these UUIDs may change — ITs (self-seeded) are the durable proof, curl probes are the fast smoke test.

**Open questions:** launcher.xml CLI registrations are broader than this domain: file-preview is missing from launcher.xml (I include porting it in BE-PREVIEW-8), but the audit also flags clarin-token and item-version-linker unregistered in launcher.xml — confirm the CLI/curation domain owns those so file-preview isn't double-owned or dropped. · clarin-dspace.cfg loading (M13): dspace/config/clarin-dspace.cfg is tracked and contains create.file-preview.on-item-page-load, but the audit says CLARIN props only load via an untracked gitignored local.cfg (config-definition.xml vanilla at HEAD). If clarin-dspace.cfg is not actually included on a fresh deploy, the preview flag and gate-related props silently vanish — the config/bootstrap domain must fix the include; my preview/gate items assume the cfg is loaded (it is, on the live stack). · FE contract for BE-CHK-5: I assert databaseChecksum.value in the /checksum response, but the FE bitstream-checksum.model.ts field names (activeStore/synchronizedStore/databaseChecksum) should be cross-checked by the FE domain so the ported BitstreamChecksumRest shape matches exactly and the checksum UI binds. · Whether the item-page 'file preview' UX must render actual extracted text on the live stack for sign-off, or whether IT-proven chain + documented env-gated population is acceptable — needs a maintainer call, since local content rendering depends on the deferred assetstore import. · allzip route shape: the endpoint is /api/core/items/{uuid}/allzip?handleId=... (uuid path var is currently ignored in favor of handleId). The FE ref-box/allzip caller should be confirmed by the FE domain to pass handleId; otherwise BE-ZIP-7's live AC URL needs the FE's actual invocation.

### BE — CLARIN OAI/CMDI/BibTeX crosswalk layer + ref-box export + CMDI upload (C5, S5, I6, O7)

The entire CLARIN OAI export stack is unported at HEAD and the failure is proven live: GET /server/oai/request?verb=ListMetadataFormats returns only the vanilla v9 prefixes (no cmdi/olac/oai_metasharev2/bibtex/elg/oai_datacite) and GET /api/core/refbox/citations?type=bibtex returns "Unknown metadata format". This is almost entirely a REUSE job. Verified against origin/dtq-dev: the 7 named CLARIN crosswalks (lindat_cmdi/bibtex/elg/olac/metasharev2/datacite_openaire/olac-dcmiterms) are already at HEAD byte-identical to dtq-dev — inert only because (a) 28 supporting Java classes are missing (SharedSaxonProcessor + 21 Saxon s9api extension functions under services/impl/resources/functions/ + BibtexUtil/LicenseUtil/LangUtil/XslLogUtil/SpecialItemService + ColComFilter) and (b) xoai.xml/oai.cfg never got the CLARIN Formats/Filters/Contexts wiring. The prior draft's blanket "all crosswalks byte-identical" claim was WRONG for oai_dc.xsl, which carries a functional CLARIN delta (downloadable_files_count dc:format, dc.date restricted to issued, broadened bitstream selection) and now has its own item (BE-OAI-9). Two more previously-omitted deltas are added: DSpaceOAIDataProvider.java (error wrapper + Matomo OAI tracking, BE-OAI-10) and description.xml sampleIdentifier (folded into BE-OAI-4). The ref-box REST front-half (ClarinRefBoxController + refbox DTOs) is ALREADY present and jakarta-migrated at HEAD; it only needs the OAI formats wired underneath (BE-OAI-5). CMDI upload (S5) is NOT a submission-form step (dtq-dev item-submission.xml has no cmdi step) — it is the standalone CMDIRestController mapped at /cmdi plus the CMDIFileBundleMaintainer validator invoked from MetadataValidation.java. Correct sequencing (verified DAG, no cycle): port the Java layer + DSpaceResourceResolver static-block first (BE-OAI-1, blocked on BE-SUB-DCINPUT-COMPLEXDEFS because SpecialItemService imports DCInput.ComplexDefinitions which is MISSING at HEAD), apply the modified-vanilla ItemUtils/XOAI deltas (BE-OAI-2/3), populate the currently-EMPTY OAI Solr core via oai import (BE-OAI-8), THEN wire xoai.xml+oai.cfg (BE-OAI-4) so ListMetadataFormats/GetRecord/refbox verify against real records. v9 risk is low on the Java (Saxon-HE 9.9.1-8 resolved; s9api ExtensionFunction API present; ColComFilter matches HEAD DSpaceFilter API) but MEDIUM on three modified-vanilla files (ItemUtils 99 lines, XOAI 72 lines, MetadataValidation large) that will NOT patch cleanly and must be hand-reconciled, and on DSpaceOAIDataProvider/CMDIRestController which in dtq-dev are pre-v9 (javax.servlet) and must be jakarta-adapted rather than ported verbatim.

#### BE-OAI-1 — Port the 28 CLARIN dspace-oai Java classes (SharedSaxonProcessor + 21 Saxon fns + 5 utils + ColComFilter) and wire them in DSpaceResourceResolver  `[critical/M]`

- **Gap/impact:** Every CLARIN OAI crosswalk (cmdi/olac/elg/metasharev2/bibtex/oai_datacite) invokes custom XSLT-2 extension functions in namespace http://custom.crosswalk.functions. Those function classes and their backing services are 100% absent at HEAD (verified: functions/ dir has 0 files at HEAD vs 21 in dtq-dev; SharedSaxonProcessor.java and ColComFilter.java MISSING), so even after the XSL is wired the transforms throw 'unknown function'. Foundational blocker for C5/I6/O7.
- **Reuse source:** Port verbatim from origin/dtq-dev: dspace-oai/src/main/java/org/dspace/xoai/services/impl/resources/SharedSaxonProcessor.java; the 21 classes under dspace-oai/src/main/java/org/dspace/xoai/services/impl/resources/functions/ (base classes ListXslFunction, StringXSLFunction, NodeXslFunction, NodeListXslFunction + BibtexifyFn, FormatFn, GetAuthorFn, GetAvailableFn, GetContactFn, GetFundingFn, GetLangForCodeFn, GetPropertyFn, GetSizeFn, GetUploadedMetadataFn, LogMissingFn, LogMissingMsgFn, ShortestIdFn, StringReplaceFn, UriToLicenseFn, UriToMetaShareFn, UriToRestrictionsFn); the 5 utils dspace-oai/src/main/java/org/dspace/utils/{BibtexUtil,LicenseUtil,LangUtil,XslLogUtil,SpecialItemService}.java; and dspace-oai/src/main/java/org/dspace/xoai/filter/ColComFilter.java. Apply the DSpaceResourceResolver.java fork delta (static{} block that calls SharedSaxonProcessor.initialize(transformerFactory) and registers the ExtensionFunction list; getTemplates() using SharedSaxonProcessor.getTransformerFactory()).
- **v9 adaptation:** Minimal on the Java: Saxon-HE 9.9.1-8 is the version resolved into dspace/modules/server/target (verified jar present), so net.sf.saxon.s9api.ExtensionFunction / SaxonTransformerFactory.getProcessor() / Processor.registerExtensionFunction(ExtensionFunction) exist unchanged. ColComFilter compiles unchanged: it overrides buildSolrQuery():SolrFilterResult + isShown(DSpaceItem), which match the HEAD DSpaceFilter abstract API exactly. These are not servlet/JPA classes so jakarta migration is N/A (only javax.xml.parsers remains, still valid in v9). HARD DEPENDENCY: SpecialItemService.java imports org.dspace.app.util.DCInput.ComplexDefinitions (verified: split(DCInput.ComplexDefinitions.getSeparator()) at 3 call sites) which is MISSING at HEAD — it will NOT compile until BE-SUB-DCINPUT-COMPLEXDEFS lands. SpecialItemService.getAvailable resolves against ClarinItemService.findByHandle (present at HEAD).
- **Depends on:** BE-SUB-DCINPUT-COMPLEXDEFS
- **Verification:** cd /c/workspace/clarin-dspace-v9-upgrade/DSpace && git ls-tree -r --name-only HEAD -- dspace-oai/src/main/java/org/dspace/xoai/services/impl/resources/functions/ | wc -l && mvn -q -pl dspace-oai -am -DskipTests compile && echo BUILD_OK

#### BE-OAI-2 — Apply the ItemUtils.java CLARIN delta (lindat bitstream URLs, restrictedAccess/owningCollection/itemId fields, excluded-bundle filtering)  `[major/M]`

- **Gap/impact:** CLARIN XSL crosswalks read XOAI internal fields that vanilla ItemUtils never emits: bitstream 'id', per-item 'restrictedAccess', 'owningCollection', 'itemId', and the lindat-style bitstream download URL /bitstream/{handle}/{sid}/{encodedName}. Without this delta the cmdi/olac ResourceProxy URLs and the availability/restriction logic are empty or wrong even once formats are wired.
- **Reuse source:** origin/dtq-dev:dspace-oai/src/main/java/org/dspace/xoai/util/ItemUtils.java — fork delta: DEFAULT_EXCLUDED_BUNDLES + getExcludedBundleNames() reading oai.bundle.excluded; createBundlesElement(context,item,AtomicBoolean restricted) signature; the /bitstream/{handle}/{sid}/{URLUtils.encode(name)} URL branch; createValue("id",...); the ClarinLicenseResourceMapping restricted-detection loop; and in the 'other' element createValue("restrictedAccess"|"owningCollection"|"itemId").
- **v9 adaptation:** Modified-vanilla file with ~99 lines of v9 drift from the 7.6.5 baseline — the fork patch will NOT apply cleanly; hand-reconcile onto the HEAD file. Dependencies satisfied at HEAD: ClarinLicenseResourceMappingService (via ClarinServiceFactory.getClarinLicenseResourceMappingService) and LicenseService present. Confirm bit.getFormat(context).getExtensions() still returns List<String> in v9 BitstreamFormat (it does). Keep the AtomicBoolean restricted plumbing threaded from getBundles up into createMetadataElement. Reads oai.bundle.excluded (delivered by BE-OAI-4's oai.cfg wiring, default TEXT,THUMBNAIL,SWORD).
- **Depends on:** BE-OAI-1, BE-OAI-8
- **Verification:** H=11234/1-3039; ID=$(curl -s 'http://localhost:18080/server/oai/request?verb=ListIdentifiers&metadataPrefix=oai_dc' | grep -oE '<identifier>[^<]*'$H'</identifier>' | head -1 | sed 's/[<>]/ /g' | awk '{print $2}'); curl -s "http://localhost:18080/server/oai/request?verb=GetRecord&metadataPrefix=xoai&identifier=$ID" | grep -oE 'name="(restrictedAccess|owningCollection|itemId)"|/bitstream/[^/\"]+/[0-9]+/'

#### BE-OAI-3 — Apply the XOAI.java local.hidden harvest delta so hidden-but-public items remain OAI-harvestable  `[medium/S]`

- **Gap/impact:** Vanilla XOAI indexer sets item.deleted = (withdrawn || !isDiscoverable || ...). CLARIN marks items local.hidden=hidden which flips isDiscoverable to false, so those items get item.deleted=true and silently drop out of every OAI harvest (WebLicht/VLO). The fork re-includes them.
- **Reuse source:** origin/dtq-dev:dspace-oai/src/main/java/org/dspace/xoai/app/XOAI.java — fork delta around the doc.addField("item.deleted",...) block: compute boolean discoverable=item.isDiscoverable(); if(!discoverable && item.isHidden()) discoverable=true; boolean isDeleted = item.isWithdrawn() || !discoverable || (isEmbargoed && isPublic).
- **v9 adaptation:** ~72 lines of v9 drift on this file — hand-apply the ~8-line block onto the HEAD vanilla doc.addField("item.deleted", (item.isWithdrawn() || !item.isDiscoverable() || (isEmbargoed ? isPublic : false))). Dependency Item.isHidden() present at HEAD. Does not reference BE-OAI-1 classes so it compiles independently, but its runtime acceptance requires the OAI import (BE-OAI-8).
- **Depends on:** BE-OAI-1
- **Verification:** cd /c/workspace/clarin-dspace-v9-upgrade/DSpace && git show HEAD:dspace-oai/src/main/java/org/dspace/xoai/app/XOAI.java | grep -n 'isHidden' || echo NOT_YET_PORTED

#### BE-OAI-8 — Populate the (currently empty) OAI Solr core via dspace oai import so all downstream acceptance is provable  `[major/S]`

- **Gap/impact:** Verified live: the clarinv9 OAI Solr core has numFound=0, so there are no records to GetRecord/ListRecords/ListIdentifiers. Every metadata-format acceptance in BE-OAI-2/3/4/5/9 requires a populated core; without this step they are not verifiable at their own level.
- **Reuse source:** N/A — operational step (dspace CLI oai import), not a code port. Must run AFTER the new XOAI indexer + ItemUtils delta are deployed so emitted xoai fields are correct.
- **v9 adaptation:** Run inside the running BE container (clarinv9) without restarting/rebuilding it: dspace oai import -c (clean rebuild of the oai core). This exercises the ported XOAI.java (BE-OAI-3) and ItemUtils.java (BE-OAI-2). Note: import needs metadata + license mappings only, not bitstream file content, so the deferred assetstore/file-content 500 does not block it (restrictedAccess is computed from clarin_license_resource_mapping, not from reading the file).
- **Depends on:** BE-OAI-1, BE-OAI-2, BE-OAI-3
- **Verification:** curl -s 'http://localhost:18080/server/oai/request?verb=ListIdentifiers&metadataPrefix=oai_dc' | grep -c '<identifier>'

#### BE-OAI-4 — Wire the CLARIN OAI Formats/Filters/Contexts into xoai.xml + oai.cfg + description.xml + description-olac reference (THE C5 fix)  `[critical/M]`

- **Gap/impact:** HEAD xoai.xml <Formats> is the vanilla v9 set (didl/dim/etdms/junii2/marc/mets/mods/oaidc/oaiopenaire/ore/qdc/rdf/rioxx/uketd_dc/xoai) — NONE of cmdi/olac/oai_metasharev2/bibtex/elg/oai_datacite Format entries, no ExcludeItemsInComOrCol/elgFilter/defaultFilter filters, and the default Context does not reference the CLARIN formats. HEAD oai.cfg has only vanilla oai.description.file. Verified live: ListMetadataFormats returns 0 CLARIN prefixes; all CLARIN harvest/export dead.
- **Reuse source:** origin/dtq-dev:dspace/config/crosswalks/oai/xoai.xml — the <Format id="cmdi">(→metadataFormats/lindat_cmdi.xsl), id="olac"(→olac.xsl), id="oai_metasharev2"(→metasharev2.xsl), id="bibtex"(→bibtex.xsl), id="elg"(→elg.xsl), id="oai_datacite"(→datacite_openaire.xsl) blocks; the default <Context baseurl="request"> Format refs; the <Filter id="ExcludeItemsInComOrCol"|"elgFilter"|"defaultFilter"> blocks + their CustomCondition entries (excludeDhCom/excludeTeachingCom→org.dspace.xoai.filter.ColComFilter, hasFilesCondition/hasMediaTypeCondition/isToolCondition/isLanguageResourceCondition); the openaire_data Context with oai_datacite. PLUS origin/dtq-dev:dspace/config/modules/oai.cfg delta (oai.bundle.excluded=TEXT,THUMBNAIL,SWORD; oai.description.file.0=description.xml + oai.description.file.1=description-olac.xml; the LINDAT description.* keys used by description-olac.xml). PLUS origin/dtq-dev:dspace/config/crosswalks/oai/description.xml delta (<sampleIdentifier>${oai.sample.identifier}</sampleIdentifier>). description-olac.xml is already byte-identical at HEAD (verified) and just needs the oai.cfg reference.
- **v9 adaptation:** Merge the fork Format/Filter/Context nodes ADDITIVELY into the HEAD v9 xoai.xml (which added vanilla rioxx/openaire4) — do not remove vanilla nodes. Confirm CLARIN Filter Class refs resolve: DSpaceAtLeastOneMetadataFilter/DSpaceMetadataExistsFilter present at HEAD, ColComFilter delivered by BE-OAI-1. Reconcile oai.cfg BY HAND (do NOT copy dtq-dev's file wholesale: dtq-dev DELETES the oai.html block and this v9 branch keeps the v9 OAI HTML-interface feature — retain the vanilla oai.html block; add only the CLARIN keys). description.xml's ${oai.sample.identifier} is defined in dspace/config/clarin-dspace.cfg:328 (oai.sample.identifier = oai:${oai.identifier.prefix}:${handle.prefix}/1234) — so this item depends on M13 (clarin-dspace.cfg loaded on a fresh deploy) or the Identify verb will emit the literal placeholder. Localize LINDAT description-olac contact values (help.mail/description.institution) for the customer/lindat repo.
- **Depends on:** BE-OAI-1, BE-OAI-8
- **Verification:** curl -s 'http://localhost:18080/server/oai/request?verb=ListMetadataFormats' | grep -oE 'metadataPrefix>(cmdi|olac|oai_metasharev2|bibtex|elg|oai_datacite)<' | sort -u

#### BE-OAI-9 — Port the oai_dc.xsl CLARIN crosswalk delta (downloadable_files_count, dc.date.issued-only, broadened bitstream selection)  `[medium/S]`

- **Gap/impact:** NEW — omitted from the prior draft, which wrongly asserted all crosswalks were byte-identical. Verified HEAD oai_dc.xsl DIFFERS from dtq-dev (+/- ~29 lines of functional change). Without it even the vanilla oai_dc harvest output (the default OAI format most external harvesters request) diverges from LINDAT prod: no downloadable_files_count, dc:date leaks accessioned/available dates, and bitstream dc:format is only picked from ORIGINAL/name-matched bundles.
- **Reuse source:** origin/dtq-dev:dspace/config/crosswalks/oai/metadataFormats/oai_dc.xsl — the fork delta: emit <dc:format>concat('downloadable_files_count: ', count(.../bundle[name='ORIGINAL']/bitstreams/bitstream))</dc:format>; restrict dc:date to doc:element[@name='date']/doc:element[@name='issued']; broaden bitstream dc:format select to doc:metadata//doc:element[@name='bitstreams']/doc:element[@name='bitstream']/doc:field[@name='format']; drop the extra dc.title.*/dc.description.* nested loops.
- **v9 adaptation:** Pure XSLT config, no Java/API surface. The HEAD file has only trivial vanilla drift; reconcile the CLARIN delta onto it so the result is byte-identical to origin/dtq-dev:oai_dc.xsl. No Saxon extension functions used here, so it works with the stock transformer even before BE-OAI-1 (but its runtime acceptance still needs a populated core, BE-OAI-8).
- **Depends on:** BE-OAI-8
- **Verification:** cd /c/workspace/clarin-dspace-v9-upgrade/DSpace && git diff --stat HEAD origin/dtq-dev -- dspace/config/crosswalks/oai/metadataFormats/oai_dc.xsl

#### BE-OAI-5 — Make ref-box BIBTEX/CMDI citation export return real citations and port its regression IT  `[major/S]`

- **Gap/impact:** The item-page reference box export is dead: verified live, GET /api/core/refbox/citations?type=bibtex returns {"metadata":"...Unknown metadata format"}. ClarinRefBoxController + refbox DTOs are ALREADY present and jakarta-migrated at HEAD (verified), but the endpoint runs an internal OAI GetRecord with metadataPrefix=bibtex/cmdi which fails until the formats are wired (BE-OAI-4). The controller's IT is MISSING at HEAD.
- **Reuse source:** Controller + DTOs already at HEAD (no re-port): dspace-server-webapp/src/main/java/org/dspace/app/rest/ClarinRefBoxController.java + model/refbox/{RefBoxDTO,ExportFormatDTO,FeaturedServiceDTO,FeaturedServiceLinkDTO}.java. Port the ABSENT regression test origin/dtq-dev:dspace-server-webapp/src/test/java/org/dspace/app/rest/ClarinRefBoxControllerIT.java (verified MISSING at HEAD) which indexes a test item into the OAI core and asserts non-empty bibtex/cmdi citations.
- **v9 adaptation:** No controller change needed — it becomes functional the moment BE-OAI-4 wires the bibtex/cmdi Formats. For the ported IT: adapt jakarta imports if any, and adapt the OAI-index setup helper to the v9 XOAI import path. Keep the assertion that the returned metadata string does NOT contain 'Unknown metadata format' and DOES contain a BibTeX entry header (@) / CMDI root.
- **Depends on:** BE-OAI-4
- **Verification:** curl -s 'http://localhost:18080/server/api/core/refbox/citations?type=bibtex' -G --data-urlencode 'handle=11234/1-3039' | grep -c 'Unknown metadata format'

#### BE-OAI-6 — Port CMDIRestController (/cmdi endpoint) and jakarta-migrate it  `[medium/M]`

- **Gap/impact:** NEW/clarified — the S5 CMDI surface. CMDIRestController.java is MISSING at HEAD (verified) but present in dtq-dev, mapped at @RequestMapping("/cmdi") with sub-routes /oai-metadata and /. It serves the CMDI metadata used by the FE CMDI viewer/export. Without it the /cmdi endpoints 404.
- **Reuse source:** origin/dtq-dev:dspace-server-webapp/src/main/java/org/dspace/app/rest/CMDIRestController.java (class @RequestMapping("/cmdi"), methods getCmdiMetadata @RequestMapping("/oai-metadata") and indexAction @RequestMapping("/"); uses com.lyncode.xoai OAIDataProvider like DSpaceOAIDataProvider).
- **v9 adaptation:** NOT a verbatim port: dtq-dev is pre-v9 and imports javax.servlet (verified: import static javax.servlet.http.HttpServletResponse.SC_BAD_REQUEST; javax.servlet.ServletException/HttpServletRequest/HttpServletResponse). Rewrite those to jakarta.servlet.* to compile on v9 (mirror the jakarta imports already used by the sibling DSpaceOAIDataProvider at HEAD). Confirm the com.lyncode.xoai OAIDataProvider/XOAIManager API it calls still resolves in the v9 dspace-oai module (it does — dspace-oai still bundles lyncode xoai). Depends on the CLARIN formats being wired (BE-OAI-4) for the cmdi transform to succeed.
- **Depends on:** BE-OAI-1, BE-OAI-4
- **Verification:** cd /c/workspace/clarin-dspace-v9-upgrade/DSpace && git cat-file -e HEAD:dspace-server-webapp/src/main/java/org/dspace/app/rest/CMDIRestController.java && echo PRESENT || echo MISSING

#### BE-OAI-7 — Port the CMDI-upload validation hook: CMDIFileBundleMaintainer + the MetadataValidation.java local.hasCMDI delta  `[medium/M]`

- **Gap/impact:** NEW/clarified — the actual CMDI-upload wiring. dtq-dev item-submission.xml has NO cmdi step (verified: only vanilla 'upload'); the CMDI-upload surface is the CMDIFileBundleMaintainer validator invoked from MetadataValidation.java when local.hasCMDI is set. CMDIFileBundleMaintainer.java (+Test) is MISSING at HEAD; MetadataValidation.java at HEAD is byte-vanilla and lacks the LOCAL_METADATA_HAS_CMDI = 'local.hasCMDI' branch that calls the maintainer.
- **Reuse source:** origin/dtq-dev:dspace-server-webapp/src/main/java/org/dspace/app/rest/submit/step/validation/CMDIFileBundleMaintainer.java and its test dspace-server-webapp/src/test/java/org/dspace/app/rest/submit/step/validation/CMDIFileBundleMaintainerTest.java. Apply the origin/dtq-dev:dspace-server-webapp/src/main/java/org/dspace/app/rest/submit/step/validation/MetadataValidation.java delta that adds LOCAL_METADATA_HAS_CMDI='local.hasCMDI' and the CMDIFileBundleMaintainer invocation.
- **v9 adaptation:** CAUTION — MetadataValidation.java at HEAD is byte-vanilla and the dtq-dev delta bundles TWO concerns in one file: (1) the multi-field type-bind logic (submit.type-bind.field as getArrayProperty + KEY_VALUE_SEPARATOR split — this is the medium 'type-bind late-load' item, dtq-dev 96e594a5be, owned by the submission domain) and (2) the CMDI local.hasCMDI hook. Hand-reconcile onto the HEAD vanilla file and coordinate so the file is ported ONCE (see openQuestions). CMDIFileBundleMaintainer itself is a validator class; adapt any javax→jakarta imports and confirm it compiles against the v9 submit/validation API (AbstractValidation).
- **Depends on:** BE-OAI-6
- **Verification:** cd /c/workspace/clarin-dspace-v9-upgrade/DSpace && git cat-file -e HEAD:dspace-server-webapp/src/main/java/org/dspace/app/rest/submit/step/validation/CMDIFileBundleMaintainer.java && git show HEAD:dspace-server-webapp/src/main/java/org/dspace/app/rest/submit/step/validation/MetadataValidation.java | grep -c 'local.hasCMDI'

#### BE-OAI-10 — Apply the DSpaceOAIDataProvider.java CLARIN delta (unhandled-exception log wrapper + Matomo OAI tracking) v9-safely  `[medium/M]`

- **Gap/impact:** NEW — omitted from the prior draft. Verified HEAD DSpaceOAIDataProvider.java DIFFERS from dtq-dev. The CLARIN delta adds (a) a trailing catch(Exception e){log.error(...)} so a crosswalk/transform failure logs instead of surfacing an opaque 500, and (b) Matomo OAI usage tracking via org.dspace.app.statistics.clarin.ClarinMatomoOAITracker — which, together with its base ClarinMatomoTracker, is MISSING at HEAD. Without this, CLARIN loses OAI harvest analytics and gets no diagnostic logging on transform errors.
- **Reuse source:** Semantic delta from origin/dtq-dev:dspace-oai/src/main/java/org/dspace/xoai/controller/DSpaceOAIDataProvider.java: @Autowired ClarinMatomoOAITracker matomoOAITracker; at the top of contextAction 'if (BooleanUtils.isTrue(configurationService.getBooleanProperty("matomo.track.enabled"))) matomoOAITracker.trackOAIStatistics(request);'; and the trailing 'catch (Exception e) { log.error("Unexpected exception e: " + e.toString()); }' before finally. Port the tracker classes origin/dtq-dev:dspace-api/src/main/java/org/dspace/app/statistics/clarin/ClarinMatomoOAITracker.java (extends ClarinMatomoTracker) and its base ClarinMatomoTracker.java (both MISSING at HEAD).
- **v9 adaptation:** DO NOT port the dtq-dev DSpaceOAIDataProvider.java verbatim — it is pre-v9 and would (1) downgrade jakarta.servlet.* back to javax.servlet.* (compile break on v9) and (2) DELETE the v9 OAI HTML-interface feature (@PostConstruct setUpHTMLTransformerFactory + the shouldServeAsHTML transform block). Apply ONLY the two CLARIN semantic additions onto the HEAD v9 file, keeping all jakarta imports and the HTML-transformer code intact. ClarinMatomoOAITracker/ClarinMatomoTracker use javax.servlet HttpServletRequest in dtq-dev → migrate to jakarta.servlet.http.HttpServletRequest. Gate the Matomo call behind matomo.track.enabled so it is inert when Matomo is off (env-gated). NOTE possible overlap: ClarinMatomoTracker base + ClarinMatomoBitstreamTracker belong to the CLARIN statistics/Matomo footprint — see openQuestions to avoid double-porting.
- **Depends on:** BE-OAI-1
- **Verification:** cd /c/workspace/clarin-dspace-v9-upgrade/DSpace && git show HEAD:dspace-oai/src/main/java/org/dspace/xoai/controller/DSpaceOAIDataProvider.java | grep -Ec 'matomoOAITracker.trackOAIStatistics|catch \(Exception e\)'

**Domain risks:** Three modified-vanilla files (ItemUtils.java ~99 lines drift, XOAI.java ~72 lines, MetadataValidation.java large) will NOT apply as fork patches and must be hand-reconciled onto HEAD; a botched merge silently regresses OAI to vanilla output (wrong ResourceProxy URLs, dropped hidden items, missing CMDI hook) while builds stay green. · OAI acceptance is gated on an operational step (BE-OAI-8 dspace oai import) that requires BE-OAI-1/2/3 to be built and deployed into the live clarinv9 container FIRST; the OAI Solr core is currently empty (numFound=0), so no metadata-format criterion is verifiable until the import runs — sequence discipline is essential. · oai.identifier.prefix is commented out in both HEAD and dtq-dev oai.cfg, so the runtime OAI identifier namespace segment is derived from ${dspace.ui.url} hostname and is NOT necessarily 'localhost'; any GetRecord-by-identifier check that hardcodes oai:localhost: will fail spuriously — identifiers must be sampled from a live ListIdentifiers response after BE-OAI-8. · description.xml's <sampleIdentifier>${oai.sample.identifier}</sampleIdentifier> resolves only if clarin-dspace.cfg (which defines oai.sample.identifier at line 328) is actually loaded on the target deploy (M13); on a fresh clone without local.cfg the Identify verb emits the literal placeholder. · DSpaceOAIDataProvider.java and CMDIRestController.java in dtq-dev are pre-v9 (javax.servlet); porting either verbatim breaks the v9 build and, for DSpaceOAIDataProvider, deletes the v9 OAI HTML-interface feature — both require selective, hand-applied deltas rather than file replacement. · SharedSaxonProcessor performs a one-time static registration of ExtensionFunctions into the shared Saxon Processor; if class-loading/registration order differs under the v9 Spring Boot standalone server (vs the 7.6 webapp), functions could be unregistered on first request — must be smoke-tested at runtime, not only at compile. · ClarinMatomoTracker base is shared by ClarinMatomoBitstreamTracker (bitstream stats) as well as the OAI tracker; if a separate statistics/Matomo domain also ports it, the class could be double-added to BE PR #1339 causing a duplicate-file conflict.

**Open questions:** MetadataValidation.java (BE-OAI-7) bundles the CMDI local.hasCMDI hook AND the multi-field type-bind logic (dtq-dev 96e594a5be), the latter owned by the submission-workflow domain (medium item 'type-bind late-load re-eval'). Confirm which domain owns porting this single file so it is reconciled once, not twice, on BE PR #1339. · ClarinMatomoTracker (base) + ClarinMatomoBitstreamTracker + the statistics.clarin package likely fall under a CLARIN statistics/Matomo domain (audit lists Matomo report-subscription as env-gated/deferred). Confirm ownership: does BE-OAI-10 port ClarinMatomoTracker+ClarinMatomoOAITracker, or does a stats domain deliver the base and BE-OAI-10 only adds the OAI subclass + the controller wiring? · Is DataCite export (Format oai_datacite → datacite_openaire.xsl, the openaire_data Context) in scope for LINDAT prod, or documented-deferred? It is wired in dtq-dev xoai.xml but is not called out as a broken live feature in the audit — confirm before adding its Context to avoid shipping an unverified path. · Operational: should BE-OAI-8's dspace oai import run against the live dev-5 production data in the clarinv9 container, and is running the CLI inside the running container (no restart) acceptable under the 'never restart/rebuild containers' constraint? Needs maintainer confirmation of the exact invocation and whether a full -c clean rebuild of the oai core is permitted on prod data. · The com.lyncode.xoai OAIDataProvider/XOAIManager library that DSpaceOAIDataProvider and CMDIRestController call — confirm it is still bundled/resolved in the v9 dspace-oai module (it appears present, but the CMDI controller's transform path was never exercised on v9); if the lyncode API drifted, CMDIRestController may need more than an import swap. · Cross-domain: the FE ref-box and CMDI viewer expect /server/cmdi/* and /api/core/refbox/citations to work; confirm the FE domain has (or plans) the corresponding service calls so BE-OAI-5/6 are consumable end-to-end (out of my BE domain).

### BE — submission steps, complex fields, autocomplete, ACL, registries, licenses config

At HEAD (d27f85b) the CLARIN submission-forms pipeline is under-ported. The parsing/rendering Java is byte-vanilla-v9: DCInput.java (254-line delta vs origin/dtq-dev 22cfef58e6), DCInputsReader (99), DCInputSet (9), SubmissionFormConverter (23), SubmissionFormFieldRest (43), DescribeStep (308), MetadataValidation (159) — so complex-definition, autocomplete-custom, <acl>, default-value and the '=>' type-bind mapping are silently dropped. ACL.java + ACE.java are ALREADY at HEAD (0-line diff vs dtq) but orphaned until DCInput is ported, so ACL wiring is far lighter than a from-scratch port (no new ACL logic — just the existing isAllowedAction call-sites). Consequently the already-ported submission-forms_cs.xml (verified: 8 complex-definition-ref, 15 autocomplete-custom, form-complex-definitions, <acl> on local.hidden/local.hasCMDI) is inert, and submission-forms.xml (EN), submission-forms.dtd and item-submission.xml are byte-vanilla (0 CLARIN markers/steps). The autocomplete REST cluster is 5 files (MetadataValueRestRepository + MetadataValueWrapperConverter + MetadataValueWrapper + MetadataValueWrapperRest + hateoas/MetadataValueWrapperResource) all ABSENT — /api/core/metadatavalues 404s — and it is NOT independent: findByValue() runs a Discovery/SOLR query (SearchService over IndexableItem), so it validates only against a populated index (live dev-5 is indexed). CMDI upload (S5) is also unported: CMDIFileBundleMaintainer.java is absent yet MetadataValidation:188 calls it (BE-SUB-2 will not compile without it) and UploadStep/UploadValidation need a MERGE. Registries: edm.xml/metashare-schema.xml/datacite.xml are DONE (0-line diff vs dtq — DO NOT re-touch); only local-types.xml (1->20 dc-type), bitstream-formats.xml (88->97) and dublin-core-types.xml (restore dc.rights.label) plus default.license (vanilla placeholder; default_cs.license already ported) remain. CRITICAL v9 gotcha: MetadataValueWrapperRest must ADD PLURAL_NAME='metadatavalues' and override getTypePlural() because RestModel.getTypePlural() is now abstract in v9 (was a default English.plural() in 7.6.x) — a verbatim port fails to compile / mis-registers the endpoint, the same M2/M3 failure class. Reuse strategy: port fork Java/config where v9 is unchanged, but MERGE DescribeStep/MetadataValidation onto v9's reworked single-String type-bind (isAllowedFor(String)+populateAllowedFieldNames) and MERGE the big XMLs onto v9 vanilla additions (coarnotify, openaire lowercase form names, bitstream-metadata form, entity processes) — never overwrite. Sequencing: parsing pipeline + DTD first (BE-SUB-1/3), then validation/CMDI (BE-SUB-2/7), then the config that depends on them (BE-SUB-4/5); the autocomplete cluster (BE-SUB-6), registries (BE-REG-1), license (BE-LIC-1) and the MetadataExposure residual (BE-EXP-1) parallelize. All work lands on BE PR #1339 (no new branches).

#### BE-SUB-1 — Port submission-forms parsing pipeline (DCInput/DCInputsReader/DCInputSet/SubmissionFormConverter/SubmissionFormFieldRest) — the linchpin  `[critical/L]`

- **Gap/impact:** At HEAD the parsing/rendering layer is byte-vanilla-v9 (verified deltas vs dtq: DCInput 254 lines, DCInputsReader 99, DCInputSet 9, SubmissionFormConverter 23, SubmissionFormFieldRest 43). It cannot parse or expose complex-definition-ref, autocomplete-custom, <acl>, default-value, or the type-bind field attribute, so every CLARIN submission field (contact_person/funding/sizeInfo complex fields, author/publisher/subject autocomplete, admin-only local.hidden/local.hasCMDI ACL fields) is dropped from the form config — even the already-ported submission-forms_cs.xml renders nothing CLARIN-specific. ACL.java + ACE.java are present at HEAD (0-line diff vs dtq) but orphaned: no consumer references them until DCInput is ported.
- **Reuse source:** origin/dtq-dev: dspace-api/src/main/java/org/dspace/app/util/DCInput.java (ComplexDefinition + ComplexDefinitions inner classes; isAllowedFor(String) line 576, isAllowedFor(List<String>) line 591, isAllowedAction(Context,int) line 638, getComplexDefinition line 649, getComplexDefinitionJSONString line 675, getAutocompleteCustom line 741, getTypeBindField line 749, getDefaultValue), dspace-api/src/main/java/org/dspace/app/util/DCInputsReader.java (+99: COMPLEX_DEFINITION_REF/AUTOCOMPLETE_CUSTOM token parsing, processComplexDefinitions(), <form-complex-definitions> section handling), dspace-api/src/main/java/org/dspace/app/util/DCInputSet.java (+9: constructor threads ComplexDefinitions), dspace-server-webapp/src/main/java/org/dspace/app/rest/converter/SubmissionFormConverter.java (+23: isInputAuthorized(Context,DCInput) ACL skip via isAllowedAction(ACL.ACTION_READ/WRITE) at line 245-247, setTypeBindField/setComplexDefinition/setAutocompleteCustom at lines 166-168), dspace-server-webapp/src/main/java/org/dspace/app/rest/model/SubmissionFormFieldRest.java (+43: complexDefinition/autocompleteCustom/typeBindField fields + getters/setters). ACL.java + ACE.java already at HEAD identical to dtq — reuse in place, DO NOT re-port.
- **v9 adaptation:** DCInput imports jakarta.annotation.Nullable and org.apache.logging.log4j.LogManager.getLogger() (v9 HEAD already uses both). Reconcile the fork DCInput(field,listMap,complexDefinitions) constructor with v9 DCInput(field,listMap): DCInputSet must pass complexDefinitions through the new arg. SubmissionFormConverter.isInputAuthorized calls ContextUtil.obtainCurrentRequestContext() (unchanged in v9). No other jakarta migration needed in the util classes beyond Nullable.
- **Verification:** mvn -pl dspace-api test -Dtest=DCInputTest ; curl -H 'Authorization: Bearer <adminJWT>' -H 'Accept-Language: cs' http://localhost:18080/server/api/config/submissionforms/traditionalpageone | jq '.rows[].fields[] | {complexDefinition, autocompleteCustom}'

#### BE-SUB-2 — Merge CLARIN DescribeStep + MetadataValidation (+ CMDIFileBundleMaintainer) onto v9-reworked type-bind  `[major/L]`

- **Gap/impact:** DescribeStep.java (308-line delta vs dtq) and MetadataValidation.java (159) at HEAD are byte-vanilla-v9. Missing: KEY_VALUE_SEPARATOR ('=>') resolution of submit.type-bind.field (dspace.cfg:1764 sets 'dc.type,dc.language.iso=>edm.type' but no code reads the '=>' form, so type-bind only ever reads dc.type), complex-definition validation (isValidComplexDefinitionMetadata + checkAllRequiredInputFieldsAreFilledIn — a partial complex value must be rejected, currently accepted), mappedToIfNotDefault handling (getMappedToIfNotDefault/loadMappedToIfNotDefaultFromComplex for OpenAIRE EU project ids), and the CMDIFileBundleMaintainer.updateCMDIFileBundle call. CMDIFileBundleMaintainer.java is ABSENT at HEAD yet MetadataValidation.java:188 references it, so this class MUST be ported here or BE-SUB-2 will not compile.
- **Reuse source:** origin/dtq-dev: dspace-server-webapp/src/main/java/org/dspace/app/rest/submit/step/DescribeStep.java (KEY_VALUE_SEPARATOR='=>' at line 66, '=>' split at line 104-105, getMappedToIfNotDefault, loadMappedToIfNotDefaultFromComplex line 449, getOperationWithChangedMetadataField line 276), dspace-server-webapp/src/main/java/org/dspace/app/rest/submit/step/validation/MetadataValidation.java (isValidComplexDefinitionMetadata line 210, checkAllRequiredInputFieldsAreFilledIn line 238, '=>' typeBindField split line 84-86, addError(ERROR_VALIDATION_REQUIRED,...) for complex at line 180), dspace-server-webapp/src/main/java/org/dspace/app/rest/submit/step/validation/CMDIFileBundleMaintainer.java (ABSENT at HEAD; referenced by MetadataValidation:188 — port together as one compile unit).
- **v9 adaptation:** MERGE, not wholesale-apply. Vanilla v9 already reworked type-bind: MetadataValidation uses input.isAllowedFor(documentTypeValue:String) (lines 93/120/141) plus inputConfig.populateAllowedFieldNames(documentTypeValue), whereas the 7.6.x fork used isAllowedFor(List<String>) + documentTypeValueList. KEEP v9's single-String flow (DCInput from BE-SUB-1 provides BOTH overloads, so no new ACL logic is added — ACL enforcement is only the existing call-sites). Graft ONLY the CLARIN net-new onto v9's flow: the '=>' KEY_VALUE_SEPARATOR resolution (so 'dc.language.iso=>edm.type' binds type off edm.type), isValidComplexDefinitionMetadata/checkAllRequiredInputFieldsAreFilledIn, mappedToIfNotDefault, and the CMDIFileBundleMaintainer.updateCMDIFileBundle call. Convert javax.servlet->jakarta.servlet; keep v9 org.apache.logging.log4j.LogManager logger. Re-expose DescribeStep.KEY_VALUE_SEPARATOR as public static final since MetadataValidation imports it (import static ...DescribeStep.KEY_VALUE_SEPARATOR).
- **Depends on:** BE-SUB-1
- **Verification:** Drive submission via REST (login, POST /api/submission/workspaceitems?owningCollection=..., PATCH describe fields, GET and inspect .errors array); mvn -pl dspace-server-webapp test -Dtest=SubmissionFormsControllerIT,ClarinWorkspaceItemRestRepositoryIT

#### BE-SUB-3 — Port submission-forms.dtd CLARIN grammar  `[major/S]`

- **Gap/impact:** dspace/config/submission-forms.dtd at HEAD is byte-vanilla (0 CLARIN grammar tokens; 42-line delta vs dtq). Without it, submission-forms.xml (EN, to port) and the already-ported submission-forms_cs.xml use ungrammatical <acl>, <default-value>, complex/autocomplete input-types, <type-bind field=...> and the <form-complex-definitions> section.
- **Reuse source:** origin/dtq-dev: dspace/config/submission-forms.dtd (adds form-complex-definitions/definition/input elements, default-value element, <!ELEMENT acl>, <!ATTLIST type-bind field>, <!ATTLIST input-type complex-definition-ref> and autocomplete-custom).
- **v9 adaptation:** None — pure config grammar, v9-agnostic. HEAD dtd == vanilla so applying the dtq content is a clean, non-conflicting replace.
- **Verification:** grep -E 'form-complex-definitions|complex-definition-ref|autocomplete-custom|ELEMENT acl' dspace/config/submission-forms.dtd ; xmllint --noout --dtdvalid dspace/config/submission-forms.dtd dspace/config/submission-forms_cs.xml

#### BE-SUB-4 — Port submission-forms.xml (EN) CLARIN content to match already-ported _cs (incl. value-pairs closure)  `[critical/L]`

- **Gap/impact:** dspace/config/submission-forms.xml (EN) at HEAD has 0 CLARIN markers (byte-vanilla-v9) while submission-forms_cs.xml is fully ported (8 complex-definition-ref, 15 autocomplete-custom, form-complex-definitions). Missing in EN: complex fields (contact_person/funding/sizeInfo/clariahSizeInfo via complex-definition-ref), autocomplete fields (solr-author_ac/solr-publisher_ac/solr-subject_ac/solr-dctype_ac/solr-dataProvider_ac, json_static-iso_langs.json), admin-only ACL fields local.hidden/local.hasCMDI, type-bind field=edm.type, the <form-complex-definitions> block, forms specialFields/clariahSubmissions{One,Two,Three}/teachingSubmissions{One,Two,Three}, and CLARIN form-value-pairs (metashare_funding, metashare_sizeunit, hasCMDI_checkbox, edm_types, common_iso_languages, hidden_list and the other metashare_* blocks).
- **Reuse source:** origin/dtq-dev: dspace/config/submission-forms.xml (CLARIN forms traditionalpageone/two complex+autocomplete+acl fields, forms specialFields/clariahSubmissions{One,Two,Three}/teachingSubmissions{One,Two,Three}, the metashare_* form-value-pairs blocks, and the closing <form-complex-definitions> with definitions contact_person/funding/sizeInfo/clariahSizeInfo).
- **v9 adaptation:** MERGE onto v9 vanilla EN form: preserve v9 additions/renames (openaire* lowercase form names, coarnotify-related fields, bitstream-metadata form) while grafting the CLARIN forms/fields/value-pairs/definitions. Every complex-definition input and every list/autocomplete input MUST have its referenced value-pairs present in the same file or DCInputsReader NPEs at boot. Keep EN labels/hints (do NOT copy the Czech text from _cs).
- **Depends on:** BE-SUB-1, BE-SUB-3
- **Verification:** grep -c complex-definition-ref dspace/config/submission-forms.xml ; curl -s -o /dev/null -w '%{http_code}' -H 'Authorization: Bearer <adminJWT>' http://localhost:18080/server/api/config/submissionforms/specialFields

#### BE-SUB-5 — Wire CLARIN submission steps into item-submission.xml  `[critical/M]`

- **Gap/impact:** dspace/config/item-submission.xml at HEAD is byte-vanilla (0 CLARIN steps). Missing: step-definitions clarin-notice(ClarinNoticeStep)/note/clarin-license(ClarinLicenseResourceStep)/specialFields; the traditional submission-process no longer appends <step id="clarin-license"/> and <step id="specialFields"/> after license; and the submission-map name-maps (default->traditional plus collection-handle 20.500.12800/3->clariahSubmissions and 11234/5012->teachingSubmissions). So even though ClarinNoticeStep/ClarinLicenseResourceStep/ClarinLicenseSubmissionUtils exist at HEAD, they are never invoked — the CLARIN license step and special-fields step never run in any collection's submission.
- **Reuse source:** origin/dtq-dev: dspace/config/item-submission.xml (step-definitions clarin-notice line 101/note/clarin-license line 170 with processing-class org.dspace.app.rest.submit.step.ClarinLicenseResourceStep/specialFields; the traditional process at line 306 appends clarin-license line 334 + specialFields line 335 after license; clariahSubmissions/teachingSubmissions processes add clarin-notice+clarin-license+specialFields; submission-map name-map 20.500.12800/3->clariahSubmissions and 11234/5012->teachingSubmissions).
- **v9 adaptation:** MERGE onto v9 vanilla item-submission.xml — preserve v9's entity submission-processes (Publication/Person/Project/OrgUnit/Journal/JournalVolume/JournalIssue), the coarnotify step and bitstream-metadata handling; graft only the CLARIN step-definitions, the traditional-process step additions and the CLARIN name-maps. processing-class package org.dspace.app.rest.submit.step.* is unchanged in v9.
- **Depends on:** BE-SUB-4
- **Verification:** grep -c clarin-license dspace/config/item-submission.xml ; curl -H 'Authorization: Bearer <adminJWT>' http://localhost:18080/server/api/config/submissiondefinitions/traditional | jq '.sections // .steps'

#### BE-SUB-6 — Port autocomplete REST cluster (5 files) WITH the v9 PLURAL_NAME fix + IT/matcher  `[medium/M]`

- **Gap/impact:** The autocomplete REST cluster is 5 files, ALL ABSENT at HEAD (verified): repository/MetadataValueRestRepository.java, converter/MetadataValueWrapperConverter.java, model/MetadataValueWrapper.java, model/MetadataValueWrapperRest.java, model/hateoas/MetadataValueWrapperResource.java. The FE core/data/metadata-value-data.service.ts (linkName='metadatavalues', searchBy('byValue')) therefore hits GET /server/api/core/metadatavalues/search/byValue -> 404, so every autocomplete field (author/publisher/subject/dctype/dataProvider) returns zero suggestions. This is NOT an independent item: MetadataValueRestRepository.findByValue() calls createAndRunDiscoverResult() (SearchService over IndexableItem, filter search.resourcetype:Item) so it depends on a populated Discovery/SOLR index — provable on the live dev-5 stack (already indexed) but not on an empty index.
- **Reuse source:** origin/dtq-dev: dspace-server-webapp/src/main/java/org/dspace/app/rest/repository/MetadataValueRestRepository.java (@Component(CATEGORY+'.'+NAME)='core.metadatavalue', @SearchRestMethod(name='byValue') with @Parameter schema/element/qualifier/searchValue), converter/MetadataValueWrapperConverter.java, model/MetadataValueWrapper.java, model/MetadataValueWrapperRest.java, model/hateoas/MetadataValueWrapperResource.java; tests dspace-server-webapp/src/test/java/org/dspace/app/rest/MetadataValueRestRepositoryIT.java + dspace-server-webapp/src/test/java/org/dspace/app/rest/matcher/MetadataValueMatcher.java.
- **v9 adaptation:** DO NOT port MetadataValueWrapperRest verbatim. VERIFIED: in v9 RestModel.getTypePlural() is an ABSTRACT method (7.6.x had 'default public String getTypePlural(){ return English.plural(getType()); }'); the fork MetadataValueWrapperRest declares NAME='metadatavalue' and relies on that removed default, so a verbatim port either fails to compile (abstract method unimplemented) or mis-registers the endpoint. FIX: add 'public static final String PLURAL_NAME = "metadatavalues";' and override 'public String getTypePlural(){ return PLURAL_NAME; }' (mirror v9 BitstreamRest lines 25/86-87) so the endpoint registers at /api/core/metadatavalues matching the FE call. Convert any javax->jakarta; SearchService/DiscoverQuery API unchanged in v9. This is the same M2/M3 PLURAL_NAME gotcha.
- **Verification:** curl -s -o /dev/null -w '%{http_code}' 'http://localhost:18080/server/api/core/metadatavalues/search/byValue?schema=dc&element=contributor&qualifier=author&searchValue=a' ; mvn -pl dspace-server-webapp test -Dtest=MetadataValueRestRepositoryIT

#### BE-SUB-7 — Wire CMDI file upload into the METADATA bundle (UploadStep/UploadValidation merge)  `[medium/M]`

- **Gap/impact:** CMDI upload (S5) is unported. CMDIFileBundleMaintainer.java is ABSENT at HEAD (ported by BE-SUB-2 because MetadataValidation:188 references it). UploadStep.java (19-line delta vs dtq) is missing the CLARIN net-new: the UPLOAD_STEP_METADATA_SECTION='bitstream-metadata' constant and the line bundles.addAll(itemService.getBundles(obj.getItem(), Constants.METADATA_BUNDLE_NAME)) that surfaces the CMDI metadata-bundle bitstream in the upload section; UploadValidation.java (6-line delta) is missing the paired validation. Without these an uploaded .cmdi is neither moved to the METADATA bundle nor shown in the upload step, so WebLicht/CMDI export has no source bitstream.
- **Reuse source:** origin/dtq-dev: dspace-server-webapp/src/main/java/org/dspace/app/rest/submit/step/UploadStep.java, dspace-server-webapp/src/main/java/org/dspace/app/rest/submit/step/validation/UploadValidation.java, dspace-server-webapp/src/main/java/org/dspace/app/rest/submit/step/validation/CMDIFileBundleMaintainer.java (shared with BE-SUB-2).
- **v9 adaptation:** MERGE, not verbatim — the dtq UploadStep predates v9 (dtq still on javax.servlet and lacks v9's primary-bitstream handling via Objects.nonNull(primaryBitstream), PRIMARY_FLAG_PATTERN and StringUtils.isBlank). KEEP all v9 vanilla logic (jakarta.servlet, primary-bitstream flag, StringUtils). Graft ONLY the CLARIN net-new: the UPLOAD_STEP_METADATA_SECTION='bitstream-metadata' constant and the bundles.addAll(itemService.getBundles(obj.getItem(), Constants.METADATA_BUNDLE_NAME)) call so METADATA-bundle (CMDI) bitstreams appear in DataUpload. CMDIFileBundleMaintainer is a pure service util (no servlet imports).
- **Depends on:** BE-SUB-2
- **Verification:** Drive REST upload of a .cmdi; then psql -h localhost -p 15432 -c "SELECT count(*) FROM bundle bn JOIN item2bundle ib ON ib.bundle_id=bn.uuid WHERE bn.name='METADATA' AND ib.item_id='<uuid>'"

#### BE-REG-1 — Re-apply CLARIN metadata/bitstream registries (local-types, bitstream-formats, dublin-core-types)  `[major/S]`

- **Gap/impact:** Registries are under-ported: dspace/config/registries/local-types.xml has 1/20 dc-type (139-line delta vs dtq — ~19 local.* entries missing), bitstream-formats.xml has 88/97 <bitstream-type> (159-line delta — archive/CLARIN formats missing), dublin-core-types.xml lost dc.rights.label (17-line delta; HEAD has 0 label elements, dtq has 2). A fresh install/test DB therefore lacks CLARIN fields. NOTE: edm.xml, metashare-schema.xml and datacite.xml are ALREADY correct at HEAD (0-line diff vs dtq) — DO NOT modify them (avoids needless churn).
- **Reuse source:** origin/dtq-dev: dspace/config/registries/local-types.xml, dspace/config/registries/bitstream-formats.xml, dspace/config/registries/dublin-core-types.xml. (edm.xml/metashare-schema.xml/datacite.xml: NO action — already ported.)
- **v9 adaptation:** None — registry XML is v9-agnostic. Apply the dtq content; for dublin-core-types.xml MERGE (restore the dc.rights.label element) onto any v9-vanilla additions rather than blindly overwriting.
- **Verification:** git show HEAD:dspace/config/registries/local-types.xml | grep -c '<dc-type>' ; git show HEAD:dspace/config/registries/bitstream-formats.xml | grep -c '<bitstream-type>'

#### BE-LIC-1 — Restore CLARIN default.license (Deposit Licence Agreement) — fix EN/CS inconsistency (M9)  `[major/S]`

- **Gap/impact:** dspace/config/default.license at HEAD is the vanilla placeholder (first line 'NOTE: PLACE YOUR OWN LICENSE HERE'; 65-line delta vs dtq whose first non-blank line is 'Deposit Licence Agreement'). New deposits attach the wrong legal text. default_cs.license is already ported (0-line diff vs dtq) — so EN is wrong while CS is correct (the M9 inconsistency).
- **Reuse source:** origin/dtq-dev: dspace/config/default.license (Deposit Licence Agreement / Preamble text).
- **v9 adaptation:** None — plain text file. Replace HEAD default.license with the dtq content.
- **Verification:** git diff HEAD origin/dtq-dev -- dspace/config/default.license ; head -3 dspace/config/default.license

#### BE-EXP-1 — Finish MetadataExposureServiceImpl residual (submitter-sees-own-hidden hook)  `[low/S]`

- **Gap/impact:** dspace-api/src/main/java/org/dspace/app/util/MetadataExposureServiceImpl.java at HEAD differs from dtq by 13 lines: the fork makes CONFIG_PREFIX a 'protected final' instance field (HEAD still has 'public static final String CONFIG_PREFIX') to support the 'submitter can see own hidden metadata' override. HEAD already ported ~90/103 lines of the fork delta, so the feature is mostly present but this residual leaves the config-prefix override incomplete — low priority but it is in this domain's fork-delta and no other item covers it.
- **Reuse source:** origin/dtq-dev: dspace-api/src/main/java/org/dspace/app/util/MetadataExposureServiceImpl.java (CONFIG_PREFIX visibility change + the submitter-visibility branch).
- **v9 adaptation:** Change 'public static final String CONFIG_PREFIX = "metadata.hide.";' to 'protected final String CONFIG_PREFIX = "metadata.hide.";' and graft the submitter-visibility branch; first grep the tree to confirm no external class references the static MetadataExposureServiceImpl.CONFIG_PREFIX (compile-safety).
- **Verification:** git diff HEAD origin/dtq-dev -- dspace-api/src/main/java/org/dspace/app/util/MetadataExposureServiceImpl.java ; mvn -q -pl dspace-api -am install -DskipTests

**Domain risks:** Boot-time NPE risk: DCInputsReader throws if BE-SUB-4's EN form references a value-pairs block that was not merged in — this fails the whole submission subsystem, not just one field. Mitigated by BE-SUB-4 acceptance criterion 2 (value-pairs closure) and the 'no DCInputsReaderException' boot check. · Highest single-line risk: an implementing agent porting MetadataValueWrapperRest verbatim (as the original draft implied) hits a compile error / 404 because RestModel.getTypePlural() is now abstract in v9. BE-SUB-6 pins the PLURAL_NAME='metadatavalues' + getTypePlural() override explicitly. · Type-bind merge (BE-SUB-2) grafts the '=>' resolution onto v9's populateAllowedFieldNames flow; a sloppy merge can regress vanilla dc.type binding. Guarded by running vanilla WorkspaceItemRestRepositoryIT alongside the CLARIN ITs. · item-submission.xml name-map hardcodes CLARIN collection handles (20.500.12800/3, 11234/5012); if those handles do not exist in the dev-5 DB the clariahSubmissions/teachingSubmissions processes are silently unmapped (traditional still works). Verify the handles resolve before relying on the mapped forms. · BE-SUB-6 and BE-REG-1 are runtime/import-time correctness gaps: the autocomplete IT needs a populated Discovery/SOLR core in the CI harness, and registry changes only affect fresh imports — they will NOT retro-populate the already-loaded dev-5 DB, so do not expect the live DB rows to change from this work. · CMDIFileBundleMaintainer is a hard compile dependency of BE-SUB-2 (MetadataValidation:188) but its full runtime behaviour is validated in BE-SUB-7 — if BE-SUB-2 lands without BE-SUB-7's UploadStep merge, compilation succeeds but CMDI files are still not surfaced in the upload step.

**Open questions:** CMDI end-to-end (WebLicht harvest) additionally depends on the OAI cmdi crosswalk (audit C5, dspace-oai layer) and the item file-list/bitstream layer (C1) — both OUTSIDE this domain. BE-SUB-7 only guarantees the CMDI file lands in the METADATA bundle; it does not guarantee OAI exposes it. · submit.type-bind.field is set TWICE in dspace/config/dspace.cfg (line 1041 = 'dc.type', line 1764 = 'dc.type,dc.language.iso=>edm.type'). Confirm the CLARIN override (1764) is last-wins at runtime AND that config-definition.xml (audit M13) actually loads clarin-dspace.cfg on a fresh deploy — if it is only pulled via the gitignored local.cfg, the '=>' property is absent on a clean clone and BE-SUB-2's type-bind mapping silently no-ops. Cross-cutting with the BE config domain (M13). · Do the dev-5 collection handles for clariahSubmissions (20.500.12800/3) and teachingSubmissions (11234/5012) exist? If not, the BE-SUB-5 name-maps are inert and the correct handles are a maintainer decision. · ~120 CLARIN test classes are unported; this plan ports only the submission-relevant ones (DCInputTest, SubmissionFormsControllerIT, MetadataValueRestRepositoryIT, ClarinWorkspaceItemRestRepositoryIT, ClarinWorkflowItemRestRepositoryIT). The remaining IT/builder/matcher backlog is a separate test-domain effort. · The FE-side renderers for these BE features (complex-field renderer, autocomplete renderer, ISO-language facet FilterType.isoLanguage — audit M4) are OUTSIDE this BE domain but are required for the complex/autocomplete UX to actually render; flag to the FE domain that the BE surfaces (complexDefinition JSON, /api/core/metadatavalues, autocompleteCustom) are only useful once the FE renderers are ported.

### BE — auth, user registration, PAT (clarin_token), config wiring, and CLARIN vanilla-file deltas

This domain is overwhelmingly WIRING/CONFIG re-application plus systematic reconciliation of dropped vanilla-file hunks, not feature rewriting: every CLARIN service/DAO/model/CLI class the items depend on already exists in HEAD (verified: ClarinTokenAdministrator + ClarinTokenCreator, ClarinTokenServiceImpl reading clarin.token.encryption.secret, ClarinToken constants, ClarinUserRegistrationRestRepository/Service, ClarinServiceFactory.getClarinUserRegistration, EPerson entity welcomeInfo/canEditSubmissionMetadata getters+columns, CachingOrcidRestConnector.java, ClarinShibbolethLoginFilter.VERIFICATION_TOKEN_HEADER, ClarinItemService.updateItemDatesMetadata), and DB migrations already ran (clarin_token table exists with 0 rows; user_registration holds 3238 imported rows). What was dropped in the v9 fork-port are the CLARIN deltas inside vanilla files that v9 rewrote or renamed. Measured backlog: dtq-dev modifies 222 baseline .java files (40 still byte-identical to dspace-7.6.5 in HEAD = delta fully dropped, plus partial ports like InstallItemServiceImpl) and 61 baseline config files (33 still byte-vanilla). Reuse strategy: for each file, extract the CLARIN hunk from `git diff dspace-7.6.5 origin/dtq-dev -- <file>` and re-apply ONLY that hunk on top of v9's structure, never reverting v9 improvements (jakarta, DSpaceCsrfAuthenticationStrategy, cookie .path hardening, Instant, PLURAL_NAME @Component beans, checkRequiredProperties(registrationData,epersonRest), the WebApplication rename). Sequencing: land config-definition include (M13) first, then the token secret, then launcher clarin-token, then JWT auth branch, then the user_registration hooks, the vanilla-file deltas, and finally a systematic reconciliation sweep that catches every residual CLARIN marker across the 222+61 files. All commits go to BE PR #1339 only.

#### BE-CFG-1 — config-definition.xml: load tracked clarin-dspace.cfg (M13)  `[major/S]`

- **Gap/impact:** VERIFIED: HEAD dspace/config/config-definition.xml is missing the clarin-dspace.cfg <properties> block (git diff HEAD origin/dtq-dev shows the 5-line block inserted after local.cfg, before dspace.cfg). Although clarin-dspace.cfg IS tracked in HEAD, it is only picked up on the live stack because an untracked/gitignored local.cfg includes it. On a fresh clone every lr.*/clarin.* property (PID prefixes, clarin.token.*, license config) silently vanishes, breaking handles, licenses, and PAT at runtime.
- **Reuse source:** origin/dtq-dev:dspace/config/config-definition.xml lines 45-49 — the block `<!-- Separate our changes from original dspace source --> <!-- Overrides everything in modules and dspace.cfg, but not local.cfg --> <properties fileName="clarin-dspace.cfg" throwExceptionOnMissing="true" config-name="clarin" encoding="UTF-8" config-reload="true" reloadingRefreshDelay="5000"/>` inserted between the local.cfg <properties> entry and the dspace.cfg <properties> entry.
- **v9 adaptation:** None — Apache Commons Configuration builder is unchanged in v9. Insert verbatim in the same position so clarin overrides modules/dspace.cfg but not local.cfg.
- **Verification:** docker exec clarinv9-dspace /dspace/bin/dspace dsprop -p lr.pid.community.configurations  # non-empty; and: git show HEAD:dspace/config/config-definition.xml | grep -c clarin-dspace.cfg  # must be 1

#### BE-CFG-2 — clarin.token.encryption.secret + max-expiration configured (M7 runtime prerequisite)  `[major/S]`

- **Gap/impact:** VERIFIED: ClarinTokenServiceImpl (HEAD line 71) reads configurationService.getProperty(ClarinToken.PROPERTY_ENCRYPTION_SECRET='clarin.token.encryption.secret') and THROWS RuntimeException('Missing clarin.token.encryption.secret configuration key') at line 74 when absent; line 114/195 builds an AES SecretKey via ClarinTokenUtils.getSecretKeyFromBase64EncodedString. No clarin.token.* value is committed in any tracked .cfg in either branch, so on the live clarinv9 stack token create and token auth fail. clarin.token.max.expiration.time.in.days (ClarinToken.PROPERTY_MAX_EXPIRATION_TIME_IN_DAYS, HEAD line 45) is likewise unset.
- **Reuse source:** N/A — new glue (deployment secret VALUE). Key generation is NOT ad hoc: `dspace clarin-token -g/--generateEncryptionKey` (ClarinTokenAdministrator option, HEAD line 67) emits exactly the base64 AES key this property expects. Property consumers already in HEAD: dspace-api/src/main/java/org/dspace/content/clarin/ClarinTokenServiceImpl.java, ClarinToken.java.
- **v9 adaptation:** None. Do NOT commit a real secret to tracked clarin-dspace.cfg (would be a shared key across deployments). Add a commented placeholder + doc line in dspace/config/clarin-dspace.cfg; set the real value on the live stack via local.cfg or DSPACE__P__CLARIN__P__TOKEN__P__ENCRYPTION__P__SECRET env, generated by `clarin-token -g`. Also set clarin.token.max.expiration.time.in.days.
- **Depends on:** BE-CFG-1
- **Verification:** docker exec clarinv9-dspace /dspace/bin/dspace clarin-token -g  # capture; set as property; then docker exec clarinv9-dspace /dspace/bin/dspace dsprop -p clarin.token.encryption.secret

#### BE-PAT-2 — Register clarin-token CLI in launcher.xml (M7 CLI)  `[major/S]`

- **Gap/impact:** VERIFIED via launcher <name> set-diff HEAD vs dtq-dev: exactly two dtq-dev commands are missing from HEAD — clarin-token AND matomo-report-generator (HEAD adds v9's healthcheck). So `dspace clarin-token` is an unknown command and no tokens can be created — which is why the live clarin_token table has 0 rows. Target class org.dspace.administer.ClarinTokenAdministrator (+ ClarinTokenCreator) and the emails/clarin_token template already exist in HEAD.
- **Reuse source:** origin/dtq-dev:dspace/config/launcher.xml lines 349-355 — `<command><name>clarin-token</name><description>Clarin Token Administrator</description><step><class>org.dspace.administer.ClarinTokenAdministrator</class></step></command>`.
- **v9 adaptation:** None — launcher.xml schema unchanged in v9. Insert the clarin-token command block only. Do NOT also add matomo-report-generator here — it is the Matomo domain's launcher entry (flag in openQuestions so it is not lost). Do NOT add item-version-linker: it is not in dtq-dev launcher.xml at all (correctly a deferred CLI, not a launcher port).
- **Depends on:** BE-CFG-2
- **Verification:** before=$(docker exec clarinv9-dspacedb psql -U dspace -d dspace -tAc 'select count(*) from clarin_token'); docker exec clarinv9-dspace /dspace/bin/dspace clarin-token -c -e test@test.cz -x 30d; after=$(docker exec clarinv9-dspacedb psql -U dspace -d dspace -tAc 'select count(*) from clarin_token'); test $((after-before)) -eq 1

#### BE-PAT-1 — clarin_token authenticates REST requests (M7 auth branch)  `[major/S]`

- **Gap/impact:** VERIFIED: HEAD dspace-server-webapp/.../security/jwt/JWTTokenRestAuthenticationServiceImpl.java has ZERO clarin references, so a Bearer clarin_token cannot authenticate any REST call. dtq-dev version imports ClarinTokenUtils/ClarinToken/ClarinTokenService (lines 21,28,29), has @Autowired ClarinTokenService (line 72) and the isClarinToken branch (lines 137-141).
- **Reuse source:** origin/dtq-dev:dspace-server-webapp/src/main/java/org/dspace/app/rest/security/jwt/JWTTokenRestAuthenticationServiceImpl.java — port ONLY: the three clarin imports; the @Autowired ClarinTokenService field; and inside the existing getEPerson/parse else-branch: `if (ClarinTokenUtils.isClarinToken(token)) { ePerson = clarinTokenService.getEPersonFromClarinToken(context, token); if (ePerson != null) { context.setCurrentUser(ePerson); context.setAuthenticationMethod(ClarinToken.AUTHENTICATION_METHOD); } } else { ePerson = loginJWTTokenHandler.parseEPersonFromToken(...); }`
- **v9 adaptation:** Insert the clarin branch into v9's existing else block only. Do NOT revert v9 code in HEAD: keep jakarta.servlet imports, log4j2 LogManager, DSpaceCsrfAuthenticationStrategy-based resetCSRFToken, and cookie .path("/server/api/authn") hardening — dtq-dev's older slf4j/manual-cookie forms are 7.6 vanilla, not CLARIN deltas.
- **Depends on:** BE-CFG-2, BE-PAT-2
- **Verification:** TOKEN=$(docker exec clarinv9-dspace /dspace/bin/dspace clarin-token -c -e test@test.cz -x 30d | tail -n1 | tr -d '\r'); curl -s -H "Authorization: Bearer $TOKEN" http://localhost:18080/server/api/authn/status | grep -o '"authenticated":true'

#### BE-AUTH-1 — EPerson admin-create writes a user_registration row (M8 REST hook)  `[major/S]`

- **Gap/impact:** VERIFIED: HEAD dspace-server-webapp/.../repository/EPersonRestRepository.java has ZERO clarin references, so POST /api/core/epersons by an admin no longer inserts a user_registration row. dtq-dev imports UNKNOWN_USER_REGISTRATION + ClarinUserRegistration(Service) (lines 10,40,41), has @Autowired clarinUserRegistrationService (line 92) and the create block (lines 152-157). Silent regression: CLARIN license-signature / UAU features that join on user_registration break for every user created post-upgrade.
- **Reuse source:** origin/dtq-dev:dspace-server-webapp/src/main/java/org/dspace/app/rest/repository/EPersonRestRepository.java lines 152-157 — after es.update + setMetadata: `ClarinUserRegistration r=new ClarinUserRegistration(); r.setOrganization(UNKNOWN_USER_REGISTRATION); r.setConfirmation(true); r.setEmail(eperson.getEmail()); r.setPersonID(eperson.getID()); clarinUserRegistrationService.create(context, r);` plus the three clarin imports and the @Autowired field. ClarinUserRegistrationService + ClarinServiceFactory already in HEAD.
- **v9 adaptation:** Keep v9's @Component(EPersonRest.CATEGORY + '.' + EPersonRest.PLURAL_NAME) (HEAD line 64), v9's createEPersonFromRestObject/createAndReturn structure and checkRequiredProperties(registrationData, epersonRest) (HEAD lines 121/194/207). Insert ONLY the user_registration create block. Do NOT port dtq-dev lines 140-141 (setWelcomeInfo/setCanEditSubmissionMetadata) here unless BE-MISC-1 has landed — those getters do not exist on HEAD's EPersonRest DTO.
- **Verification:** curl -s -X POST -H "Authorization: Bearer <admin_jwt>" -H 'Content-Type: application/json' -d '{"email":"foo@test.cz","metadata":{"eperson.firstname":[{"value":"Foo"}]},"canLogIn":true}' http://localhost:18080/server/api/core/epersons; docker exec clarinv9-dspacedb psql -U dspace -d dspace -tAc "select organization,confirmation from user_registration where email='foo@test.cz'"

#### BE-AUTH-2 — CLI create-administrator writes a user_registration row + -o option (M8 CLI hook)  `[major/S]`

- **Gap/impact:** VERIFIED: HEAD dspace-api/.../administer/CreateAdministrator.java is one of the 40 files byte-identical to dspace-7.6.5 (CLARIN hunk fully dropped) — it lacks the -o/--organization option and never creates a ClarinUserRegistration, so `dspace create-administrator` produces an admin with no user_registration entry.
- **Reuse source:** origin/dtq-dev:dspace-api/src/main/java/org/dspace/administer/CreateAdministrator.java — port: OPT_ORGANIZATION Option('o','organization'); options.addOption; the organization guard; clarinUserRegistrationService = ClarinServiceFactory.getInstance().getClarinUserRegistration(); the createAdministrator(...,String organization) signature+call sites; and the create block (setOrganization(organization), setConfirmation(true), setEmail, setPersonID, create). ClarinServiceFactory.getClarinUserRegistration() confirmed present in HEAD.
- **v9 adaptation:** Re-apply the CLARIN hunk on top of v9's CreateAdministrator method bodies (v9 uses the same negotiateEPersonPassword flow). Document whether -o is required (matches dtq-dev) or optional-defaulting-to 'Unknown'.
- **Verification:** docker exec clarinv9-dspace /dspace/bin/dspace create-administrator -e a@t.cz -f A -l T -c en -p pw -o MyOrg; docker exec clarinv9-dspacedb psql -U dspace -d dspace -tAc "select organization from user_registration where email='a@t.cz'"

#### BE-MISC-1 — EPersonRest DTO welcomeInfo/canEditSubmissionMetadata field + converter + repo setters (feature port)  `[medium/M]`

- **Gap/impact:** VERIFIED: HEAD dspace-server-webapp EPersonRest.java (DTO) has ZERO welcomeInfo/canEditSubmissionMetadata, and EPersonConverter.java is byte-vanilla (no mapping). The dspace-api EPerson ENTITY already exposes both getters/setters + DB columns welcome_info/can_edit_submission_metadata (HEAD lines 76-80,452-465), so only the REST surface is missing. Consequence: the eperson admin form cannot read/write welcomeInfo or canEditSubmissionMetadata, and BE-AUTH-1's setWelcomeInfo/setCanEditSubmissionMetadata lines (dtq-dev EPersonRestRepository 140-141) cannot compile — this item unblocks the full EPersonRestRepository port and the FE eperson-form fields.
- **Reuse source:** origin/dtq-dev DTO+converter+repo trio: (1) dspace-server-webapp/.../model/EPersonRest.java lines 42-43 (fields) + 109-119 (getters/setters); (2) dspace-server-webapp/.../converter/EPersonConverter.java lines 33-34 (eperson.setWelcomeInfo(obj.getWelcomeInfo()); eperson.setCanEditSubmissionMetadata(obj.getCanEditSubmissionMetadata())); (3) EPersonRestRepository.java lines 140-141 (eperson.setWelcomeInfo(epersonRest.getWelcomeInfo()); eperson.setCanEditSubmissionMetadata(...)).
- **v9 adaptation:** Add fields + JavaBean getters/setters to v9 EPersonRest (keep v9 imports/annotations); add the two mapping lines to v9 EPersonConverter.convert; add the two setter lines to v9 EPersonRestRepository.createEPersonFromRestObject and the PATCH/update path. Entity + DB column already present, no migration.
- **Verification:** curl -s -H 'Authorization: Bearer <admin_jwt>' http://localhost:18080/server/api/core/epersons/<uuid> | grep -oE '"(welcomeInfo|canEditSubmissionMetadata)"'

#### BE-CFG-3 — WebApplication.java: StrictHttpFirewall bean for encoded-slash handles + ISO/Shibboleth header values (M10)  `[major/S]`

- **Gap/impact:** VERIFIED and CORRECTED landing site: v9 RENAMED Application.java to WebApplication.java (org.dspace.app.rest.WebApplication, HEAD line 57) — the old path does not exist in HEAD. HEAD WebApplication has NO HttpFirewall @Bean (git grep HttpFirewall over dspace-server-webapp/src/main/java returns nothing). Without it, StrictHttpFirewall rejects encoded-slash handle URLs (%2F) and non-ASCII ISO/Shibboleth header values → request failures. dtq-dev adds allowUrlEncodedSlashHttpFirewall() (Application.java lines 180-187) which Spring Security auto-detects as an HttpFirewall bean; it also adds CLARIN CORS allowedHeaders (x-recaptcha-token, VERIFICATION_TOKEN_HEADER, X-On-Behalf-Of) at lines 219-221.
- **Reuse source:** origin/dtq-dev:dspace-server-webapp/src/main/java/org/dspace/app/rest/Application.java — port into HEAD's WebApplication.java: (a) the @Bean HttpFirewall allowUrlEncodedSlashHttpFirewall() { StrictHttpFirewall f=new StrictHttpFirewall(); f.setAllowedHeaderValues(v->true); ... return f; } (lines 180-187) + imports HttpFirewall/StrictHttpFirewall (lines 43-44); (b) into each WebMvcConfigurer addMapping('/api/**') .allowedHeaders(...) add "X-On-Behalf-Of","x-recaptcha-token",VERIFICATION_TOKEN_HEADER + import static ClarinShibbolethLoginFilter.VERIFICATION_TOKEN_HEADER (line 10; ClarinShibbolethLoginFilter confirmed present in HEAD).
- **v9 adaptation:** v9 renamed the class and reorganized CORS into WebMvcConfigurer.addCorsMappings with .exposedHeaders("Authorization","DSPACE-XSRF-TOKEN","Location","WWW-Authenticate") at HEAD lines 178-212. Add the @Bean to WebApplication (no explicit wiring needed — Spring Security picks up the HttpFirewall bean). Extend allowedHeaders (NOT exposedHeaders) on the /api/** mapping with the three CLARIN headers. Keep all v9 mappings/methods intact.
- **Verification:** curl -s -o /dev/null -w '%{http_code}' 'http://localhost:18080/server/api/pid/find?id=<community-handle-with-slash>'  # expect 200/404, never 400 firewall-reject

#### BE-CONV-1 — ItemConverter.updateItemDatesMetadata delta (M10)  `[major/S]`

- **Gap/impact:** VERIFIED: HEAD dspace-server-webapp/.../converter/ItemConverter.java is byte-identical to dspace-7.6.5 (one of the 40 dropped) — ZERO clarin refs. dtq-dev injects ClarinItemService (line 50) and calls clarinItemService.updateItemDatesMetadata(context, obj) during convert (line 62) so item date metadata (e.g. local.approximateDate.issued / normalized issued dates) is refreshed on every item serialization. ClarinItemService.updateItemDatesMetadata already exists in HEAD.
- **Reuse source:** origin/dtq-dev:dspace-server-webapp/src/main/java/org/dspace/app/rest/converter/ItemConverter.java — port: import org.dspace.content.service.clarin.ClarinItemService (line 27); @Autowired(or @Lazy) private ClarinItemService clarinItemService (line 50); and the clarinItemService.updateItemDatesMetadata(context, obj) call inside convert(...) (line 62).
- **v9 adaptation:** Re-apply on v9 ItemConverter.convert signature. Confirm Context availability in v9's convert path (v9 ItemConverter obtains context similarly). No API change to ClarinItemService.
- **Verification:** git show HEAD:dspace-server-webapp/src/main/java/org/dspace/app/rest/converter/ItemConverter.java | grep -c updateItemDatesMetadata

#### BE-INSTALL-1 — InstallItemServiceImpl.addLanguageNameToMetadata partial-port completion (M10)  `[major/S]`

- **Gap/impact:** VERIFIED partial port: HEAD InstallItemServiceImpl.java HAS the CLARIN SET_OWNING_COLLECTION_EVENT_DETAIL constant + @Autowired ConfigurationService (lines 47,66), but is MISSING the addLanguageNameToMetadata(Context,Item) method (dtq-dev line 343) and its invocation from the install flow (dtq-dev line 238). Consequence: freshly installed items never get the human-readable language-name metadata written from the language code, so downstream displays/facets relying on it are empty for new deposits.
- **Reuse source:** origin/dtq-dev:dspace-api/src/main/java/org/dspace/content/InstallItemServiceImpl.java — port: the private addLanguageNameToMetadata(Context c, Item item) method body (line 343 onward) and its call site inside installItem/finishItem (line 238). Uses already-present configurationService.
- **v9 adaptation:** Insert the method + call on top of HEAD's already-partially-ported class; keep v9 install flow ordering. Verify the metadata field/schema the method writes (local.* language name) exists in the registry (cross-ref M12 local-types.xml).
- **Verification:** git show HEAD:dspace-api/src/main/java/org/dspace/content/InstallItemServiceImpl.java | grep -c addLanguageNameToMetadata

#### BE-WSI-1 — WorkspaceItemRestRepository CLARIN license-granted/resource PATCH ops (M10)  `[major/M]`

- **Gap/impact:** VERIFIED: HEAD WorkspaceItemRestRepository.java has ZERO clarin refs; dtq-dev adds the CLARIN submission license PATCH handling — imports OPERATION_PATH_LICENSE_GRANTED/OPERATION_PATH_LICENSE_RESOURCE (lines 10-11), ClarinLicenseNotFoundException/ClarinLicenseSubmissionUtils/ClarinLicenseService/ClarinLicenseResourceMappingService (lines 34,47,65-66,145,148), and the patch dispatch (lines 247-251) + maintainClarinLicense logic (lines 522-561) that applies a selected clarin license to workspace-item bitstreams. Without it the submission clarin-license step's granted/resource PATCH ops are inert (cross-cuts the license/submission domains but is a namesake vanilla-file delta).
- **Reuse source:** origin/dtq-dev:dspace-server-webapp/src/main/java/org/dspace/app/rest/repository/WorkspaceItemRestRepository.java — port the six clarin imports, the two @Autowired clarin service fields (lines 145,148), the OPERATION_PATH_LICENSE_RESOURCE/OPERATION_PATH_LICENSE_GRANTED branches in the patch handler (lines 247-251), and the private clarin-license maintenance method (lines 522-561, calling ClarinLicenseSubmissionUtils.applyLicense).
- **v9 adaptation:** Re-apply on v9's WorkspaceItemRestRepository patch(...) structure (v9 dispatches operations similarly). All four Clarin* services + ClarinLicenseSubmissionUtils + ClarinLicenseRestRepository constants confirmed present in HEAD. Coordinate with license-domain items (ClarinLicenseRestRepository) to avoid divergent constant definitions.
- **Verification:** git show HEAD:dspace-server-webapp/src/main/java/org/dspace/app/rest/repository/WorkspaceItemRestRepository.java | grep -c OPERATION_PATH_LICENSE

#### BE-CFG-4 — default.license: real CLARIN Deposit Licence Agreement text (M9)  `[major/S]`

- **Gap/impact:** VERIFIED asymmetry: HEAD dspace/config/default.license is still vanilla ('NOTE: PLACE YOUR OWN LICENSE HERE ...') while dtq-dev has the real 'Deposit Licence Agreement' text; default_cs.license IS already ported in HEAD. So every new deposit records the wrong legal text for the EN default license while CS is correct — a legal-text regression.
- **Reuse source:** origin/dtq-dev:dspace/config/default.license — copy the file content verbatim (starts 'Deposit Licence Agreement'). One of the 33 config files still byte-vanilla in HEAD.
- **v9 adaptation:** None — plain text asset, unchanged loader in v9. Straight file replacement.
- **Verification:** git show HEAD:dspace/config/default.license | head -1  # expect: Deposit Licence Agreement

#### BE-CFG-5 — orcid-authority-services.xml: register CachingOrcidRestConnector bean (ORCID authority NPE)  `[medium/S]`

- **Gap/impact:** VERIFIED: HEAD dspace/config/spring/api/orcid-authority-services.xml wires plain orcidRestConnector (line 24) but is MISSING dtq-dev's <bean name="CachingOrcidRestConnector" class="org.dspace.external.CachingOrcidRestConnector"> (dtq-dev line 28). The CachingOrcidRestConnector.java class IS present in HEAD but the bean is never referenced in any HEAD config, so enabling ORCID authority NPEs on the unregistered caching connector.
- **Reuse source:** origin/dtq-dev:dspace/config/spring/api/orcid-authority-services.xml line 28+ — the <bean name="CachingOrcidRestConnector" class="org.dspace.external.CachingOrcidRestConnector"> definition (+ any property refs it declares) and the property/ref that points the ORCID authority source at it instead of the plain connector.
- **v9 adaptation:** Add the bean + repoint the authority source ref. Verify constructor/property signatures of HEAD's CachingOrcidRestConnector.java match the dtq-dev bean definition (class is byte-present; confirm no v9 constructor drift).
- **Verification:** git show HEAD:dspace/config/spring/api/orcid-authority-services.xml | grep -c CachingOrcidRestConnector

#### BE-CFG-6 — authentication-shibboleth.cfg: CLARIN header mapping delta (env-tuned)  `[medium/S]`

- **Gap/impact:** VERIFIED: HEAD authentication-shibboleth.cfg differs from baseline only cosmetically (v9 typo fixes) — the CLARIN header-mapping delta is unported. dtq-dev sets netid-header=eppn,persistent-id; email-header=mail; firstname-header=givenName; lastname-header=sn; and enables authentication-shibboleth.eperson.metadata = SHIB-telephone => eperson.phone,SHIB-cn => cn,SHIB-GIVENNAME => eperson.firstname,SHIB-SURNAME => eperson.lastname. Without it, Shibboleth login (when an IdP is wired) reads the wrong attribute headers.
- **Reuse source:** origin/dtq-dev:dspace/config/modules/authentication-shibboleth.cfg — port the 4 header-value lines + the uncommented eperson.metadata mapping line (baseline->dtq diff shows 5+/7- lines). Do NOT copy dtq-dev's 'limitted'/'attirbutes' typos (keep v9's corrected spelling).
- **v9 adaptation:** Apply only the property-VALUE changes on top of HEAD's (typo-fixed) file; v9 ShibbolethAuthentication reads these keys unchanged. This is env-gated: header names must match the real production IdP (coordinate with the external-Shibboleth deferred item); land the tracked defaults but treat live verification as env-dependent.
- **Depends on:** BE-CFG-1
- **Verification:** docker exec clarinv9-dspace /dspace/bin/dspace dsprop -p authentication-shibboleth.netid-header

#### BE-CFG-7 — Email templates: CLARIN dspace.name→dspace.shortname branding delta (low)  `[low/S]`

- **Gap/impact:** VERIFIED: ~13 dspace/config/emails/* templates (register, feedback, welcome, change_password, submit_* , request_item.* , registration_notify, internal_error, export_*, harvesting_error, doi_maintenance_error, flowtask_notify, healthcheck) are byte-vanilla in HEAD; dtq-dev swaps ${config.get('dspace.name')} → ${config.get('dspace.shortname')} in subjects/signatures so outgoing mail carries the CLARIN short brand.
- **Reuse source:** origin/dtq-dev:dspace/config/emails/* — apply the dspace.name→dspace.shortname substitution in each of the ~13 templates listed in the config-reconciliation manifest (BE-RECONCILE-1). Pure text edits.
- **v9 adaptation:** None — Velocity email format unchanged in v9. Verify dspace.shortname is configured (it is in dspace.cfg); otherwise subjects render empty.
- **Verification:** for f in register feedback welcome; do git show HEAD:dspace/config/emails/$f | grep -c "dspace.shortname"; done

#### BE-RECONCILE-1 — Systematic reconciliation of dropped CLARIN vanilla-file/config hunks (M10 backlog)  `[major/L]`

- **Gap/impact:** MEASURED backlog beyond the explicitly-named items: dtq-dev modifies 222 baseline .java files of which 40 are STILL byte-identical to dspace-7.6.5 in HEAD (CLARIN hunk fully dropped) PLUS partial ports like InstallItemServiceImpl; and 61 baseline config files of which 33 are still byte-vanilla. Cherry-picking single files leaves the domain's namesake work unscoped. A repeatable enumerate→triage→port mechanism is required so nothing is silently left vanilla, and so files owned by other domains (OAI DSpaceResourceResolver; submission DCInputSet/DCInputsReader/MetadataValidation/UploadValidation/SubmissionFormConverter/SubmissionFormFieldRest/DSpaceObjectMetadata*Operation; versioning VersioningConsumer/CanCreateVersion*; curation RequiredMetadata/ItemCheck; storage StorageServiceFactory*; bitstream BitstreamConverter/BitstreamRemoveOperation/ChecksumResultCode; iiif IIIFCacheEventConsumer) are routed to those domains rather than dropped.
- **Reuse source:** origin/dtq-dev entire baseline→dtq delta: for each path in `git diff dspace-7.6.5 origin/dtq-dev --name-only --diff-filter=M -- '*.java' 'dspace/config/**'`, the added-line CLARIN markers (new identifiers, method names, property keys, string literals) are the port source. No new code — every marker resolves to an existing dtq-dev hunk.
- **v9 adaptation:** Build a triage manifest script (kept under scripts/ or dspace/util, committed to BE PR #1339): for each modified file, extract CLARIN marker tokens from the baseline→dtq added lines, grep HEAD:<file> for each token, classify present / absent-fully-vanilla / absent-partial. For each ABSENT token, either port the residual CLARIN hunk onto v9's structure or record a waiver row with reason (v9-obsoleted | env-gated-deferred | owned-by:<other-domain-item-id>). This domain ports the auth/user-registration/config/converter subset; hands off the rest via the manifest.
- **Depends on:** BE-AUTH-2, BE-CONV-1, BE-INSTALL-1, BE-WSI-1, BE-CFG-4, BE-CFG-5, BE-CFG-6, BE-CFG-7, BE-MISC-1
- **Verification:** git diff dspace-7.6.5 HEAD --name-only -- '*.java' 'dspace/config/**' | sort > /tmp/head_ported.txt; git diff dspace-7.6.5 origin/dtq-dev --name-only -- '*.java' 'dspace/config/**' | sort > /tmp/dtq_all.txt; comm -13 /tmp/head_ported.txt /tmp/dtq_all.txt  # residual list must be fully accounted for in the manifest

**Domain risks:** ClarinTokenServiceIT and the ClarinUserRegistration*/EPerson-create ITs referenced as acceptance are among the ~120 unported CLARIN test classes; porting even one drags in v9-adapted builders/matchers. Mitigation: the PRIMARY machine-check for BE-PAT-1/BE-AUTH-1/BE-AUTH-2 is the live-stack curl+psql probe (self-sufficient); the IT is a secondary gate. ClarinTokenServiceIT self-sets the secret in @Before (dtq-dev line 64) so it is comparatively self-contained. · clarin.token.encryption.secret is a deployment secret, not a committable value. If an operator forgets to set it (or sets a non-AES-length base64 string) BE boots but every token op throws at runtime. Mitigation: BE-CFG-2 asserts a decodable 16/24/32-byte key and mandates generation via `clarin-token -g`; add the commented placeholder to tracked clarin-dspace.cfg so the requirement is discoverable. · BE-WSI-1 (WorkspaceItemRestRepository license PATCH) and BE-INSTALL-1 (language-name metadata) cross-cut the license/submission and metadata-registry (M12) domains — the CLARIN metadata fields they write (local.*, dc.rights.label) must already exist in the registry or the writes fail silently. Sequence M12 registry re-apply before live verification of these two. · BE-CFG-6 shibboleth header values are production-IdP-specific; committing them as tracked defaults is correct but live end-to-end verification is env-gated on a real IdP (external Shibboleth is a documented-deferred item). Do not block the PR on a live Shibboleth login. · The 222/61 reconciliation counts include many files owned by OTHER domains; BE-RECONCILE-1 must NOT unilaterally re-port them (risk of clobbering another domain's in-flight work on the same PR #1339). It hands off via waiver rows keyed to other-domain item ids — coordination risk if those ids are not yet assigned. · BE-CFG-3 firewall bean setAllowedHeaderValues(v->true) disables StrictHttpFirewall header validation entirely (matches dtq-dev). This is a deliberate CLARIN relaxation for ISO/Shibboleth headers but widens the attack surface; note it in the PR so reviewers accept the trade-off consciously.

**Open questions:** matomo-report-generator is the SECOND dtq-dev launcher command missing from HEAD (verified via launcher <name> set-diff). It is out of this domain (Matomo/reporting) — ensure the Matomo domain owns registering it in launcher.xml so it is not lost. · The 40 fully-dropped .java files span multiple domains: OAI (dspace-oai .../DSpaceResourceResolver.java), submission (DCInputSet, DCInputsReader, MetadataValidation, UploadValidation, SubmissionFormConverter, SubmissionFormFieldRest, DSpaceObjectMetadata{Add,Remove,Replace}Operation, BitstreamRemoveOperation, ItemDiscoverableReplaceOperation), versioning (VersioningConsumer, VersionHistoryRestRepositoryIT, CanCreateVersionFeatureIT, VersioningWithRelationshipsIT), curation (RequiredMetadata, ItemCheck), storage/S3 (StorageServiceFactory, StorageServiceFactoryImpl), bitstream/checksum (BitstreamConverter, ChecksumResultCode), iiif (IIIFCacheEventConsumer), plus tests (EPersonBuilder, WorkflowItemMatcher, EPersonRestAuthenticationProviderTest, UtilsTest, HandleDAOImplTest, PubmedImportMetadataSourceServiceIT, DiscoverConfigurationConverterTest, DiscoverConfigurationConverter, ScriptConverter, ItemExportScriptConfiguration, CommunityServiceImpl, ResourcePolicyDAO, ResourcePolicyService). Each needs an owning domain item; my BE-RECONCILE-1 manifest enumerates them but does not port them. · Config residual (33 byte-vanilla) includes registry files local-types.xml and bitstream-formats.xml (M12 domain), oai crosswalk files description.xml/oai_dc.xsl and requestitem.xml/versioning-service.xml/identifiers.cfg/ehcache.xml/log4j2*.xml/submission-forms.dtd — route registry/OAI/versioning files to their domains; identifiers.cfg (uncomment identifiers.submission.register=true + display=handle) likely belongs to the Handle/PID domain. · Confirm whether ResourcePolicyDAO/ResourcePolicyService CLARIN deltas (in the 40) underpin the C2 license/download-gate (AuthorizeServiceImpl.authorizeAction hook). If so, the license-gate domain should own them; if they are prerequisite to C2 they must land before C2's authorizeBitstream hook. · EPerson entity already carries welcome_info/can_edit_submission_metadata columns+getters in HEAD but the FE eperson admin form and any REST consumers are unverified — the FE domain should confirm the eperson-form binds welcomeInfo/canEditSubmissionMetadata once BE-MISC-1 exposes them. · Container/DB coordinates used in all verification commands (clarinv9-dspace, clarinv9-dspacedb, psql -U dspace -d dspace, BE :18080) are confirmed live now, but an implementing agent must not rebuild/restart containers per the read-only mandate; if the stack is recreated the encryption secret (BE-CFG-2) must be re-set or all token verifications regress to failing.

### FE — admin GUIs (license/handle/ePIC), ISO language facet, static pages, item-edit license tab

Seven work items across six audit findings. Correcting the draft's central error (flagged by the critique and confirmed by git): M6 is NOT FE-only. The license-attach WRITE endpoint — the PUT `updateLicenseForBundle(...@RequestParam("licenseID"))` method in ItemAddBundleController — was deleted at HEAD (git diff origin/dtq-dev HEAD confirms the whole method + ClarinLicenseService/ClarinLicenseResourceMappingService/ProvenanceService fields removed; `git grep licenseID HEAD -- dspace-server-webapp/src/main/java` is empty). ItemLicenseMapperComponent (HEAD line ~199) PUTs to `{root}/core/items/{uuid}/bundles?licenseID=<id>`, so even after the FE route/guard/FeatureID wiring the tab would render but Update would fail. So M6 splits into FE-ADMIN-M6-FE (PR #1316) and BE-ADMIN-M6-BE (PR #1339). The other five are wiring drops from the v9 standalone/tree-shaking migration where CLARIN component code was ported but its registration was lost (module provider, model-registration array, enum member, filterType map entry, asset glob, route/guard); one (M3 ePIC) is a BE bean-name gotcha. I verified every BE half is live and correct: clarinlicenses/handles/clarinlicense endpoints 200, iso_language facet returns localized labels, CanManageLicense feature plugin exists (BE HEAD .../authorization/impl/CanManageLicense.java, NAME="canManageLicense"), and the bean-name→root-rel mapping is proven (live root exposes handles/clarinlicenses/epichandle — plural for the plural-bean repos, singular for ePIC). Sequencing: M1/M2/M4 are independent 1–3-line edits (do first); C4 is a bulk 82-file port + one angular.json line; M6-FE (4 files) needs M6-BE for the end-to-end Update criterion; M3 and M6-BE land in BE PR #1339, everything else in FE PR #1316. Coordination: M2 edits the SHARED src/app/core/provide-core.ts models[] array that out-of-domain C1 (metadatabitstream) must also touch — batch one edit to PR #1316 to avoid conflict.

#### FE-ADMIN-M1 — Add providers:[NgbActiveModal] to ClarinLicenseTableComponent so /licenses/manage-table renders (NG0201)  `[major/S]`

- **Gap/impact:** /licenses/manage-table renders blank with runtime NG0201 'No provider for NgbActiveModal'. Standalone ClarinLicenseTableComponent imports NgbActiveModal (HEAD src/app/clarin-licenses/clarin-license-table/clarin-license-table.component.ts line 8) and injects `public activeModal: NgbActiveModal` in its constructor (line 78), but its @Component decorator (line 57) has no providers array. It is rendered as a page (ClarinLicensePageComponent host), NOT opened via NgbModal.open(), so no modal injector supplies NgbActiveModal. The entire CLARIN license/label admin GUI is dead though BE returns data.
- **Reuse source:** dspace-angular origin/dtq-dev:src/app/clarin-licenses/clarin-license.module.ts lines 9,29-30 (`import { NgbActiveModal }` + `providers: [ NgbActiveModal ]` at NgModule level). Target file already ported: HEAD src/app/clarin-licenses/clarin-license-table/clarin-license-table.component.ts.
- **v9 adaptation:** Standalone migration dropped ClarinLicenseModule; move its provider onto the component. Add `providers: [NgbActiveModal]` to the @Component({...}) decorator at line 57 of clarin-license-table.component.ts. The child modal components (DefineLicenseFormComponent, DefineLicenseLabelFormComponent) stay unchanged — they still receive NgbActiveModal from NgbModal.open()'s injector.
- **Verification:** Serve FE prod build (npm run build:prod), log in as admin, open /licenses/manage-table; assert rows and clean console. BE data confirmed live: curl -s http://localhost:18080/server/api/core/clarinlicenses -> HTTP 200 with type=clarinlicense entries.

#### FE-ADMIN-M2 — Register Handle model in provide-core models[] so /handle-table deserializes (VALUE import)  `[major/S]`

- **Gap/impact:** /handle-table shows 0 rows; RemoteData fails with 'cannot deserialize type handle'. Handle's @typedObject decorator never runs because HEAD src/app/core/provide-core.ts `export const models` (line 110+) omits Handle. Handle is referenced only in TYPE positions in HandleDataService (BaseDataService<Handle> etc.), so the v9 isolatedModules build elides it as type-only and the decorator that registers resource type 'handle' in the serializer registry never executes. Breaks handle listing, create-handle, edit-handle, change-prefix.
- **Reuse source:** dspace-angular origin/dtq-dev:src/app/core/core.module.ts models[] (included Handle at the array tail; note dtq's core.module models[] deliberately OMITS EpicHandle — same asymmetry we preserve, see M3). Model file already ported: HEAD src/app/core/handle/handle.model.ts (@typedObject, static type = HANDLE).
- **v9 adaptation:** dtq models[] lived in core.module.ts (NgModule); in v9 it moved to standalone provide-core.ts. In HEAD src/app/core/provide-core.ts add a VALUE import `import { Handle } from './handle/handle.model';` and add `Handle,` to the `export const models` array (near line 114 where `Bundle,` sits). Do NOT use `import type` — the value reference is what forces the decorator to run. COORDINATION: out-of-domain C1 (metadatabitstream) must add its model(s) to this SAME array — apply one batched edit to PR #1316.
- **Verification:** Load /handle-table as admin; assert rows and no deserialize error. BE confirmed live: curl -s 'http://localhost:18080/server/api/core/handles?size=1' -> "type":"handle", totalElements 8673; root HAL exposes rel "handles".

#### BE-ADMIN-M3 — Add EpicHandleRest.PLURAL_NAME and register controller bean as core.epichandles so ePIC GUI resolves its endpoint (BE PR #1339)  `[major/S]`

- **Gap/impact:** /epic-handle-table/prefix is dead: FE src/app/core/data/epic-handle-data.service.ts calls halService.getEndpoint('epichandles') (plural, line 128/160/179/204) but the live root /server/api exposes only the singular rel 'epichandle', so getEndpoint returns undefined. Root cause: EpicHandleRestController (HEAD .../repository/EpicHandleRestController.java line 54) registers `@Component(EpicHandleRest.CATEGORY + "." + EpicHandleRest.NAME)` = bean 'core.epichandle', and the root HAL rel is derived from the bean short-name (proven live: core.handles->rel 'handles', core.clarinlicenses->rel 'clarinlicenses', core.epichandle->rel 'epichandle'). EpicHandleRest lacks the PLURAL_NAME constant every working v9 model declares.
- **Reuse source:** BE HEAD sibling pattern to mirror: dspace-server-webapp/.../model/HandleRest.java line 26 `public static final String PLURAL_NAME = NAME + "s";` and .../repository/HandleRestRepository.java line 49 `@Component(HandleRest.CATEGORY + "." + HandleRest.PLURAL_NAME)`. Files to edit exist at HEAD: .../model/EpicHandleRest.java and .../repository/EpicHandleRestController.java.
- **v9 adaptation:** Documented v9 PLURAL_NAME/bean-name gotcha (BE, push to PR #1339). (a) In EpicHandleRest.java add `public static final String PLURAL_NAME = NAME + "s";` (="epichandles"; NAME="epichandle" at line 20). (b) In EpicHandleRestController.java line 54 change the @Component argument from `EpicHandleRest.NAME` to `EpicHandleRest.PLURAL_NAME` so the bean registers as core.epichandles. The controller already @RequestMappings both URI_PREFIX and URI_PREFIX_PLURAL and only maps `{prefix}` / `{prefix}/{suffix}` sub-paths (no base handler), so no @RequestMapping change. No FE change — FE already calls the plural.
- **Verification:** After BE redeploy, curl root api for 'epichandles' rel; load /epic-handle-table/prefix as admin. Confirmed current break live: root shows only 'epichandle'; /api/core/epichandles base->404, /api/core/epichandle base->405. NOTE: full GET-by-prefix returning real handle data depends on the external ePIC PID service config (may be env-gated locally) — the root-rel check proves the bean fix independent of that.

#### FE-ADMIN-M4 — Add FilterType.isoLanguage + map it to SearchTextFilterComponent so the Language (ISO) facet renders  `[major/S]`

- **Gap/impact:** On /search the 'Language' facet body is empty. BE returns facetType 'iso_language' with already-localized labels (English, Czech, German, Spanish, 'Nolinguistic content'), but HEAD src/app/shared/search/models/filter-type.model.ts has no isoLanguage member and no component is mapped to it, so renderFilterType('iso_language') returns undefined and the facet renders nothing.
- **Reuse source:** dspace-angular origin/dtq-dev:src/app/shared/search/models/filter-type.model.ts line 33 (`isoLanguage = 'iso_language'`) and origin/dtq-dev:.../search-text-filter/search-text-filter.component.ts line 24 (`@renderFacetFor(FilterType.isoLanguage)` — reuses the text-filter renderer).
- **v9 adaptation:** (1) Add `isoLanguage = 'iso_language'` to the FilterType enum in HEAD filter-type.model.ts. (2) The load-bearing v9 registration is the static map, NOT the @renderFacetFor decorator (deprecated and tree-shakeable): in HEAD src/app/shared/search/search-filters/search-filter/search-filter-type-decorator.ts add `filterTypeMap.set(FilterType.isoLanguage, SearchTextFilterComponent);` after the existing map.set block (lines 13-17; SearchTextFilterComponent already imported there for FilterType.text). Adding the decorator too matches dtq but is not sufficient alone.
- **Verification:** Open /search, expand Language facet, assert entries present and filter works. BE confirmed live: curl -s 'http://localhost:18080/server/api/discover/facets/language?size=5' -> facetType iso_language, labels English/Czech/German/Spanish.

#### FE-ADMIN-C4 — Port src/static-files/ (82 html, exact filenames) + add angular.json 'src/static-files' asset glob so /static/*.html serve  `[critical/M]`

- **Gap/impact:** ~82 CLARIN static HTML pages (about, faq, cite, cookies, deposit, terms-of-service, metadata, item-lifecycle, ~54 licence-/license-*.html, cs/* translations) 404. The serving path exists at HEAD (StaticPageComponent + html-content.service.ts fetching /static-files/<file>, route 'static' registered, STATIC_FILES_PROJECT_PATH='static-files'), but HEAD src/static-files/ has 0 files AND HEAD angular.json build `assets` omits the 'src/static-files' glob (dtq had it at line 43), so even ported files would not be copied to dist. Result: /static/<any>.html renders the error/empty page.
- **Reuse source:** dspace-angular origin/dtq-dev:src/static-files/** — exactly 82 .html files (verified count): incl. 25 `licence-*.html` (British) AND 29 `license-*.html` (American) spelling variants, plus an 11-file cs/ subtree. Plus origin/dtq-dev:angular.json line 43 build-target `assets` entry `"src/static-files"` (sits next to `"src/assets"`). NOTE: the referenced page images src/assets/images/static-pages/*.png ALREADY EXIST at HEAD (verified) — do NOT re-port images.
- **v9 adaptation:** Port dtq src/static-files/** verbatim into HEAD preserving every filename BYTE-FOR-BYTE (British 'licence' vs American 'license' spelling AND case) and the cs/ subfolder — StaticPageComponent serves by exact filename, so any renamed/dropped file 404s that page. Add the string `"src/static-files"` to the `assets` array of the build architect target in HEAD angular.json (which already contains "src/assets", "src/robots.txt", etc.). No serving-code change. The audit's example 'aup.html' is NOT in the set — use about.html / terms-of-service.html / faq.html and real licence pages for checks.
- **Verification:** After FE build+deploy: `find dist/browser/static-files -name '*.html' | wc -l` and curl the URLs above. Confirmed current break: HEAD src/static-files has 0 files, angular.json has no 'static-files' asset entry, all /static/*.html 404.

#### FE-ADMIN-M6-FE — Wire item-edit license tab: add CanManageLicense FeatureID, a v9 FUNCTIONAL guard, the license route, and the .title i18n key  `[major/M]`

- **Gap/impact:** Item-edit 'license' tab is absent and its page unreachable. ItemLicenseMapperComponent is ported (HEAD src/app/item-page/edit-item-page/item-license-mapper/item-license-mapper.component.ts) but: (1) FE FeatureID enum (HEAD src/app/core/data/feature-authorization/feature-id.ts) lacks `CanManageLicense` (present in dtq at line 37); (2) there is no license route entry in HEAD edit-item-page-routes.ts (grep empty) — and EditItemPageComponent derives its tab bar from route.routeConfig.children, so no route => no tab (verified); (3) no license guard exists. The BE authz feature IS ported (CanManageLicense.java, NAME='canManageLicense') — this item is pure FE wiring; the WRITE endpoint is the separate BE-ADMIN-M6.
- **Reuse source:** For content/FeatureID: dtq feature-id.ts:37 `CanManageLicense = 'canManageLicense'`. For route SHAPE only (NOT verbatim): dtq edit-item-page.routing.module.ts:134-138 license route block. Guard is REWRITTEN — mirror the v9 functional sibling HEAD src/app/item-page/edit-item-page/item-page-collection-mapper.guard.ts (do NOT port dtq's class guard item-page-license-mapper.guard.ts). i18n: dtq en.json5 shipped only `item.edit.tabs.license.head` (line 5991) — `.head` already present at HEAD en.json5:7539.
- **v9 adaptation:** (a) feature-id.ts: add `CanManageLicense = 'canManageLicense',` to the FeatureID enum. (b) NEW file src/app/item-page/edit-item-page/item-page-license-mapper.guard.ts as a v9 FUNCTIONAL CanActivateFn (dtq's class `ItemPageLicenseMapperGuard extends DsoPageSingleFeatureGuard` will NOT work with v9 functional canActivate): `export const itemPageLicenseMapperGuard: CanActivateFn = dsoPageSingleFeatureGuard(() => itemPageResolver, () => of(FeatureID.CanManageLicense));` (copy imports from item-page-collection-mapper.guard.ts). (c) edit-item-page-routes.ts: add a child route `{ path: 'license', component: ItemLicenseMapperComponent, canActivate: [itemPageLicenseMapperGuard], data: { title: 'item.edit.tabs.license.title', showBreadcrumbs: true } }` (mirror the collection-mapper block at lines 127-131; add the two imports). (d) src/assets/i18n/en.json5: add `"item.edit.tabs.license.title"` (the route data.title key — sibling `item.edit.tabs.status.title` exists at HEAD but license.title does not; `.head` for the tab label already exists).
- **Depends on:** BE-ADMIN-M6
- **Verification:** npm run build:prod, log in as admin, open the item edit page, assert License tab present and /license renders the mapper; then test the guard as anon; then (with BE-ADMIN-M6 deployed) click Update and assert 200. FE state confirmed: CanManageLicense absent from HEAD feature-id.ts, no license route in edit-item-page-routes.ts, ItemLicenseMapperComponent present.

#### BE-ADMIN-M6 — Re-port ItemAddBundleController PUT updateLicenseForBundle(licenseID) write endpoint (BE PR #1339)  `[major/M]`

- **Gap/impact:** The license-attach WRITE endpoint the FE tab depends on is deleted at HEAD. ItemLicenseMapperComponent's Update does PutRequest to `{root}/core/items/{uuid}/bundles?licenseID=<id>`, but HEAD dspace-server-webapp/.../ItemAddBundleController.java contains only the vanilla POST addBundleToItem; the CLARIN PUT `updateLicenseForBundle` method and its ClarinLicenseService / ClarinLicenseResourceMappingService / ProvenanceService fields were removed. Proof: `git grep licenseID HEAD -- dspace-server-webapp/src/main/java` is empty; `git diff origin/dtq-dev HEAD -- .../ItemAddBundleController.java` shows the removed `@RequestMapping(method=RequestMethod.PUT)` + `@RequestParam("licenseID") Integer licenseId` handler. Without it, an admin can pick a license but Update fails (no PUT handler).
- **Reuse source:** dspace-server-webapp/src/main/java/org/dspace/app/rest/ItemAddBundleController.java as it exists on origin/dtq-dev — re-add the whole `updateLicenseForBundle(@PathVariable UUID uuid, @RequestParam("licenseID") Integer licenseId, ...)` method (`@RequestMapping(method=RequestMethod.PUT)`, `@PreAuthorize("hasPermission(#uuid,'ITEM','ADD')")`) plus its @Autowired ClarinLicenseService / ClarinLicenseResourceMappingService / ProvenanceService fields and the removed imports. All three services + methods exist at HEAD (verified: ClarinLicenseServiceImpl.addLicenseMetadataToItem/clearLicenseMetadataFromItem, ClarinLicenseResourceMappingServiceImpl.attachLicense/detachLicenses, org.dspace.core.ProvenanceService.updateLicense).
- **v9 adaptation:** Adapt the ported method to v9: change `javax.servlet.http.HttpServletRequest/Response` imports to `jakarta.servlet.http.*`; reuse the already-@Autowired ObjectMapper `mapper` field pattern where relevant (do not `new ObjectMapper()`); keep the body verbatim (loop CONTENT bundle bitstreams -> detachLicenses then attachLicense if license non-null; clearLicenseMetadataFromItem; addLicenseMetadataToItem if non-null; itemService.update; provenanceService.updateLicense; context.commit; return converter.toRest(item, utils.obtainProjection())). licenseID=-1 semantics: detach only, do not attach.
- **Verification:** After BE redeploy to PR #1339: git-grep the controller; obtain an admin JWT; PUT with a real licenseID and a real item uuid; assert 200 + metadata + DB rows via psql on localhost:15432. Confirmed missing at HEAD via git diff origin/dtq-dev HEAD -- ItemAddBundleController.java (full method removed).

**Domain risks:** SHARED-FILE CONFLICT: FE-ADMIN-M2 and out-of-domain C1 (metadatabitstream) both edit src/app/core/provide-core.ts models[] on the single FE PR #1316 — apply one batched edit or the second patch conflicts. Both model imports MUST be value imports (not `import type`) or the @typedObject decorators are tree-shaken and the fix silently regresses. · M6 GUARD MIS-PORT: if the implementer copies dtq's class guard (ItemPageLicenseMapperGuard extends DsoPageSingleFeatureGuard, @Injectable) verbatim, the v9 functional canActivate array will not accept it and the route breaks. The fix MUST be the functional dsoPageSingleFeatureGuard factory form mirroring item-page-collection-mapper.guard.ts. · C4 FILENAME FIDELITY: dtq src/static-files mixes 25 British `licence-*.html` and 29 American `license-*.html` names plus a cs/ subtree; StaticPageComponent serves by exact filename, so any renamed/case-folded/dropped file 404s that page. The 82-file dist count + both-spelling spot-checks are the guardrail. · M6 SPLIT ACROSS PRs: FE-ADMIN-M6-FE (PR #1316) and BE-ADMIN-M6 (PR #1339) must both land for the tab to work end-to-end; shipping only the FE half yields a tab whose Update button 404s. Sequence/track them together. · M3 goes to BE PR #1339 (not FE PR #1316) — route it correctly. The bean-rename is verifiable via the root-rel flip regardless of whether the external ePIC PID service is configured locally; full GET-by-prefix data may be env-gated.

**Open questions:** CROSS-DOMAIN (theme/footer): C4 makes /static/*.html reachable by direct URL, but the footer/navbar menu links that POINT to these static pages (about, faq, terms-of-service, cite, cookies, deposit) may have been dropped in the standalone migration — if so users cannot navigate to them even after C4. Likely lives in the theme/footer/menu-config domain; flag for that section to verify the link config was ported. · CROSS-DOMAIN (C1 metadatabitstream): C1 must also register its model(s) in the same src/app/core/provide-core.ts models[] array that M2 edits — confirm the C1 owner coordinates one batched edit to PR #1316. · M3 ePIC end-to-end: GET /api/core/epichandles/{prefix} returning real handle rows depends on the external ePIC/PID service config (handle.canonical.prefix / lr.pid.* / ePIC credentials) — confirm with the maintainer whether ePIC is configured on the target deploy, or whether M3's acceptance should stop at the root-rel + endpoint-resolution proof (env-gated data). · M6 Czech i18n: dtq shipped only `item.edit.tabs.license.head` (no `.title`); the new `item.edit.tabs.license.title` (en) plus the license tab head/title keys likely also need cs.json5 entries — folds into the separately-tracked Czech i18n gap (598/679 fork keys missing). Confirm ownership.

### FE — share submission, license selector, submission form glue (complex/type-bind), Czech i18n, meta tags, view-tracker

Nine FE regressions where the v9 standalone/control-flow migration either dropped a CLARIN customization entirely or reverted a fork-customized file to vanilla. Strategy is pure reuse from origin/dtq-dev (FE fork, head d8c5d54598); every item cites concrete files/commits verified against git this pass. Corrections applied after critique: (1) FE-NEWVERSION-1 — HEAD item-actions.component.ts imports are [NgbTooltip,RouterLink,TranslateModule] (NOT empty); the ported button uses @if(canCreateVersion$|async) + [dsBtnDisabled], so AsyncPipe AND BtnDisabledDirective MUST be added to imports[]; the spec asserts getVersioningTooltipMessage IS called (my earlier 'not called' criterion was wrong); dso-versioning-modal.service on HEAD already exposes isNewVersionButtonDisabled/getVersioningTooltipMessage/openCreateVersionModal so the item is fully self-contained. (2) FE-LICENSE-SELECTOR-1 — license-selector.js is NOT UMD and does NOT vendor jQuery; it is a webpack IIFE whose last two modules are `e.exports=jQuery` and `e.exports=_`, i.e. it externalizes global jQuery + global lodash. HEAD index.html already loads jQuery 2.1.4 from CDN (for DiscoJuice) so `$` is present, but global `_` is NOT — must add node_modules/lodash/lodash.min.js to angular.json scripts[] before license-selector.js. license-selector-creation.js's onLicenseSelected iterates dead XMLUI DOM (#aspect_submission_StepTransformer_list_license-list) and hardcodes legacy lindat licenses, so it must be rewritten to resolve the chosen license against the component's BE-driven licenses4Selector and set #secret-selected-license-from-license-selector + click #secret-change-button. (3) Czech i18n scope is 598 fork-added keys, NOT 627; the 29-key difference is v9-renamed/removed vanilla keys (cookies.consent.*, menu.section.icon.*, relationships.is*Of, grant-request-copy.intro) that must be EXCLUDED. (4) FE-REFRESH-UPLOAD-1 runtime depends on C1 (metadatabitstreams byHandle) which the commit invalidates and which currently 404s. Sequencing: land the independent verbatim/i18n/spec ports first (SHARE, NEWVERSION, VIEWTRACKER, METATAGS, I18N-CS, COMPLEX-PARSER-SPEC), do the TYPEBIND reconciliation with its 311-line ported spec as the gate (runtime half blocked on BE-FORMS), do LICENSE-SELECTOR (global provisioning + onLicenseSelected rewrite), and land REFRESH-UPLOAD last since its runtime proof is blocked on BE C1. All changes go to FE PR #1316 only. The biggest cross-domain blocker remains BE submission-forms.xml/item-submission.xml (EN) being byte-vanilla: the FE complex/type-bind/autocomplete renderers exist and are wired on HEAD but render nothing until BE form config defines those fields.

#### FE-SHARE-1 — M5: Restore MyDSpace 'Share submission' button in workspaceitem-actions  `[major/M]`

- **Gap/impact:** The v9 standalone migration rewrote workspaceitem-actions.component.{ts,html} and dropped the CLARIN share button: `git grep -c shareSubmission` on HEAD's workspaceitem-actions.component.ts returns 0. The share-submission PAGE + /share-submission route are ported and BE GET /submission/share exists (401 anon, not 404), so the whole flow is unreachable solely because the trigger button is missing. Submitters cannot hand a workspace item to another submitter.
- **Reuse source:** origin/dtq-dev:src/app/shared/mydspace-actions/workspaceitem/workspaceitem-actions.component.ts — shareSubmission() method (lines 142-170), ShareSubmissionLink interface (end of file), shareSubmissionSpinner field (line 44), constructor injections halService:HALEndpointService (line 80) + rdbService:RemoteDataBuildService (line 81), imports {GetRequest} from ../../../core/data/request.models (22), {HALEndpointService} (23), {RemoteDataBuildService} (24), getFirstCompletedRemoteData; origin/dtq-dev:.../workspaceitem-actions.component.html (button id `share_<id>` block, lines ~50-60); origin/dtq-dev:.../workspaceitem-actions.component.spec.ts (share test).
- **v9 adaptation:** HEAD component is already standalone with imports:[AsyncPipe,NgbTooltip,RouterLink,TranslateModule] — AsyncPipe is already present, so NO new imports[] entries are needed for the button. Add the 3 TS imports (GetRequest/HALEndpointService/RemoteDataBuildService), the 2 constructor injections, the shareSubmissionSpinner boolean field, and shareSubmission()/ShareSubmissionLink (requestService is already a base-class ctor param — reuse it). HEAD's workspaceitem-actions.component.html uses @if control flow (confirmed: `@if ((canEditItem$ | async)) {` at lines 10/19), so wrap the share button in `@if ((canEditItem$ | async)) { ... }` (NOT the dtq-dev *ngIf). shareSubmission() builds url = halService.getRootHref()+'/submission/share?workspaceitemid='+object.id, sends GetRequest, then on hasSucceeded router.navigate(['/share-submission'],{queryParams:{changeSubmitterLink: rd.payload?.shareLink}}) else error notification. EN keys already on HEAD (submission.workflow.generic.share-submission[.tooltip] en.json5:7742-7743; .email.successful/.error 7744-7745) — no EN key work.
- **Verification:** npm test -- --include='**/workspaceitem-actions.component.spec.ts'; manual: log in as submitter, open MyDSpace, confirm Share button appears on a workspace item and clicking it issues GET /submission/share (network tab) and redirects to /share-submission?changeSubmitterLink=...

#### FE-NEWVERSION-1 — Port 'New version' button for archived submission items (f46cb9cb3e)  `[medium/M]`

- **Gap/impact:** Commit f46cb9cb3e (item-actions.component.{ts,html,spec.ts}) is unported; `git grep -c 'canCreateVersion\|openCreateVersionModal'` on HEAD item-actions.component.ts returns 0. Archived items in MyDSpace have no 'New version' action.
- **Reuse source:** origin/dtq-dev commit f46cb9cb3e — src/app/shared/mydspace-actions/item/item-actions.component.ts (canCreateVersion$/disableNewVersion$/newVersionTooltip$ fields, initVersioningControls(), openCreateVersionModal(), ctor injections AuthorizationDataService + DsoVersioningModalService, imports FeatureID + hasValue + {Observable, of as observableOf} + shareReplay), item-actions.component.html (New version button block: `@if canCreateVersion$|async` with [dsBtnDisabled]="disableNewVersion$|async", [ngbTooltip]=(newVersionTooltip$|async)|translate, (click)=openCreateVersionModal()), item-actions.component.spec.ts (5 new it() blocks).
- **v9 adaptation:** CORRECTED: HEAD item-actions.component.ts imports = [NgbTooltip, RouterLink, TranslateModule] (NON-empty). The button uses `@if (canCreateVersion$ | async)` + three `| async` pipes AND `[dsBtnDisabled]`, so ADD TWO imports[] entries: AsyncPipe (from @angular/common) and BtnDisabledDirective (from ../../btn-disabled.directive; selector [dsBtnDisabled], class BtnDisabledDirective — exists on HEAD at src/app/shared/btn-disabled.directive.ts). Add ctor injections AuthorizationDataService + DsoVersioningModalService (both exist on HEAD; FeatureID.CanCreateVersion exists at feature-id.ts:28). No BE-helper port: HEAD dso-versioning-modal.service.ts already exposes isNewVersionButtonDisabled (line 93), getVersioningTooltipMessage (104), openCreateVersionModal (52). disableNewVersion$ = service.isNewVersionButtonDisabled(object).pipe(shareReplay(1)); newVersionTooltip$ = service.getVersioningTooltipMessage(object,'item.page.version.hasDraft','item.page.version.create'). HEAD item-actions.component.html currently has no @if — the button is a net-new @if block. EN keys item.page.version.create (en.json5:2988) + item.page.version.hasDraft (2994) present.
- **Verification:** npm test -- --include='**/item-actions.component.spec.ts'; manual: as an authorized user open MyDSpace, confirm New version button appears on an archived item and enabled/disabled+tooltip state matches draft presence.

#### FE-VIEWTRACKER-1 — Restore dc_identifier property in ViewTrackerResolverService page_view event  `[medium/S]`

- **Gap/impact:** HEAD's ViewTrackerResolverService (src/app/statistics/angulartics/dspace/view-tracker-resolver.service.ts) is wired into item/collection/community routes but its resolve() pushes only {object, referrer} to angulartics2.eventTrack — the CLARIN `dc_identifier` property (item's dc.identifier.uri, consumed by the Matomo/statistics tracker) is dropped. dtq-dev's version adds it. The fork's view-tracker-resolver.service.spec.ts (144 lines) is also absent on HEAD.
- **Reuse source:** origin/dtq-dev:src/app/statistics/angulartics/dspace/view-tracker-resolver.service.ts (resolve() body: `const object = this.getNestedProperty(...); const dc_identifier = object?.firstMetadataValue?.('dc.identifier.uri'); this.angulartics2.eventTrack.next({action:'page_view', properties:{object, referrer, dc_identifier}})`); origin/dtq-dev:src/app/statistics/angulartics/dspace/view-tracker-resolver.service.spec.ts (144 lines, absent on HEAD).
- **v9 adaptation:** Minimal: inside the existing HEAD resolve() subscribe, hoist `object` to a const, add `const dc_identifier = object?.firstMetadataValue?.('dc.identifier.uri');` and add `dc_identifier` to the eventTrack properties. firstMetadataValue is unchanged on v9 DSpaceObject. Port the spec verbatim; adjust import paths if any moved.
- **Verification:** npm test -- --include='**/view-tracker-resolver.service.spec.ts'; manual: open an item page with DevTools, confirm the angulartics page_view payload includes dc_identifier.

#### FE-METATAGS-1 — Restore CLARIN dataset_* meta tags in MetadataService  `[medium/M]`

- **Gap/impact:** HEAD src/app/core/metadata/metadata.service.ts has ZERO CLARIN meta-tag customization (`git grep -c 'setDataset\|dataset_' ` on HEAD == 0); it differs from baseline 7.6.3 only by v9's own isVirtual/virtualValue additions. The dtq-dev fork adds six dataset discovery meta tags (dataset_keywords/dataset_license/dataset_url/dataset_citation/dataset_identifier/dataset_creator) emitted unconditionally in listMetadataTags. On HEAD these tags never reach <head>, so external harvesters/aggregators that read them get nothing.
- **Reuse source:** origin/dtq-dev:src/app/core/metadata/metadata.service.ts — setDatasetKeywordsTag()/setDatasetLicenseTag()/setDatasetUrlTag()/setDatasetCitationTag()/setDatasetIdentifierTag()/setDatasetCreatorTag() (defined ~lines 314-345) and their six invocations in the tag-listing method (~lines 174-179); plus metadata.service.spec.ts fork additions. (Note: metadata-value.model.ts and metadata-bitstream.model.ts in the same fork directory are autocomplete/C1-adjacent and are owned by the BE/core-models port, not this item.)
- **v9 adaptation:** HEAD metadata.service.ts is v9-refactored (isVirtual/virtualValue added) — reconcile, do NOT overwrite. Add the six setDataset* private methods (each `this.addMetaTag('dataset_<x>', this.getMetaTagValue('<dc.field>'))`) and add the six calls into HEAD's equivalent of listMetadataTags. addMetaTag/getMetaTagValue signatures are unchanged on v9; Angular Meta service API unchanged. Verify the dc field names still resolve on v9 metadata (dc.subject, dc.rights.uri, dc.identifier.uri, dc.relation.isreferencedby, dc.contributor.author).
- **Verification:** npm test -- --include='**/metadata.service.spec.ts'; manual: load an item page and run `document.querySelectorAll('meta[name^="dataset_"]').length` in console — expect 6.

#### FE-I18N-CS-1 — Merge the 598 missing fork-added Czech (cs.json5) keys  `[medium/M]`

- **Gap/impact:** HEAD src/assets/i18n/cs.json5 is missing CLARIN Czech translations; verified counts (this pass): fork-added cs keys (dtq-dev minus dspace-7.6.3) = 679, of which 598 are absent on HEAD (matches audit 598/679). Czech users see raw i18n keys for CLARIN features.
- **Reuse source:** origin/dtq-dev:src/assets/i18n/cs.json5 — the 598 fork-added keys absent on HEAD. Compute exactly: keys(dtq-dev) ∩ ¬keys(dspace-7.6.3) ∩ ¬keys(HEAD).
- **v9 adaptation:** CORRECTED SCOPE: port ONLY the 598 fork-added missing keys, NOT the naive 627 (all dtq keys missing from HEAD). The 29-key difference (627−598) is v9-renamed/removed VANILLA drift and MUST be EXCLUDED to avoid reintroducing dead keys: cookies.consent.app.disable-all.{description,title}, cookies.consent.content-modal.{service,services}, cookies.consent.content-notice.{description.no-privacy,title}, grant-request-copy.intro, menu.section.icon.{processes,registries,statistics_task,workflow}, and relationships.is*Of (isAuthorOf[.OrgUnit|.Person], isContributorOf[.OrgUnit|.Person], isFundingAgencyOf.OrgUnit, isIssueOf, isJournalIssueOf, isJournalOf, isOrgUnitOf, isPersonOf, isProjectOf, isPublicationOf, isPublicationOfJournalIssue, isSingleJournalOf, isSingleVolumeOf, isVolumeOf). Keep cs.json5 alphabetical/section ordering consistent with HEAD.
- **Verification:** Recompute deltas: extract keys from cs.json5 on HEAD/dtq/7.6.3, assert comm -23 (dtq−7.6.3) HEAD == empty and the 29 exclusions absent; then `npm run build:prod` and spot-check a CLARIN page in Czech renders translated strings, not raw keys.

#### FE-COMPLEX-PARSER-SPEC-1 — Port complex-field-parser.spec.ts (form-glue test coverage)  `[low/S]`

- **Gap/impact:** The complex/autocomplete field .ts components and the complex-field-parser.ts ARE present and wired on HEAD, but the fork's complex-field-parser.spec.ts exists on origin/dtq-dev and is ABSENT on HEAD (`git ls-tree` confirms). The complex-field parser therefore ships with no regression coverage, so a future refactor can silently break complex-field submission rendering.
- **Reuse source:** origin/dtq-dev:src/app/shared/form/builder/parsers/complex-field-parser.spec.ts (test-only; the parser under test, complex-field-parser.ts, is already on HEAD).
- **v9 adaptation:** Test-only port. Reconcile import paths and TestBed setup to v9 (standalone providers, jakarta N/A). If the parser's constructor signature drifted on v9, adjust the spec's instantiation accordingly; do not change parser source.
- **Verification:** npm test -- --include='**/complex-field-parser.spec.ts'

#### FE-TYPEBIND-1 — Restore CLARIN multi-field type-bind (Map storage) + late-load re-evaluation (96e594a5be)  `[major/L]`

- **Gap/impact:** The v9 migration reverted form-builder.service.ts to VANILLA single-model type-bind: HEAD has `private typeBindModel: DynamicFormControlModel` (single, line 64), `private typeField: string` (79), `getTypeBindModel()` no-arg (107), `setTypeBindFieldFromConfig()` no-arg (517). The CLARIN fork uses Map storage (`typeBindModel: Map<string,DynamicFormControlModel>`, `typeFields: Map<string,string>`, TYPE_BIND_DEFAULT_KEY, `setTypeBindFieldFromConfig(map, metadataField)` with `=>` delimiter parsing, `getTypeBindModel(typeBindField)`) so a form can type-bind against fields other than dc.type. On top of that the late-load fix commit 96e594a5be (typeBindModelUpdates Subject + getTypeBindModelUpdates() + evaluateRelations + attachRelatedModels + MATCH_VISIBLE fallback) is entirely unported — it prevents duplicate language fields after hard refresh and re-evaluates visibility when a bind model registers late. HEAD's ds-dynamic-type-bind-relation.service.ts calls getTypeBindModel() no-arg at lines 76 and 105 (matching the reverted vanilla service). Both layers are missing.
- **Reuse source:** origin/dtq-dev:src/app/shared/form/builder/form-builder.service.ts (TYPE_BIND_DEFAULT_KEY const line 58; typeBindModel Map line 66; typeBindModelUpdates Subject line 71; typeFields Map line 86; ctor init lines 97-101; getTypeBindModel(typeBingField) 125; setTypeBindModel emitting typeBindModelUpdates.next 142-144; getTypeBindModelUpdates():Observable<string> 147; setTypeBindFieldFromConfig(map, metadataField) with '=>' parsing 576-612); origin/dtq-dev:src/app/shared/form/builder/ds-dynamic-form-ui/ds-dynamic-type-bind-relation.service.ts (getTypeBindModel(rel?.id) line 70, getTypeBindModel(condition?.id) 99, MATCH_VISIBLE fallback 101, evaluateRelations 224, attachRelatedModels + getTypeBindModelUpdates().subscribe path 188-217); origin/dtq-dev:src/app/shared/mocks/form-builder-service.mock.ts (getTypeBindModelUpdates mock); the base CLARIN Map customization predates 96e594a5be — commit 96e594a5be adds only the late-load delta (form-builder.service.ts +12, ds-dynamic-type-bind-relation.service.ts +86/-30, mock +3, spec +168).
- **v9 adaptation:** RECONCILE, do NOT blind-overwrite (HEAD form-builder.service.ts carries v9 refactors). (a) Replace single typeBindModel/typeField with the Map fields + TYPE_BIND_DEFAULT_KEY + typeBindModelUpdates Subject; add `import { Observable, Subject } from 'rxjs'`. (b) getTypeBindModel(typeBindField) resolves via typeFields map with TYPE_BIND_DEFAULT_KEY fallback; setTypeBindModel(model) does typeBindModel.set(model.id,model) then typeBindModelUpdates.next(model.id); add getTypeBindModelUpdates(). (c) In ds-dynamic-type-bind-relation.service.ts re-point the two getTypeBindModel() no-arg calls (HEAD lines 76,105) to pass rel?.id / condition?.id, switch startWith->the evaluateRelations + attachRelatedModels + getTypeBindModelUpdates().subscribe pattern, add MATCH_VISIBLE-missing-model fallback. Keep v9 ds-dynamic-form-constants imports. (d) Update form-builder-service.mock.ts with getTypeBindModelUpdates.
- **Depends on:** BE-FORMS-SUBMISSION-XML
- **Verification:** npm test -- --include='**/ds-dynamic-type-bind-relation.service.spec.ts' --include='**/form-builder.service.spec.ts' (spec gate, runs without BE); runtime checks deferred until BE submission-forms.xml (EN) defines a non-dc.type bind — then exercise the form and hard-refresh an edit page.

#### FE-LICENSE-SELECTOR-1 — OPEN license-selector popup: provision globals, bundle scripts, rewrite onLicenseSelected  `[medium/M]`

- **Gap/impact:** The clarin-license-resource section-license.component IS ported; its clickLicense() does document.getElementById('license-text').click() (line 356) and its template has the #license-text/#secret-selected-license-from-license-selector/#secret-change-button hooks. But the #license-text anchor is created by src/license-selector-creation.js which calls the jQuery plugin $.fn.licenseSelector from src/license-selector.js — both files are absent on HEAD and angular.json build scripts[] is [] (HEAD angular.json:72). Result: clickLicense() dereferences null; the popup never opens.
- **Reuse source:** origin/dtq-dev:src/license-selector.js (44,617 bytes, webpack IIFE), origin/dtq-dev:src/license-selector-creation.js, origin/dtq-dev:angular.json (build target scripts[] lines 66-68). Component helpers already on HEAD: section-license.component.ts getLicenseIdByName (487), getLicenseNameById (473), licenses4Selector list, changeLicenseNameFromRef() driven by #secret-change-button.
- **v9 adaptation:** CORRECTED — three distinct steps, this is NOT a verbatim two-file copy: (1) GLOBALS: license-selector.js is a webpack IIFE (NOT UMD, does NOT vendor jQuery) whose trailing modules are `function(e,t){e.exports=jQuery}` and `e.exports=_` — it externalizes global jQuery AND global lodash. HEAD src/index.html already loads jQuery 2.1.4 via CDN <script> (for DiscoJuice) so `$`/jQuery is present, but global `_` (lodash) is NOT (HEAD ships lodash-es, no global). Add `lodash` to package.json and add `node_modules/lodash/lodash.min.js` to angular.json build scripts[] BEFORE `src/license-selector.js`; final order: lodash.min.js -> src/license-selector.js -> src/license-selector-creation.js (jQuery is already loaded earlier in <head>). (2) BOOTSTRAP DECISION — explicit NO: do NOT add node_modules/bootstrap/dist/js/bootstrap.bundle.min.js (dtq-dev used BS4 ^4.6.1; HEAD pins bootstrap ^5.3 and uses ng-bootstrap). The widget renders its own overlay via its internal modal.show() (no Bootstrap-JS dependency), so injecting the BS5 jQuery-era bundle globally risks double-init/data-attribute conflicts. Criterion: scripts[] must NOT contain any bootstrap*.js. (3) REWRITE onLicenseSelected: the dtq-dev creation.js callback iterates dead XMLUI DOM (`$('#aspect_submission_StepTransformer_list_license-list li a')`) and toggles #aspect_submission_StepTransformer_item_license-not-supported-message — neither exists in Angular, and its license map is hardcoded legacy lindat licenses (pcedt2/cnc/hamledt/...). Rewrite onLicenseSelected to resolve the chosen widget license (by name/url) against the component's BE-driven option list, set document.getElementById('secret-selected-license-from-license-selector').value to the matching numeric ClarinLicense id (or '' when unmatched), then document.getElementById('secret-change-button').click() — which fires the component's changeLicenseNameFromRef()/getLicenseNameFromRef() path. EN key license-open-selector present (en.json5:7723).
- **Verification:** npm run build:prod; open a submission with the clarin-license section, click OPEN License Selector, confirm overlay opens, pick a license present in the BE clarin license registry, confirm it flows into the dropdown and console is clean. Note: unmatched (legacy-only) widget licenses will fall to the not-supported branch — acceptable, but see risk on registry name alignment.

#### FE-REFRESH-UPLOAD-1 — Refresh file listings/preview after bitstream upload (3a219e4613)  `[medium/M]`

- **Gap/impact:** Commit 3a219e4613 is unported (9 files, 214 insertions). After uploading/removing a bitstream and navigating via breadcrumb, the item file list, CLARIN files section, and preview do not refresh because bundle bitstream hrefs and the metadatabitstreams search-by-handle cache are never marked stale. The commit switches removeByHrefSubstring->setStaleByHrefSubstring and invalidates '/api/core/metadatabitstreams/search/byHandle?handle=...&fileGrpType=ORIGINAL' (both encoded and raw handle variants).
- **Reuse source:** origin/dtq-dev commit 3a219e4613 — 9 files: src/app/item-page/edit-item-page/edit-bitstream-page/edit-bitstream-page.component.ts, src/app/core/breadcrumbs/bitstream-breadcrumbs.service.ts, src/app/item-page/bitstreams/upload/upload-bitstream.component.{ts,spec.ts}, src/app/item-page/.../clarin-files-section.component.ts, src/app/item-page/edit-item-page/item-bitstreams/item-bitstreams.component.ts, src/app/item-page/item.resolver.ts, src/app/item-page/.../preview-section/preview-section.component.{ts,spec.ts}.
- **v9 adaptation:** Reconcile onto v9 (standalone). Replace removeByHrefSubstring with requestService.setStaleByHrefSubstring (v9 RequestService API) for bundle bitstreams href, item self href, item bundles href, and the two byHandle URL variants (encoded handle + raw handle, fileGrpType=ORIGINAL). Guard combineLatest against undefined bundle (the commit's follow-up fix). Keep v9 resolver signatures.
- **Depends on:** BE-C1-METADATABITSTREAM-BYHANDLE
- **Verification:** npm test -- --include='**/upload-bitstream.component.spec.ts' --include='**/preview-section.component.spec.ts' (spec gate, no BE needed); runtime refresh check deferred until C1 (MetadataBitstreamRestRepository byHandle) is live — then upload a file and confirm the list/preview update in place.

**Domain risks:** FE-LICENSE-SELECTOR-1 data alignment: the license-selector widget ships hardcoded legacy lindat licenses whose names/urls may not match the BE-driven ClarinLicense registry (licenses4Selector). After the onLicenseSelected rewrite, licenses with no registry match fall to the not-supported branch. Full coverage depends on the BE clarin license registry (audit M12) being populated with matching license names. · FE-LICENSE-SELECTOR-1 global collision: adding node_modules/lodash/lodash.min.js exposes a global `_` alongside the app's lodash-es usage; verify no runtime conflict. Also license-selector.js requires global jQuery, currently supplied only by a code.jquery.com CDN <script> in index.html — an external-network dependency that a CSP-hardened/offline deploy may block, breaking both DiscoJuice and the license selector. Consider vendoring jquery locally on scripts[] instead of CDN. · FE-TYPEBIND-1 is a reconciliation, not an overwrite: HEAD form-builder.service.ts carries v9 refactors, so re-applying the CLARIN Map storage risks regressing vanilla single-bind (dc.type) forms. The full form-builder + type-bind spec suites are the mandatory gate before merge. · Two runtime acceptance checks (FE-TYPEBIND-1 non-dc.type show/hide + no-duplicate-language; FE-REFRESH-UPLOAD-1 in-place refresh) cannot be proven in FE-only CI — they are blocked on BE-FORMS (submission-forms.xml EN) and BE C1 (metadatabitstreams byHandle) respectively. Their spec gates are decoupled and can land/verify independently on PR #1316. · FE-METATAGS-1: the HEAD-vs-dtq metadata.service.ts diff is large (v9 refactor churn); scope must stay to the six dataset_* tags (and their spec), not a wholesale file overwrite, to avoid clobbering v9's isVirtual/virtualValue and citation logic.

**Open questions:** Cross-domain FE gaps confirmed NOT owned by this domain (must be owned elsewhere on PR #1316, else they fall through the cracks): M4 FilterType.isoLanguage facet enum + renderer (search/facet FE domain); M6 item-edit license tab route+guard+CanManageLicense FeatureID — I verified FeatureID.CanManageLicense is ABSENT from HEAD feature-id.ts, so the ported ItemLicenseMapperComponent is unreachable (item-edit FE domain); M1 license-admin /licenses/manage-table NG0201 NgbActiveModal provider (admin-GUI FE domain); M2 handle-table Handle model missing from provide-core models[] (core-models FE domain). Confirm each has an owning work item. · BE-FORMS ownership: FE-TYPEBIND-1 runtime and the complex/autocomplete/admin-only/field-ACL renderers (all present and wired on HEAD) render NOTHING until BE submission-forms.xml (EN) + item-submission.xml define the complex/type-bind/autocomplete/clarin-license fields. submission-forms_cs.xml is ported but the EN form is byte-vanilla — who owns re-applying the CLARIN EN form config on the BE? · BE-C1 ownership: FE-REFRESH-UPLOAD-1 and metadata-bitstream-data.service (searchByHandle) both target /api/core/metadatabitstreams/search/byHandle, which currently 404s (audit C1). Confirm the BE MetadataBitstreamRestRepository byHandle port is scheduled — several FE file-list/preview features are inert without it. · Czech i18n value-drift: beyond the 598 missing keys, keys present on BOTH HEAD and dtq-dev may hold stale English placeholder values on HEAD (the fork translated them). A value-level reconciliation of shared cs keys is out of FE-I18N-CS-1's strict add-missing-keys scope — decide whether translation-value refresh is in scope for this remediation. · MetadataValueRestRepository (/api/core/metadatavalues) 404 (audit S4): FE autocomplete's data service targets it; this is a BE gap that gates the FE autocomplete renderer — confirm BE ownership so the FE renderer is not falsely marked done. · jQuery delivery policy: HEAD index.html loads jQuery from code.jquery.com. If the production/CSP posture forbids external script hosts, both DiscoJuice (env-gated) and the license selector need jquery vendored into scripts[] instead — is a local jquery bundle acceptable for PR #1316?

### DevOps — dtq-dev CI/CD workflows, Docker images, Matomo deploy infra, and full-stack local build/deploy (owns audit M11)

The v9 branches reset ALL dtq-dev CI/Docker customization to vanilla DSpace-9. Verified at HEAD: BE .github has 8 workflows (all byte-vanilla) and is MISSING 5 dtq-dev files (tag-release, trigger-builds, new_issue_assign, PM-label-review-process, migrate-docker) + copilot-instructions.md; FE .github has 7 vanilla workflows and is MISSING 10 (create_bitstreams, deploy, erase_db, import-weekly, new_issue_assign, new_issue_label, playwright-tests, tag-release, trigger-builds, trigger-ui-tests) + 2 composite actions (erase-db, import-db) + copilot-instructions.md. The present docker.yml files are guarded `github.repository == 'dspace/dspace(-angular)'` so they NEVER run on the dataquest fork; dtq-dev guards on lowercase `dataquest-dev/dspace` (GitHub == is case-insensitive, so it matches the real dataquest-dev/DSpace repo — do NOT capitalize). Docker: the BE/FE Dockerfiles are correctly upgraded to v9 (temurin standalone runtime; FE node:22-alpine) but the v9 BE runtime dropped the dtq-dev non-root uid-1100 user (runs as root now); the entire BE scripts/ tree (29 files incl. the CI-critical sourceversion.py) and FE build-scripts/ tree (16 files) are absent. Matomo is PARTLY present at HEAD: the vanilla-v9 org.dspace.matomo.* event subsystem + CLARIN MatomoReportSubscription REST/service/DAO/SQL + matomo.cfg (enabled=false, wired via dspace.cfg include) + docker-compose-matomo.yml all exist, but the dtq-dev CLARIN download/OAI trackers (app/statistics/clarin/ClarinMatomo*) and the scripts/docker/matomo dev-compose are missing — this domain owns only the dev-compose infra; the tracker/launcher-wiring gap is pushed to openQuestions for the BE domain. Reuse strategy: port each dtq-dev CI/Docker/script artifact back onto the two target branches verbatim, then apply a small v9-mechanical adaptation (JDK17, dspace-9_x tags, temurin runtime, dataquest image org, reconciled reusable-build inputs). Split into (a) RUNNABLE-locally items (local runbook, assetstore seed, Dockerfile user delta, scripts, matomo compose) that must actually work and (b) DATAQUEST-INFRA-GATED CI items (self-hosted dspace-dep-1 runner, /opt/dspace-envs, dev-5, private repos dspace-import-clarin/dspace-ui-tests) that are ported for mandate compliance but proven only via actionlint+grep since they cannot execute from the ufal fork. Sequencing: land DEV-RUN-1/DEV-RUN-2 FIRST because every other domain's acceptance test depends on rebuilding the BE image from PR HEAD and serving the FE; then Dockerfile+scripts+matomo; then CI (BE-1 reconciles reusable-build before BE-2; FE-1's cross-repo ref depends on BE-1, and FE-2 supplies the deploy.yml FE-1 references). All work is plain files committed to BE PR #1339 / FE PR #1316 branches, satisfying the push restriction.

#### DEV-RUN-1 — Author the canonical full-stack local build+deploy RUNBOOK (BE rebuild from PR HEAD + FE build:prod+serve) that every other domain's acceptance tests depend on  `[major/M]`

- **Gap/impact:** There is no documented, reproducible way to rebuild the BE image from PR HEAD. The live BE container image dspace/dspace:dspace-9_x-test was built 2026-07-07 (predating fixes that will land on PR #1339), and the FE at :14000 is served by an ad-hoc HOST node process (curl :14000 = 200, but there is NO FE container in the clarinv9 docker project and no scripted way to rebuild it from PR #1316). The base image dspace/dspace-dependencies:dspace-9_x is NOT cached locally (docker images shows only dspace-solr/postgres tags), so a naive `docker compose build` would fail on FROM resolution. Acceptance tests for C1..M13 cannot be run because nobody can deterministically get fixed code onto the live stack; the progress file itself admits 'Docker: bring up full CLARIN v9 stack locally (not yet done)'.
- **Reuse source:** Existing at HEAD: DSpace/docker-compose.yml + DSpace/docker-compose.clarinv9.yml (working port-override) + DSpace/docker-compose-cli.yml (create-administrator) + DSpace/Dockerfile.test (in-image mvn build). FE serve chain from dspace-angular/package.json scripts (build:prod -> build:ssr -> serve:ssr = node dist/server/main). Port the local-run recipe skeleton from origin/dtq-dev(FE):build-scripts/run/README.md + build-scripts/run/start.backend.bat + build-scripts/run/start.frontend.local.bat (HOST-build variant, not the remote-image start.sh).
- **v9 adaptation:** BE: precondition `docker pull dspace/dspace-dependencies:dspace-9_x` (NOT cached locally); the live image tag ends in -test so rebuild uses Dockerfile.test via `docker compose -p clarinv9 -f docker-compose.yml -f docker-compose.clarinv9.yml build dspace` then `... up -d --no-deps dspace`. The container bind-mounts ./dspace/config, so config-only fixes (M9/M12/M13) apply on `up -d --force-recreate` WITHOUT a rebuild — document both paths. FE: node 22, `npm ci && npm run build:prod` (NODE_ENV=production is MANDATORY per memory note fe-prod-build-i18n-gotcha, else all i18n renders as raw keys), then serve on :14000. Runtime config is config/config.yml (built from config/config.example.yml) — there is NO config.prod.yml at HEAD; set rest.host/rest.port to localhost:18080 /server and ui.port 14000 via config.yml or DSPACE_REST_*/DSPACE_UI_* env vars.
- **Verification:** docker pull the dependencies base; run the runbook BE+FE blocks; assert dspace version ~ ^9., BE /api==200, FE :14000==200 with the raw-key-absent + translated-phrase-present greps.

#### DEV-RUN-2 — Seed a real assetstore so bitstream-content / preview / license-gate acceptance tests (C1, C2) can be exercised locally  `[major/M]`

- **Gap/impact:** clarinv9-dspace /dspace/assetstore currently contains 1 file (`docker exec clarinv9-dspace find /dspace/assetstore -type f | wc -l` == 1); real production bitstream /content fetches fail. C1 (item preview) and C2 (license download-gate) acceptance criteria require actual file bytes behind bitstreams. Currently audit-deferred ('assetstore not imported, file content 500 locally') but it BLOCKS objective verification of two CRITICAL items.
- **Reuse source:** origin/dtq-dev(FE):build-scripts/run/assetstore/77/89/37/77893754617268908529226218097860272513 (a committed 1748-byte seed bitstream) and the copy mechanism in origin/dtq-dev(FE):build-scripts/run/start.sh (final block: `docker cp assetstore dspace:/dspace/ && docker exec -u root ... chown -R dspace:dspace /dspace/assetstore`), plus the same docker-cp+chown pattern in origin/dtq-dev(FE):.github/actions/import-db/action.yml.
- **v9 adaptation:** Two-tier, no code change. Tier 1 (no env dep): deposit/seed a controlled test item via CLI or REST submission and confirm its uploaded bitstream /content returns 200 through the v9 REST path. Tier 2 (env-gated): if the dev-5 production assetstore tarball is present on the host, `docker cp <assetstore> clarinv9-dspace:/dspace/ && docker exec -u root clarinv9-dspace chown -R dspace:dspace /dspace/assetstore` then `docker exec clarinv9-dspace /dspace/bin/dspace checker -v -l`.
- **Depends on:** DEV-RUN-1
- **Verification:** Deposit a test bitstream, fetch /content -> 200; assert file count > 1; if dev-5 tarball present, docker cp + chown + re-fetch /content on a known production handle.

#### DEV-DOCKER-1 — Restore the dtq-dev non-root uid-1100 runtime user on the v9 BE Dockerfile (security hardening delta) and align the base-image org with CI  `[medium/M]`

- **Gap/impact:** The v9 vanilla BE runtime stage is FROM docker.io/eclipse-temurin (HEAD Dockerfile line 53) and runs as ROOT — the dtq-dev image ran the server as a non-root user (`RUN useradd -u 1100 -m -s /bin/bash dspace` + `USER dspace`, dtq-dev Dockerfile lines 86/93). This is a silent security regression versus the fork baseline the mandate says to preserve. The base FROM is also dspace/dspace-dependencies (upstream) while the fork publishes/consumes dataquest/dspace-dependencies via CI (DEV-CI-BE-1).
- **Reuse source:** origin/dtq-dev:Dockerfile lines 86 (`RUN useradd -u 1100 -m -s /bin/bash dspace \`) and 93 (`USER dspace`), and the `FROM ${DOCKER_REGISTRY}/dataquest/dspace-dependencies:${DSPACE_VERSION}` build-stage base (dtq-dev line 16).
- **v9 adaptation:** The dtq-dev useradd lives in a tomcat:9 runtime stage that no longer exists in v9 (v9 is an eclipse-temurin standalone boot jar). Re-apply the delta to the v9 FINAL stage: add `RUN useradd -u 1100 -m -s /bin/bash dspace && chown -R dspace:dspace /dspace` before the ENTRYPOINT, then `USER dspace`. Keep the local-pullable base FROM as dspace/dspace-dependencies for reproducible local builds (the dataquest org is applied at publish time by the CI image_name in DEV-CI-BE-1, not by the source Dockerfile). Verify the added USER does not break the bind-mounted /dspace/config and /dspace/assetstore (chown or run docker with matching uid) — see risks.
- **Depends on:** DEV-RUN-1
- **Verification:** grep the Dockerfile for useradd/USER; docker build to exit 0; rebuild+up and assert non-root uid + BE /api still 200 with bitstream /content still 200.

#### DEV-SCRIPTS-BE-1 — Port the BE scripts/ tree (fast-build, index-scripts, pre-commit, restart_debug, envs, and the CI-critical sourceversion.py) — excluding matomo (DEV-MATOMO-1)  `[medium/S]`

- **Gap/impact:** The entire BE scripts/ tree is absent at HEAD (`git ls-tree -r HEAD -- scripts/` is empty), 29 files in dtq-dev. Critically, scripts/sourceversion.py is REQUIRED by the CI reusable-build (DEV-CI-BE-1 runs `python scripts/sourceversion.py ... > dspace/config/VERSION_D.txt`); without it the image-build workflow errors. The fast-build/index/pre-commit/restart_debug tooling is documented dtq-dev developer heritage the mandate requires preserving.
- **Reuse source:** origin/dtq-dev:scripts/** = sourceversion.py, __tests.yaml, log4j2.solr.xml, build.dspace.bat, run.build.bat, delete.dspace.parent.bat, run.delete.dspace.parent.bat, fast-build/{cfg-update,config-update,crosswalks-update,dspace-api-package-update,oai-pmh-package-update,update-solr-configsets}.bat + fast-build/tomcat/{start,stop}.bat, index-scripts/{autoindexf,indexhandle}.sh, pre-commit/checkstyle.py, restart_debug/{custom_run,redebug,undebug}.sh, envs/{__basic.example.bat,__dspace.parent.basic.bat,__dspace.parent.basic.example.bat}. EXCLUDE scripts/docker/matomo/* (owned by DEV-MATOMO-1).
- **v9 adaptation:** sourceversion.py, index-scripts/*.sh, pre-commit/checkstyle.py are layout-agnostic — port verbatim (no code change). The fast-build/*.bat scripts assume the 7.x tomcat webapp layout (fast-build/tomcat/) and are non-functional against the v9 standalone boot jar — port for heritage but prepend a header comment marking them 7.x-era/superseded by DEV-RUN-1 for v9.
- **Verification:** git ls-tree for the four load-bearing paths; run sourceversion.py to exit 0 with output; count non-matomo scripts >= 24.

#### DEV-SCRIPTS-FE-1 — Port the FE build-scripts/ tree (run/import helper scripts + README) — excluding the assetstore seed (DEV-RUN-2)  `[low/S]`

- **Gap/impact:** The entire FE build-scripts/ tree is absent at HEAD (`git ls-tree -r HEAD -- build-scripts/` is empty), 16 files in dtq-dev. The overview and DEV-RUN-1 depend on the run/ recipe skeleton; harvest/import helpers are dtq-dev heritage.
- **Reuse source:** origin/dtq-dev(FE):build-scripts/** = import/{harvest.sh,harvest.bat,assets/test_community_collection.xml}, run/{README.md,start.bat,start.backend.bat,start.frontend.bat,start.frontend.local.bat,start.sh,stop.bat,reindex.sh,check.logs.bat,.gitignore,envs/.default,envs/.local}. EXCLUDE run/assetstore/** (owned by DEV-RUN-2).
- **v9 adaptation:** start.sh/start.*.bat pull remote dataquest images and source dev-5 envs — port verbatim, prepend a header comment that the remote-image variants are env-gated and that DEV-RUN-1 is the canonical local path for v9.
- **Verification:** git ls-tree for the three key paths and count of non-assetstore build-scripts >= 14.

#### DEV-MATOMO-1 — Port the Matomo dev-deploy compose infra (scripts/docker/matomo/*) and regression-guard the matomo config wiring  `[low/S]`

- **Gap/impact:** The dtq-dev matomo dev-compose helper (scripts/docker/matomo/*) is absent at HEAD (scripts/ tree is empty). NOTE: matomo is otherwise wired at HEAD — matomo.cfg (matomo.enabled=false) is present and included by dspace.cfg (line 1750 `include = ${module_dir}/matomo.cfg`), spring/rest/matomo.xml + docker-compose-matomo.yml + the vanilla-v9 org.dspace.matomo.* event subsystem + the CLARIN MatomoReportSubscription REST/service/DAO/SQL all exist — so this DevOps item owns ONLY the missing dev-compose that stands up a local Matomo+DB for tracking tests. The CLARIN download/OAI trackers and report-subscription launcher/REST wiring are a separate application-layer gap (see openQuestions).
- **Reuse source:** origin/dtq-dev:scripts/docker/matomo/{matomo-w-db.yml,startContainers.sh,startContainers.bat,deleteContainers.sh,deleteContainers.bat}. The matomo_report email template is already present at HEAD (dspace/config/emails/matomo_report).
- **v9 adaptation:** None for the compose (external matomo image, not a DSpace image). Place under scripts/docker/matomo/. matomo.cfg already defaults matomo.tracker.url=http://localhost:8081, which matches the compose service, so no config edit is required to point a local build at the compose Matomo.
- **Verification:** git ls-tree count == 5; docker compose config exits 0 on matomo-w-db.yml; grep dspace.cfg for the matomo include.

#### DEV-CI-BE-1 — Restore BE image-build CI so the fork publishes deployable images: docker.yml (dataquest org + lowercase fork guard) + reconcile reusable-docker-build.yml inputs + sourceversion.py  `[major/M]`

- **Gap/impact:** HEAD BE docker.yml is byte-vanilla and hard-guarded `if: github.repository == 'dspace/dspace'` (7 jobs, lines 27..138), so on dataquest-dev/DSpace every job is skipped and the fork builds/pushes NOTHING (mandate 'preserve dtq-dev CI/Docker' violated). Additionally, HEAD reusable-docker-build.yml (vanilla v9) does NOT define the `run_python_version_script`/`python_version_script_dest` inputs that the dtq-dev dspace job passes (dtq-dev docker.yml lines 49-50) — a verbatim port of docker.yml would fail the workflow_call with an unknown-input error until those input DEFINITIONS + the sourceversion step are ported into reusable-build.
- **Reuse source:** origin/dtq-dev:.github/workflows/docker.yml (jobs image_name dataquest/dspace-dependencies|dspace|dspace-cli|dspace-solr|dspace-postgres-pgcrypto; guard lowercase `github.repository == 'dataquest-dev/dspace'` at lines 25/41/64/84/102/124/141; dspace job passes run_python_version_script: true + python_version_script_dest: dspace/config/VERSION_D.txt) + origin/dtq-dev:.github/workflows/reusable-docker-build.yml (DEFINES those inputs at lines 11 and 15, uses them at line 111-112 `run: python scripts/sourceversion.py ... > <dest>`) + origin/dtq-dev:scripts/sourceversion.py (ported by DEV-SCRIPTS-BE-1).
- **v9 adaptation:** Keep vanilla-9 build mechanics (JDK17, DSPACE_VERSION=dspace-9_x, temurin base, solr Dockerfile at dspace/src/main/docker/dspace-solr). Re-apply dtq-dev deltas: image_name -> dataquest/*; guard -> verbatim LOWERCASE `github.repository == 'dataquest-dev/dspace'` (GitHub == is case-insensitive and this empirically matches the real dataquest-dev/DSpace fork — do NOT capitalize 'DSpace'); PORT the run_python_version_script + python_version_script_dest input definitions AND the sourceversion step into HEAD's reusable-docker-build.yml; for the postgres image, map the dtq-dev pgcrypto job onto the v9-renamed image dataquest/dspace-postgres-loadsql with dockerfile_context ./dspace/src/main/docker/dspace-postgres-loadsql/ (v9 replaced pgcrypto with loadsql — do NOT resurrect the pgcrypto Dockerfile).
- **Depends on:** DEV-SCRIPTS-BE-1
- **Verification:** grep the lowercase guard + dataquest image names + reconciled reusable inputs; actionlint both files to exit 0; run sourceversion.py; docker compose build dspace to exit 0.

#### DEV-CI-BE-2 — Restore BE release + fanout + PM-automation workflows (tag-release, trigger-builds, new_issue_assign, PM-label-review-process) + copilot-instructions; neutralize the obsolete 7.5-era migrate-docker  `[medium/S]`

- **Gap/impact:** 5 dtq-dev BE workflow files and .github/copilot-instructions.md are absent at HEAD. tag-release.yml (retags dataquest images on git tag) and trigger-builds.yml (customer-branch fanout) implement the documented dtq-dev release pipeline the mandate requires preserved. migrate-docker.yml is a 7.5-era migration workflow that triggers on push + pull_request(ALL PRs) + workflow_dispatch with a fork-matching guard — if naively ported it would attempt a broken 7.5 docker build on every PR to the fork.
- **Reuse source:** origin/dtq-dev:.github/workflows/{tag-release.yml,trigger-builds.yml,new_issue_assign.yml,PM-label-review-process.yml,migrate-docker.yml} + origin/dtq-dev:.github/copilot-instructions.md.
- **v9 adaptation:** tag-release.yml: keep IMAGE_BASE_NAME=dataquest/dspace and update the retagged set to the v9 image list (dependencies, dspace, cli, solr, and dspace-postgres-loadsql — NOT pgcrypto). trigger-builds/PM-label-review-process/new_issue_assign reference dataquest secrets (DOCKER_USERNAME, GITHUB_TOKEN) + customer/* branches: port verbatim, keep runs-on ubuntu-latest, document as dataquest-org-gated (non-executable from the ufal fork). migrate-docker.yml: it is OBSOLETE for v9 — RECOMMENDED to NOT port it; if ported for heritage, REPLACE its `on:` block with `on: { workflow_dispatch: {} }` (a header comment alone does NOT stop it firing on push/PR).
- **Depends on:** DEV-CI-BE-1
- **Verification:** git ls-tree the 5 paths; actionlint the 4 active workflows; assert migrate-docker is absent or has no push/pull_request triggers.

#### DEV-CI-FE-1 — Restore FE image-build CI: docker.yml re-adapted to dataquest/dspace-angular + lowercase fork guard + a resolvable cross-repo reusable-build ref  `[major/M]`

- **Gap/impact:** HEAD FE docker.yml is vanilla, guarded `if: github.repository == 'dspace/dspace-angular'` (lines 28/44/67) so it is SKIPPED on the fork, and its image_name is dspace/dspace-angular — the fork publishes no dataquest/dspace-angular image. (Correction to prior draft: the workflow is NOT structurally broken — it references the UPSTREAM cross-repo workflow `uses: DSpace/DSpace/.github/workflows/reusable-docker-build.yml@main` which exists; it is only skipped by the guard.)
- **Reuse source:** origin/dtq-dev:.github/workflows/docker.yml (FE): image_name dataquest/dspace-angular (lines 32/53); guard lowercase `github.repository == 'dataquest-dev/dspace-angular'` (lines 27/48/67); cross-repo build ref `uses: dataquest-dev/DSpace/.github/workflows/reusable-docker-build.yml@dtq-dev` (lines 29/50); and a deploy job `uses: dataquest-dev/dspace-angular/.github/workflows/deploy.yml@dtq-dev` (line 67, supplied by DEV-CI-FE-2).
- **v9 adaptation:** Keep vanilla-9 node22/Dockerfile.dist mechanics. Re-apply dtq-dev deltas: image_name -> dataquest/dspace-angular; guard -> `dataquest-dev/dspace-angular` (lowercase, case-insensitive match); repoint the cross-repo build ref to the v9 BE branch that carries the reconciled reusable-build from DEV-CI-BE-1: `uses: dataquest-dev/DSpace/.github/workflows/reusable-docker-build.yml@ufal/clarin-dspace-upgrade-v9`. FE docker.yml does NOT pass the python_version_script inputs, so upstream `DSpace/DSpace/....@main` also resolves as a fallback if the fork branch ref is unavailable — but prefer the fork ref for org consistency. Ensure deploy.yml exists (DEV-CI-FE-2) so the deploy job ref resolves.
- **Depends on:** DEV-CI-BE-1
- **Verification:** grep the lowercase guard + dataquest image name + a resolvable reusable ref; actionlint docker.yml to exit 0.

#### DEV-CI-FE-2 — Restore the remaining 10 FE dtq-dev workflows + 2 composite actions + copilot-instructions (deploy, import/erase DB, ui-tests, PM automation)  `[medium/M]`

- **Gap/impact:** 10 dtq-dev FE workflows, 2 composite actions, and copilot-instructions.md are absent at HEAD. deploy.yml is referenced by docker.yml's deploy job (DEV-CI-FE-1) so its absence dangles that ref; the import/erase-db + create_bitstreams + playwright/ui-test workflows implement the dtq-dev dev-5 data pipeline and E2E suite the mandate requires preserved.
- **Reuse source:** origin/dtq-dev(FE):.github/workflows/{create_bitstreams.yml,deploy.yml,erase_db.yml,import-weekly.yml,new_issue_assign.yml,new_issue_label.yml,playwright-tests.yml,tag-release.yml,trigger-builds.yml,trigger-ui-tests.yml} + .github/actions/{erase-db,import-db}/action.yml + .github/copilot-instructions.md.
- **v9 adaptation:** import-weekly/erase_db/create_bitstreams + the import-db/erase-db composite actions target the self-hosted dspace-dep-1 runner + private repo dspace-import-clarin + /opt/dspace-envs — port verbatim, document as ENV-GATED (self-hosted runner + dataquest secrets, non-executable from the ufal fork). playwright-tests/trigger-ui-tests reference the private dspace-ui-tests repo — port verbatim, env-gated. deploy.yml is called by docker.yml's deploy job (DEV-CI-FE-1) — must exist for that ref to resolve. tag-release/trigger-builds/new_issue_assign/new_issue_label -> dataquest org, port verbatim.
- **Depends on:** DEV-CI-FE-1
- **Verification:** git ls-tree the 10 workflow + 2 action + copilot paths; actionlint the executable-syntax subset to exit 0.

**Domain risks:** Adding a non-root USER dspace to the v9 BE runtime (DEV-DOCKER-1) can break the bind-mounted /dspace/config and /dspace/assetstore (owned by a different uid on the host) — the container may fail to write logs/assetstore or serve /content. Mitigate with a chown in the Dockerfile and gate the runtime-uid acceptance behind a successful rebuild that still returns BE /api==200 and bitstream /content==200. · Nearly all restored CI workflows are dataquest-org and/or self-hosted-runner gated (dspace-dep-1 runner, /opt/dspace-envs, private repos dspace-import-clarin + dspace-ui-tests, dataquest Docker Hub secrets). They CANNOT execute from PR #1339/#1316 (which target the ufal fork, not dataquest-dev). Local proof is limited to actionlint + grep of guards/image-names; true green requires dataquest infrastructure and is flagged non-executable. · Rebuilding the BE image from PR HEAD (DEV-RUN-1) requires a multi-GB `docker pull dspace/dspace-dependencies:dspace-9_x` (not cached locally) plus an in-image mvn package (~10-20 min). Network flakiness on the dependencies pull is a hard blocker for every downstream domain's acceptance test. · The push restriction (only PR #1339 / #1316) means FE docker.yml's cross-repo ref `@ufal/clarin-dspace-upgrade-v9` only resolves if the BE branch actually carries reusable-docker-build.yml at that ref (true only after DEV-CI-BE-1 lands). If BE-1 slips, FE-1 must keep the upstream `DSpace/DSpace/....@main` fallback or the FE image-build call dangles. · migrate-docker.yml is a live footgun: verbatim-ported it fires a broken 7.5-era docker build on every PR to the fork (its `on:` includes pull_request with no path/branch filter and its guard matches dataquest-dev case-insensitively). It must be omitted or have its push/pull_request triggers stripped, not merely comment-disabled. · Whether the fork's v9 line should publish under the dataquest/* org at all (vs ufal/*) is unconfirmed; the plan ports the dtq-dev dataquest naming verbatim. If the real publish org is ufal/*, all image_name + guard values need a global rename before any CI job would target the correct registry.

**Open questions:** MATOMO APPLICATION LAYER (BE domain, not DevOps): at HEAD the CLARIN download/OAI trackers org.dspace.app.statistics.clarin.{ClarinMatomoBitstreamTracker,ClarinMatomoOAITracker,ClarinMatomoTracker} are ABSENT, while a different vanilla-v9 org.dspace.matomo.* event subsystem (MatomoUsageEventHandler, client/, factory/, model/) IS present. The BE owner must decide whether the vanilla-v9 subsystem supersedes the CLARIN trackers or whether the trackers must be re-ported/adapted for download+OAI tracking. Separately, MatomoReportSubscription REST/service/DAO/SQL are present at HEAD but launcher.xml has NO matomo report command and MatomoReportSubscriptionRestRepositoryIT is absent — the report-subscription feature is likely un-wired. Needs a BE work item; DevOps owns only the dev-compose (DEV-MATOMO-1). · VERSION_D.txt consumer: the dtq-dev CI (DEV-CI-BE-1) writes dspace/config/VERSION_D.txt via sourceversion.py. Confirm whether any BE/FE runtime code or the About/version REST endpoint reads VERSION_D.txt — if so, a consumer/wiring item is needed in the BE or FE build-info domain, else the file is CI-cosmetic. · pgcrypto -> loadsql DB image: v9 renamed dspace-postgres-pgcrypto to dspace-postgres-loadsql and the live clarinv9-dspacedb runs plain postgres:15 (no custom image). Confirm the v9 stack no longer needs the pgcrypto extension image and that the dtq-dev dspace-postgres-pgcrypto-curl variant (curl-in-postgres, used by the import workflows) is genuinely obsolete — DB/infra owner to confirm before the postgres CI job is finalized. · Publish org decision (maintainer): confirm dataquest/* Docker Hub images are still the intended target for the v9 line, or whether ufal/* is the real registry org. This gates the image_name + repository-guard values across DEV-CI-BE-1/BE-2/FE-1/FE-2. · FE assetstore/import pipeline (cross-domain): DEV-RUN-2 tier-2 and the import-db/erase-db composite actions both depend on a dev-5 production assetstore/DB tarball being available on the host. Confirm whether that tarball is provisioned in this environment or whether C1/C2 verification must rely solely on the tier-1 locally-deposited test bitstream.

### Testing & acceptance strategy — the "done" gate

This domain owns HOW we prove the CLARIN v9 upgrade is finished, not the feature fixes. The current "done" evidence is hollow AND actively misleading. Two independent problems: (1) CI runs zero CLARIN tests — 126 fork-added BE test-tree files (55 ITs + 24 unit *Test.java + 11 builders + 5 matchers + 31 support/fixtures/resources) and 64 of 66 fork-added FE karma specs are absent at HEAD, and the dspace-ui-tests Playwright CLARIN gate is a local, untracked toggle. (2) VERIFIED build-provenance divergence: the running clarinv9 container serves code NOT in PR HEAD — live GET /server/api/core/metadatabitstreams/search/byHandle?handle=11234/1-5419 returns 200 with full HAL while MetadataBitstreamRestRepository.java is ABSENT at HEAD d27f85b (present only in dtq-dev). So any live probe today certifies unshipped code. Strategy is reuse-first AND provenance-first: port builders/matchers/fixtures first (shared deps), then criticals-gating ITs, then the rest and the unit tests, adapting to v9 (jakarta, MockMvc, plural HAL bean names, Instant), while a hard provenance gate forces every live functional probe to run only against an image built from PR #1339/#1316 HEAD. The master deliverable (TEST-GATE-0) maps DoD (a)-(f) to objective probes: provenance-verified live curls + BE failsafe ITs + BE surefire unit tests + FE karma + a fully-localized Playwright run (0 conditional gate-skips) + two manual-spec sign-off sheets. Sequencing: TEST-PROVENANCE + TEST-BE-BUILDERS + TEST-PW-2 unblock everything; IT/karma/unit ports run in parallel with feature-domain fixes and become their acceptance oracle; OAI needs genuine new glue (no fork IT exists); manual runbooks and the master gate close last. Push constraint honored: BE tests land in DSpace #1339 test dirs, FE karma in #1316; dspace-ui-tests is a third repo used as a LOCAL oracle only (its localized config is a run artifact, never pushed).

#### TEST-PROVENANCE — Build-provenance gate: deployed stack SHA must equal PR #1339/#1316 HEAD before any live probe counts  `[critical/S]`

- **Gap/impact:** THE core done-gate defect. The running clarinv9 BE serves code absent from PR HEAD: live byHandle returns HTTP 200 with full CLARIN HAL (_embedded.metadatabitstreams, canPreview, fileInfo, checksum) yet git cat-file -e HEAD:dspace-server-webapp/src/main/java/org/dspace/app/rest/repository/MetadataBitstreamRestRepository.java fails (present only in dtq-dev). Every live functional probe therefore certifies features that may NOT be in the PR. Without a provenance assertion, gate (b) is false-confidence.
- **Reuse source:** N/A — new glue. No fork source; a mechanism the audit exposed as missing. Coordinates with M11 Docker/CI domain (image build labeling) in DSpace #1339 and dspace-angular #1316.
- **v9 adaptation:** Implementing agent rebuilds BE+FE images from PR HEAD and redeploys the clarinv9 stack (allowed; only pushes restricted). Stamp each image at build with label org.opencontainers.image.revision=$GIT_SHA (add to the ported dtq-dev Dockerfiles under M11). Provenance check reads the label from the running container (docker inspect) and/or enables management.info.git (spring git-commit-id, already on DSpace classpath) so authenticated /server/actuator/info exposes git.commit.id (actuator/info is currently 401 — so use the docker label as the primary machine-checkable source). No live probe in any other work item is honored unless this check passed in the same run.
- **Verification:** Run probe-provenance.sh: assert it prints the two SHAs, that they equal the two `git rev-parse HEAD` values, and exits 0; deliberately point it at a stale image to confirm it exits non-zero.

#### TEST-BE-BUILDERS — Port CLARIN test builders + matchers + support fixtures and re-apply modified base-builder deltas (shared dependency for all CLARIN ITs)  `[critical/L]`

- **Gap/impact:** Every CLARIN BE IT depends on builders/matchers/fixtures absent at HEAD. Missing: 11 added builders (incl. VersionHistoryBuilder and PreviewContentBuilder), 5 matchers, and support fixtures ProvenanceExpectedMessages.java (required by ProvenanceServiceIT) and MockHarvestedCollectionServiceImpl.java. Separately, modified-vanilla base builders (AbstractBuilder, AbstractBuilderCleanupUtil, BitstreamBuilder, ItemBuilder, EPersonBuilder, WorkspaceItemBuilder) carry unapplied fork deltas. Without all of these no ported IT compiles or cleans up.
- **Reuse source:** ADDED builders (git diff --diff-filter=A dspace-7.6.5 origin/dtq-dev): dspace-api/src/test/java/org/dspace/builder/{ClarinBitstreamBuilder,ClarinLicenseBuilder,ClarinLicenseLabelBuilder,ClarinLicenseResourceMappingBuilder,ClarinLicenseResourceUserAllowanceBuilder,ClarinUserMetadataBuilder,ClarinUserRegistrationBuilder,HandleBuilder,PreviewContentBuilder,VersionHistoryBuilder}.java (VersionHistoryBuilder is a NEW file — absent at dspace-7.6.5 — so it is ONLY a new-builder port, never a modified-base delta). Matchers: dspace-server-webapp/src/test/java/org/dspace/app/rest/matcher/{ClarinLicenseMatcher,ClarinLicenseLabelMatcher,HandleMatcher,ExternalHandleMatcher,MetadataValueMatcher}.java. Fixtures: dspace-server-webapp/src/test/java/org/dspace/app/rest/ProvenanceExpectedMessages.java, dspace-server-webapp/src/test/java/org/dspace/harvest/MockHarvestedCollectionServiceImpl.java. MODIFIED-base deltas ONLY via: git diff dspace-7.6.5 origin/dtq-dev -- dspace-api/src/test/java/org/dspace/builder/{AbstractBuilder,BitstreamBuilder,ItemBuilder,EPersonBuilder,WorkspaceItemBuilder}.java and builder/util/AbstractBuilderCleanupUtil.java (do NOT include VersionHistoryBuilder here).
- **v9 adaptation:** javax->jakarta persistence imports; register every new Clarin*/Handle/PreviewContent/VersionHistory builder in AbstractBuilderCleanupUtil cleanup order (fork delta) or teardown leaks orphan rows; align factory calls to v9 bean names (ClarinLicenseService, ClarinLicenseResourceMappingService, HandleClarinService); matchers must assert v9 HAL PLURAL embeds (metadatabitstreams, clarinlicenses, handles) and Instant/ISO-8601 dates, not legacy Date.
- **Verification:** mvn --no-transfer-progress -pl dspace-server-webapp -am test-compile -Denforcer.skip=true -Dcheckstyle.skip=true (BUILD SUCCESS); grep -c -E 'Clarin|Handle|PreviewContent|VersionHistory' dspace-api/src/test/java/org/dspace/builder/util/AbstractBuilderCleanupUtil.java returns >=10.

#### TEST-BE-IT-CRIT — Port the critical/major-gating CLARIN BE integration tests (C1/C2/C3/M2/M3/M7 + preview + Shibboleth)  `[critical/L]`

- **Gap/impact:** The ITs that would have caught every confirmed critical are absent, so the feature fixes owned by other domains have no regression oracle. Missing: MetadataBitstreamRestRepositoryIT (C1: byHandle 200 + _embedded.metadatabitstreams + canPreview/fileInfo/checksum), AuthorizationRestControllerIT (C2: 401 without agreement, 200 with dtoken, expired-token denied, MissingLicenseAgreementException), HandleRestRepositoryIT (M2), EpicHandleRestControllerIT (M3), ClarinTokenServiceTest/IT (M7), Preview/FilePreview ITs, ClarinShibbolethLoginFilterIT.
- **Reuse source:** origin/dtq-dev dspace-server-webapp/src/test/java/org/dspace/app/rest/{MetadataBitstreamRestRepositoryIT,MetadataBitstreamControllerIT,BitstreamByHandleRestControllerIT,AuthorizationRestControllerIT,HandleRestRepositoryIT,ExternalHandleRestRepositoryIT,EpicHandleRestControllerIT,PreviewContentRestRepositoryIT,PreviewContentServiceImplIT}.java; .../security/{ClarinShibbolethLoginFilterIT,ClarinShibbolethAuthAssing2GroupsIT}.java; dspace-api/.../scripts/filepreview/FilePreviewIT.java, handle/HandleClarinServiceImplIT.java. TEST RESOURCES (all required or IT throws IOException/NPE): dspace-server-webapp/src/test/resources/org/dspace/app/rest/assetstore/{dspace-logo.png.gz,hello.html,logos.7z,logos.tar.gz,logos.tar.xz,logos.tgz,logos.xz} (7 files) AND dspace-api/src/test/resources/org/dspace/scripts/filepreview/{logos.tgz,preview-file-test.zip} (2 files).
- **v9 adaptation:** jakarta imports; AbstractControllerIntegrationTest/AbstractIntegrationTestWithDatabase base already at HEAD; assert HAL PLURAL 'metadatabitstreams'/'handles' embeds (PLURAL_NAME gotcha); enable webui.preview.enabled and clarin preview cfg in test config; port ALL 9 binary test resources above via git checkout of the blobs (7z/xz/tar.xz decode requires commons-compress + XZ-for-Java (org.tukaani:xz) on the v9 test classpath — verify present, add to pom if missing); ClarinTokenService bean lookup by v9 name.
- **Depends on:** TEST-BE-BUILDERS
- **Verification:** mvn --no-transfer-progress -pl dspace-server-webapp -am verify -P-assembly -DskipUnitTests=true -DskipIntegrationTests=false -Denforcer.skip=true -Dcheckstyle.skip=true -Dlicense.skip=true -Dit.test='MetadataBitstreamRestRepositoryIT,AuthorizationRestControllerIT,HandleRestRepositoryIT,EpicHandleRestControllerIT,PreviewContentServiceImplIT,PreviewContentRestRepositoryIT,ClarinShibbolethLoginFilterIT' — expect BUILD SUCCESS, Failures: 0; independently ls the 9 resource paths.

#### TEST-BE-UNIT — Port the 24 fork-added CLARIN BE unit tests and run them under surefire (skipUnitTests=false)  `[major/M]`

- **Gap/impact:** 24 fork-added *Test.java unit tests are unaddressed and the other work items' mvn commands all pass -DskipUnitTests=true, so these never execute. They are the ONLY coverage for several pure-logic CLARIN units: ClarinTokenServiceTest (M7), RegexPasswordValidatorTest, EpicHandleServiceTest+PIDConfigurationTest (M3), CachingOrcidRestConnectorTest (ORCID NPE medium), CuratorReporterTest (curation medium), ShibHeadersTest, ACLTest/DCInputTest (submission ACL L4/S3), LocalMetadataTest, MatomoHelperTest/ClarinMatomoBitstreamTrackerTest/MatomoReportSubscriptionServiceTest, S3DirectDownloadServiceTest (O1 deferred), HandleClarinDAOImplTest/ClarinLicenseResourceUserAllowanceDAOImplTest.
- **Reuse source:** origin/dtq-dev added (git diff --diff-filter=A ... | grep 'Test\.java$'): dspace-api/src/test/java/org/dspace/{app/configuration/service/ConfigFileServiceImplTest, app/util/ACLTest, app/util/DCInputTest, authenticate/ShibHeadersTest, authorize/RegexPasswordValidatorTest, content/BundleClarinTest, content/LocalMetadataTest, content/RelationshipServiceImplVersioningTest, content/dao/clarin/ClarinLicenseResourceUserAllowanceDAOImplTest, content/service/ItemServiceTest, content/service/clarin/ClarinTokenServiceTest, content/service/clarin/MatomoReportSubscriptionServiceTest, curate/CuratorReporterTest, external/CachingOrcidRestConnectorTest, external/OpenAIRERestConnectorTest, handle/EpicHandleServiceTest, handle/PIDConfigurationTest, handle/dao/impl/HandleClarinDAOImplTest, matomo/MatomoHelperTest, service/impl/DspaceObjectClarinImplTest, statistics/ClarinMatomoBitstreamTrackerTest, storage/bitstore/S3DirectDownloadServiceTest}.java; dspace-server-webapp/src/test/java/org/dspace/app/rest/{submit/step/validation/CMDIFileBundleMaintainerTest, utils/UtilsTest}.java.
- **v9 adaptation:** jakarta; Mockito/JUnit already at v9 baseline; several are AbstractUnitTest subclasses (no DB) so no builder dependency; DAO*Test extend AbstractIntegrationTestWithDatabase and may run under failsafe instead — classify each by its base class and route accordingly; S3DirectDownloadServiceTest is env-gated (O1 deferred) — annotate @Ignore("deferred: O1") and list in the DoD deferral table, do not delete.
- **Depends on:** TEST-BE-BUILDERS
- **Verification:** mvn --no-transfer-progress -pl dspace-api,dspace-server-webapp -am test -Denforcer.skip=true -Dcheckstyle.skip=true -Dtest='Clarin*Test,RegexPasswordValidatorTest,EpicHandleServiceTest,CachingOrcidRestConnectorTest,CuratorReporterTest,ACLTest,DCInputTest,ShibHeadersTest,LocalMetadataTest,Matomo*Test' — expect Tests run: >0, Failures: 0, Skipped only the deferred ones.

#### TEST-BE-IT-REST — Port the remaining ~43 CLARIN BE ITs (license REST, user metadata/registration, import controllers, curation, provenance, matomo, submission, refbox, discovery)  `[major/XL]`

- **Gap/impact:** The bulk of CLARIN BE IT coverage is unported, leaving the submission cluster, curation tasks, provenance, matomo, refbox export, and license/handle REST with no regression oracle. Of 55 fork-added ITs, TEST-BE-IT-CRIT covers ~12; this covers the rest.
- **Reuse source:** origin/dtq-dev dspace-server-webapp/src/test/java/org/dspace/app/rest/{ClarinLicense*IT,ClarinUser*IT,Clarin*ImportControllerIT,ClarinRefBoxControllerIT,ClarinDiscoveryRestControllerIT,ClarinDiscoJuiceFeedsControllerIT,ClarinGroupRestControllerIT,MetadataValueRestRepositoryIT,MatomoReportSubscriptionRestRepositoryIT,ConfigFileRestControllerIT,CollectionHarvestSettingsControllerIT,HarvesterMetadataControllerIT,SuggestionRestControllerIT}.java; dspace-api/.../curate/{ItemHandleCheckerIT,ItemMetadataQACheckerIT,RequiredMetadataIT}.java. NOTE: OAIpmhIT.java and ALL dspace-oai/src/test/** files are byte-vanilla (git diff dspace-7.6.5 origin/dtq-dev returns EMPTY) — they are NOT fork CLARIN coverage and porting them is a no-op; OAI-endpoint coverage is handled by TEST-OAI-ORACLE, not here. The only CLARIN OAI/crosswalk test-tree asset is ClarinRefBoxControllerIT (refbox bibtex/cmdi citation export).
- **v9 adaptation:** jakarta; MetadataValueRestRepositoryIT asserts /api/core/metadatavalues returns 200 (live is currently 404 — this IT is the oracle for the S4 fix); ClarinRefBoxControllerIT asserts /api/core/refbox/citations returns bibtex + cmdi payloads (oracle for ref-box export, NOT for OAI harvest); curation ITs require task registration in curate.cfg; annotate @Ignore("deferred: <finding-id>") on any IT whose feature is explicitly deferred (e.g. Matomo PDF e2e) and record it in the DoD deferral list rather than deleting.
- **Depends on:** TEST-BE-BUILDERS, TEST-BE-IT-CRIT
- **Verification:** mvn --no-transfer-progress install -P-assembly -DskipUnitTests=true -DskipIntegrationTests=false -Denforcer.skip=true -Dcheckstyle.skip=true -Dlicense.skip=true; assert failsafe summary lists the ported Clarin ITs with 0 failures (deferred ones show skipped); grep the diff to confirm no dspace-oai test file or OAIpmhIT was touched.

#### TEST-OAI-ORACLE — OAI crosswalk oracle for C5 (WebLicht/VLO harvest) — new IT + provenance-gated live probe (no reusable fork IT exists)  `[major/M]`

- **Gap/impact:** There is NO fork IT that hits the OAI endpoint for CLARIN crosswalks — the only CLARIN OAI test-tree asset is ClarinRefBoxControllerIT (refbox citation, not harvest), and OAIpmhIT + all dspace-oai tests are vanilla/unchanged. So C5 (cmdi/olac/metasharev2/elg + WebLicht/VLO) would have no automated done-signal even after the crosswalk classes are ported. VERIFIED live: /server/oai/request?verb=ListMetadataFormats returns only the 13 vanilla formats (didl,mods,ore,mets,xoai,dim,rioxx,uketd_dc,qdc,oai_dc,rdf,marc,etdms) — none of cmdi/olac/metasharev2/elg.
- **Reuse source:** N/A — new glue (genuinely no fork IT to port; verified by exhaustive git grep of dtq-dev test tree). Model the new IT on the existing vanilla dspace-server-webapp/src/test/java/org/dspace/app/rest/OAIpmhIT.java structure and on ClarinRefBoxControllerIT for CLARIN metadata assertions. Live probe reuses the running OAI endpoint. This is the one place where write-from-scratch is justified — flag as such.
- **v9 adaptation:** New IT lands in DSpace #1339 dspace-server-webapp/src/test; jakarta; asserts (1) verb=ListMetadataFormats response contains metadataPrefix cmdi, olac, metasharev2 and elg; (2) verb=GetRecord&metadataPrefix=cmdi&identifier=<seeded item> returns HTTP 200 with a non-error OAI-PMH body (no <error code='cannotDisseminateFormat'>). IT is @Ignore("deferred: C5") ONLY until the OAI crosswalk layer (SharedSaxonProcessor, SpecialItemService, xoai.xml/oai.cfg wiring) lands in the config/OAI domain, then enabled.
- **Depends on:** TEST-BE-BUILDERS
- **Verification:** After a provenance-clean rebuild: curl the two OAI URLs above and grep for cmdi/olac/metasharev2/elg and absence of cannotDisseminateFormat; mvn verify -Dit.test=<new OAI IT> BUILD SUCCESS.

#### TEST-BE-IT-MODVANILLA — Re-apply fork test-deltas to the modified-vanilla BE test files (M10 coverage)  `[major/L]`

- **Gap/impact:** Fork-modified vanilla test files assert CLARIN behavior on shared/vanilla flows (ItemTest addLanguageNameToMetadata, VersioningTest/VersioningWithRelationshipsIT, GoogleMetadataTest, UtilsTest, WorkspaceItem/Item REST license-patch ops). At HEAD they are byte-vanilla, so the CLARIN deltas in InstallItemServiceImpl/WorkspaceItemRestRepository/ItemConverter (M10) have no coverage and can silently regress.
- **Reuse source:** git diff dspace-7.6.5 origin/dtq-dev -- (filter=M, test tree only): the modified vanilla test files under dspace-api/src/test and dspace-server-webapp/src/test (e.g. content/ItemServiceTest is ADDED not modified — route via TEST-BE-UNIT; genuinely modified ones include app/rest/WorkspaceItemRestRepositoryIT, app/rest/ItemRestRepositoryIT, versioning/VersioningWithRelationshipsIT, and google-metadata/versioning unit tests). Enumerate the exact set with: git diff --name-only --diff-filter=M dspace-7.6.5 origin/dtq-dev -- 'dspace-api/src/test/*' 'dspace-server-webapp/src/test/*'.
- **v9 adaptation:** Apply only the fork hunks (not wholesale overwrite — v9 upstream also changed these files); jakarta; align license-patch REST assertions to v9 JSON-Patch paths; assert InstallItemServiceImpl.addLanguageNameToMetadata writes the ISO language name (M10) via ItemRestRepositoryIT/ItemServiceTest.
- **Depends on:** TEST-BE-BUILDERS
- **Verification:** git diff --name-only --diff-filter=M dspace-7.6.5 origin/dtq-dev -- test tree to enumerate; for each, git diff HEAD origin/dtq-dev -- <file> and confirm only non-CLARIN/v9-API lines differ; mvn verify -Dit.test=WorkspaceItemRestRepositoryIT,ItemRestRepositoryIT BUILD SUCCESS.

#### TEST-FE-KARMA — Port the 64 missing CLARIN FE karma specs into dspace-angular #1316  `[major/XL]`

- **Gap/impact:** FE CLARIN unit coverage is almost entirely absent: 66 fork-added *.spec.ts exist in dtq-dev, and 64 are absent at HEAD (the 2 present are vanilla accessibility specs, not CLARIN). So metadata-bitstream-data.service, license-selector/agreement components, isoLanguage filter renderer (M4), handle-table/license-manage components (M1/M2), share-submission (M5), and static-page/html-content services have no karma oracle.
- **Reuse source:** origin/dtq-dev added specs: git diff --name-only --diff-filter=A dspace-7.6.3 origin/dtq-dev -- '*.spec.ts' (66 files; 64 absent at HEAD). Priority specs mapping to audit findings: src/app/**/metadata-bitstream-data.service.spec.ts (C1), src/app/shared/html-content.service.spec.ts + static-page/static-page.component.spec.ts (C4), the isoLanguage FilterType renderer spec (M4), licenses/manage-table + handle-table component specs (M1/M2), workspaceitem share-button spec (M5).
- **v9 adaptation:** Angular standalone migration: specs must provide standalone imports (mirror the component's imports[]) not NgModule TestBed declarations; NgbActiveModal provider must be supplied in specs for modal components (M1 root cause); HttpClientTestingModule -> provideHttpClientTesting; align RxJS/observable APIs to v9; specs land in dspace-angular #1316 alongside their components.
- **Verification:** cd dspace-angular && npx ng test --watch=false --browsers=ChromeHeadless 2>&1 | tail -40 — expect 'Executed N of N SUCCESS (0 FAILED)'; grep the karma spec list for the four priority specs.

#### TEST-PW-2 — Localize the dspace-ui-tests Playwright config + fixtures against the local clarinv9 stack (LOCAL run artifact, never pushed)  `[major/M]`

- **Gap/impact:** The CLARIN Playwright suite (lindat_specific_tests) is gated behind config.env.lindat_specific_tests, which is an UNTRACKED local addition (present in the working-tree config.json with a 'TEMPORARY audit probe' comment, ABSENT from tracked HEAD). Without a committed-to-workspace localized config (localhost:14000 URLs, seeded handles like 11234/1-5419, lindat toggle ON) the suite either points at prod or skips all CLARIN cases.
- **Reuse source:** dspace-ui-tests (dataquest-dev/dspace-ui-tests master, ALREADY in workspace at /c/workspace/clarin-dspace-v9-upgrade/dspace-ui-tests): tests/pages/** page objects, tests/tests/{homePage,itemPage,searchPage,submissionPage,universalPage}.spec.ts, and config.json / scripts/merge-config.ts. The gate is `if (!config.env.lindat_specific_tests) { test.skip(); }` at each CLARIN case.
- **v9 adaptation:** Set config.env.lindat_specific_tests=true and override baseURL/handle fixtures to the local stack; keep this config as a LOCAL run artifact under dspace-ui-tests (a third repo) — it MUST NOT be pushed (push restriction allows only BE #1339 / FE #1316). Fix any selector drift from BS4->BS5 / standalone-component DOM changes in the page objects.
- **Verification:** npx playwright test --list | count of skipped == count of unconditional test.skip only; grep config.json for lindat_specific_tests:true and localhost URLs.

#### TEST-PW-1 — Run the full CLARIN Playwright suite (gate ON) against a provenance-clean stack as the E2E acceptance oracle  `[critical/M]`

- **Gap/impact:** The progress file's '17/17 Playwright' / 'END-TO-END green' claim is unproven: the CLARIN cases are gate-skipped and no run has executed them against a build equal to PR HEAD. E2E coverage for item file-list/preview (C1), download license gate (C2), static pages (C4), facets (M4), share (M5) has never actually run green on the PR.
- **Reuse source:** Same suite as TEST-PW-2 (dspace-ui-tests tests/tests/*.spec.ts). No new tests — this is the execution+acceptance step.
- **v9 adaptation:** Execute against the clarinv9 stack ONLY after TEST-PROVENANCE confirms deployed==PR HEAD (otherwise a green run certifies unshipped code, exactly the byHandle-200-but-absent-at-HEAD trap). Chromium headed or headless; retries=0 for the acceptance run.
- **Depends on:** TEST-PW-2, TEST-PROVENANCE
- **Verification:** probe-provenance.sh exits 0, THEN npx playwright test --reporter=list,json 2>&1 | tail -20 shows 'X passed' with skipped==unconditional-only; inspect the JSON report for 0 'unexpected'.

#### TEST-FE-KARMA-MODVANILLA — Re-apply fork spec deltas to modified-vanilla FE specs (search filter, item-page, submission)  `[medium/M]`

- **Gap/impact:** Beyond the 64 added specs, the fork modified several vanilla specs to assert CLARIN behavior (e.g. search-filter renderer selection incl. isoLanguage, item-page metadata rendering, submission section specs). At HEAD these are vanilla so the FE-side CLARIN deltas have no karma oracle.
- **Reuse source:** git diff --name-only --diff-filter=M dspace-7.6.3 origin/dtq-dev -- '*.spec.ts' to enumerate; apply only the CLARIN hunks (isoLanguage FilterType branch, item-page CLARIN metadata, submission clarin-license/notice steps).
- **v9 adaptation:** Standalone TestBed provider set (mirror component imports[]); apply fork hunks onto the v9-migrated spec, not wholesale overwrite; ensure @if-migration strict-null branches asserted per the FE ngIf gotcha.
- **Depends on:** TEST-FE-KARMA
- **Verification:** git diff --name-only --diff-filter=M dspace-7.6.3 origin/dtq-dev -- '*.spec.ts' to list; for each git diff HEAD origin/dtq-dev shows only v9-API differences; ng test --watch=false 0 FAILED.

#### TEST-MANUAL-55 — Manual-spec sign-off sheet #55 — every scenario routed to an automated case, a CLI+DB probe, or an explicit deferral  `[major/M]`

- **Gap/impact:** The dspace-customers #55 manual acceptance checklist is the human-facing definition of CLARIN done, but it is not mapped to objective probes and its source repo (dspace-customers) is NOT present in the workspace (only DSpace, dspace-angular, dspace-ui-tests exist), so the checklist must first be acquired.
- **Reuse source:** dspace-customers issue/checklist #55 (repo NOT in workspace — must be obtained: clone dspace-customers or export the #55 checklist via gh/issue API before this item can run). Route each row to an existing oracle: BE IT (TEST-BE-IT-*), FE karma (TEST-FE-KARMA), Playwright case (TEST-PW-1), or a CLI+DB probe.
- **v9 adaptation:** For each #55 scenario produce a bucket: AUTOMATED (name the IT/spec/PW case that proves it), PROBE (exact curl/CLI + SQL that proves it), or DEFERRED (finding-id + reason). No row left unmarked.
- **Depends on:** TEST-PW-1, TEST-BE-IT-CRIT, TEST-BE-IT-REST, TEST-FE-KARMA
- **Verification:** Open the sheet; assert row count == #55 scenario count and 0 rows without a bucket; spot-run 3 PROBE commands and 3 AUTOMATED tests.

#### TEST-MANUAL-411 — Manual-spec sign-off sheet #411 — every scenario routed to an automated case, a CLI+DB probe, or an explicit deferral  `[major/M]`

- **Gap/impact:** Same as #55 for the dspace-customers #411 checklist; unmapped and source repo not in workspace.
- **Reuse source:** dspace-customers checklist #411 (repo NOT in workspace — acquire first). Route rows to TEST-BE-IT-*/TEST-FE-KARMA/TEST-PW-1/CLI+DB probes.
- **v9 adaptation:** Bucket each row AUTOMATED/PROBE/DEFERRED with the exact oracle; env-gated rows (external Shibboleth IdP, S3 sync, Matomo PDF e2e, assetstore not imported) explicitly DEFERRED with finding-id.
- **Depends on:** TEST-PW-1, TEST-BE-IT-CRIT, TEST-BE-IT-REST, TEST-FE-KARMA
- **Verification:** Open the sheet; row count == #411 count, 0 unmarked; deferral rows reconcile 1:1 with the DoD deferral table.

#### TEST-CI-VERIFY — Prove the ported tests actually EXECUTE in the PR CI (gate d), not just exist  `[major/M]`

- **Gap/impact:** A green PR today means nothing because CI runs zero CLARIN tests. Even after porting, the tests must be wired so CI runs them and a failure turns CI red — otherwise 'CI green' remains hollow. This depends on the dtq-dev CI workflows (M11) being restored into both repos.
- **Reuse source:** origin/dtq-dev .github/workflows/** (BE 13, FE 18 workflow files — restored under the CI/Docker domain M11). This item verifies wiring, not authoring the workflows.
- **v9 adaptation:** Confirm the BE workflow invokes failsafe (integration ITs) AND surefire (unit tests, skipUnitTests=false) so both TEST-BE-IT-* and TEST-BE-UNIT run; confirm the FE workflow runs ng test headless; assert a deliberately-broken assertion turns the PR check red.
- **Depends on:** TEST-BE-IT-CRIT, TEST-BE-UNIT, TEST-FE-KARMA
- **Verification:** gh run view <latest #1339 run> --log | grep -E 'MetadataBitstreamRestRepositoryIT|ClarinTokenServiceTest' shows executed; gh run view <#1316> --log | grep spec.ts; confirm canary red then green.

#### TEST-GATE-0 — Master Definition-of-Done acceptance gate + runbook (the single 'done' check)  `[critical/M]`

- **Gap/impact:** There is no executable 'done' definition. The progress file's headline claims ('END-TO-END green', '17/17 Playwright') are false: CLARIN Playwright cases are gate-skipped, ported BE/FE CLARIN tests do not exist, and no runbook maps DoD (a)-(f) to objective, provenance-safe probes. An implementing agent cannot presently prove done vs vibes, and live probes today lie (byHandle 200 while code absent at HEAD).
- **Reuse source:** N/A — new glue. Assembles existing oracles: TEST-PROVENANCE guard, BE failsafe ITs + surefire unit tests, FE karma, the localized dspace-ui-tests Playwright run, dspace-customers #55/#411 sheets, and provenance-gated live curls.
- **v9 adaptation:** Codify DoD as 6 gates keyed to the audit's letters: (a) fork-delta reconciliation = 0 unexplained missing runtime files; (b) provenance-gated live functional probes pass (ONLY after TEST-PROVENANCE exits 0); (c) BE ITs + BE unit tests + FE karma + Playwright(gate ON) all green; (d) BE #1339 / FE #1316 CI green WITH the new tests executing (TEST-CI-VERIFY); (e) 0 unresolved critical/major audit findings; (f) fresh-clone deploy reproduces the working stack (registries/config/Docker/CI present). One aggregator script runs each and prints PASS/FAIL per gate + per audit-finding-id.
- **Depends on:** TEST-PROVENANCE, TEST-PW-1, TEST-BE-IT-CRIT, TEST-BE-IT-REST, TEST-BE-UNIT, TEST-OAI-ORACLE, TEST-FE-KARMA, TEST-MANUAL-55, TEST-MANUAL-411, TEST-CI-VERIFY
- **Verification:** Run probe-provenance.sh (exit 0), then the aggregator; confirm it prints PASS for all 6 gates and a non-empty per-finding table; independently re-run the four live probes and confirm 200/200/formats-present/200-on-about.html.

**Domain risks:** Live functional probes against the CURRENT running container are false-confidence: byHandle returns 200 while MetadataBitstreamRestRepository.java is absent at HEAD d27f85b. Any acceptance that trusts the live stack without TEST-PROVENANCE will certify code that is not in PR #1339/#1316. This is the single highest-risk trap and is why TEST-PROVENANCE gates gate (b). · Volume: 126 fork-added BE test files + 64 missing FE specs + modified-vanilla deltas is XL. Test-compile churn (jakarta, plural HAL bean names, standalone TestBed providers, @if strict-null) may cascade, and correctly-ported ITs will surface REAL feature-domain regressions — coupling the test schedule to the feature fixes (that is the intended function of the oracle, but it means TEST-GATE-0 cannot go green until the feature domains land). · OAI harvest (C5) has NO reusable fork IT — TEST-OAI-ORACLE is genuine new glue. If the crosswalk classes are not ported by the config/OAI domain, the OAI IT stays @Ignore'd and the live probe stays failing; risk of the deferral being mistaken for done. · dspace-customers (#55/#411 source) is not in the workspace; the manual sheets cannot be produced until that repo/checklist is obtained. Risk of TEST-MANUAL-* being blocked on access. · Playwright config lives in a third repo (dspace-ui-tests) that must NOT be pushed to #1339/#1316; risk of accidentally committing localhost overrides or the lindat toggle into a PR that only permits BE/FE changes. · actuator/info is 401-secured and does not expose git.commit publicly, so provenance must rely on docker image labels added under the M11 Docker domain — a cross-domain dependency that, if skipped, leaves provenance unverifiable. · Deferred-but-@Ignore'd tests (OAI cmdi, S3 sync/O1, Matomo PDF e2e, external Shibboleth) risk silently masquerading as done unless the DoD deferral table is enforced as a first-class, reconciled artifact.

**Open questions:** OUT OF DOMAIN (feature port): MetadataBitstreamRestRepository.java and its wrapper/rest/resource/converter (C1) are absent at HEAD yet the running container serves them — the C1 owner MUST actually land these files in PR #1339; testing can only gate, not supply, the feature. Who confirms the running container is rebuilt from PR HEAD? · OUT OF DOMAIN (OAI/config): the OAI crosswalk layer (SharedSaxonProcessor, SpecialItemService, xoai.xml/oai.cfg wiring, 28 dspace-oai classes) must land before TEST-OAI-ORACLE can pass; live ListMetadataFormats currently exposes none of cmdi/olac/metasharev2/elg. Confirm C5 owner and target module (dspace-oai vs config). · OUT OF DOMAIN (M11 Docker/CI): TEST-PROVENANCE needs images stamped with org.opencontainers.image.revision=$GIT_SHA and the restored dtq-dev CI workflows to actually execute the ported tests. If M11 is not done, provenance is unverifiable and gate (d) stays hollow. · ACCESS: dspace-customers repo (source of #55/#411 manual checklists) is not in the workspace — how is it obtained (clone URL / gh issue export)? Blocks TEST-MANUAL-55/411. · OUT OF DOMAIN (S4): /api/core/metadatavalues returns 404 live; MetadataValueRestRepository owner must land the endpoint for MetadataValueRestRepositoryIT to pass. Confirm ownership. · POLICY: is the implementing agent permitted to rebuild+redeploy the clarinv9 docker stack (needed for provenance-clean live probes and Playwright)? The planner is read-only; the acceptance gate assumes the implementer can rebuild locally while pushing only to #1339/#1316.

## Evaluator findings (verbatim, for traceability)

Plan gaps (closed by GAP-1..5 above):
- C2 under-specified: BE-GATE-1 says only 'wire the gate into AuthorizeServiceImpl (5-arg overload)' but does NOT name porting ClarinBitstreamServiceImpl.java, which I confirmed is present in origin/dtq-dev and ABSENT at HEAD and is explicitly listed by the audit as required for the download gate. AuthorizationBitstreamUtils exists at HEAD, but the service that issues/validates download-tokens is unported. Risk: the enforcement half lands while the token-grant half is silently missing.
- C2 positive/token path has NO acceptance check anywhere. Every C2 criterion is a negative gate (anonymous /content -> 401; open bitstream stays 200). There is no AC of the form 'a holder of a valid download-token GET /content returns 200', so the token issuance+consumption subsystem can regress undetected. A blanket deny-all misconfiguration would PASS all listed C2 checks.
- FE preview VIEWER component not owned: C1 is 'file list + preview dead', but the FE side is covered only by 'renders 2 file rows' + BE-PREVIEW-8 (BE content chain). No work item verifies the FE preview/viewer component and its route actually render the previewed content. If that component/wiring was also dropped in the standalone migration, byHandle 200 alone will not restore preview.
- Fresh-deploy Solr configset/core provisioning is not an owned work item. BE-OAI-8 populates the existing OAI core on the dev-5 stack, but no item covers CLARIN Solr schema/configset deltas (statistics, authority, oai fields) needed for a genuinely fresh clone — inconsistent with the plan's own stated fresh-deploy-correctness goal behind M13/M12.
- OAI-PMH harvest statistics are an accepted silent loss (native org.dspace.matomo has no OAI tracker; dtq-dev ClarinMatomoOAITracker not re-ported). This interacts with C5 (VLO/WebLicht harvest) and is acknowledged in native-v9 notes, but no work item or AC records/verifies the deliberate loss in the progress file — it should be an explicit line in PROGRESS-FILE-honest-rewrite.

Unrealistic criteria (addressed by the hardening rules):
- 'FE item page renders exactly 2 file rows' and the pinned UUIDs (gated faf919b4, open f15230ca) are dataset-specific to dev-5; a data refresh changes counts/UUIDs and breaks the check. The plan acknowledges fixture fragility yet still lists these as live acceptance gates rather than deriving them from self-seeded ITs.
- Phase 2 exit 'complex/autocomplete/admin-only/license/notice steps render and function' — 'function' is subjective with no named per-step test or specific UI/HTTP assertion; not machine-checkable as written.
- Negative-gate-only C2/C3 acceptance cannot distinguish correct gating from total breakage: a 401 on gated /content is also produced by an over-blocking bug. Without the paired authorized-200 assertion (present for open bitstreams, MISSING for token holders) the criterion can be satisfied by a broken deny-all gate.
- Several 'acceptance' gates are conditional on deferred infrastructure that may never run in this effort: Matomo e2e and the full CLARIN Playwright suite are gated on M11 Docker (itself a deferred port), and positive byte delivery is gated on a seeded assetstore (DEV-RUN-2) that contradicts the documented-deferred 'assetstore not imported' status — so these DoD items risk being declared 'deferred/green' without ever executing.
- '~82 static HTML pages return 200' is asserted but only one example (/static/cookies.html) is a concrete probe; without an enumerated manifest check (count == expected filenames) a partial port of the 82 files passes the spot-check.

Top risks:
- Green-but-broken recurrence (the exact failure the audit caught): because assetstore and Docker/Matomo infra are env-gated, most live ACs are negative gates or metadata-only 200s that short-circuit before file read. Positive byte delivery, token-grant, and OAI harvest CONTENT are provable only via ITs that stand up their own assetstore — if those ITs are weak or skipped, the PRs can go green with the same regressions intact.
- Scope vs constraints mismatch: 100+ work items across BE/FE/OAI/tests/CI/Docker are funneled into exactly two existing PRs (#1339/#1316), no new branches, no container rebuild, while a parallel process edits the same tree. I confirmed the 6 C1 files are still untracked ('??'), so the clobber/merge-loss risk called out in BE-BASE-0 is live and material.
- Modified-vanilla hand-reconcile is the highest defect risk: ItemUtils (~99 lines), XOAI (~72), MetadataValidation, and AuthorizeServiceImpl carry heavy v9 drift; fork patches will not apply cleanly and a botched reconcile silently breaks the OAI crosswalk (C5) or the download gate (C2) while still compiling and passing negative gates.
- PLURAL_NAME / provide-core models[] bean-resolution bug class recurs silently: it already broke M2/M3; BE-CHK-5 is flagged, but any REST repository/link ported verbatim from dtq-dev (autocomplete cluster, metadatavalues, checksum, byHandle) is exposed to the same 404, and only grep-level ACs guard against it.
- C2's missing ClarinBitstreamServiceImpl port (planGap 1) plus no positive token AC could ship a download gate that enforces denial but cannot actually issue/honor a license-agreement token — leaving legitimate downloads permanently blocked, a regression that the current negative-only acceptance set would not catch.
