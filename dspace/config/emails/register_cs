## E-mail sent to DSpace users when they register for an account
##
## Parameters: {0} is expanded to a special registration URL
##
## See org.dspace.core.Email for information on the format of this file.
##
#set($subject = "${config.get('dspace.shortname')} Registrace účtu")
#set($phone = ${config.get('mail.message.helpdesk.telephone')})
Pro dokončení registrace účtu v DSpace klikněte, prosím, na odkaz níže:

  ${params[0]}

Potřebujete-li pomoci s vaším účtem, pošlete prosím email na
adresu ${config.get("mail.helpdesk")}
#if( $phone )
nebo nám zavolejte na ${phone}.
#end

Váš tím DSpace